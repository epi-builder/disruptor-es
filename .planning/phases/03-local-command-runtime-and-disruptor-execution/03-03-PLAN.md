---
phase: 03-local-command-runtime-and-disruptor-execution
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - crates/es-runtime/src/lib.rs
  - crates/es-runtime/src/disruptor_path.rs
  - crates/es-runtime/src/cache.rs
  - crates/es-runtime/src/shard.rs
  - crates/es-runtime/src/gateway.rs
  - crates/es-runtime/tests/shard_disruptor.rs
  - .planning/phases/03-local-command-runtime-and-disruptor-execution/03-VALIDATION.md
autonomous: true
requirements: [RUNTIME-03, RUNTIME-04]
must_haves:
  truths:
    - "Shard runtime state owns aggregate cache and dedupe cache locally without production `Arc<Mutex<_>>` business-state maps."
    - "The disruptor path uses non-blocking publication and maps a full ring to a typed shard overload."
    - "Accepted routed commands are not processable until the disruptor consumer/poller releases the matching tenant-scoped unique handoff token."
    - "The chosen disruptor-to-async bridge compiles and releases ordered handoff tokens without nested Tokio blocking."
  artifacts:
    - path: "crates/es-runtime/src/cache.rs"
      provides: "Shard-local aggregate and dedupe caches"
      contains: "DedupeCache"
    - path: "crates/es-runtime/src/disruptor_path.rs"
      provides: "Narrow disruptor try_publish wrapper"
      contains: "try_publish"
    - path: "crates/es-runtime/src/shard.rs"
      provides: "ShardState, shard-owned dedupe cache, ShardHandle, tenant-scoped unique pending envelope table, and disruptor-released ordered handoff buffer"
      contains: "pub struct ShardHandle"
  key_links:
    - from: "crates/es-runtime/src/disruptor_path.rs"
      to: "disruptor"
      via: "try_publish wrapper"
      pattern: "try_publish"
    - from: "crates/es-runtime/src/shard.rs"
      to: "crates/es-runtime/src/cache.rs"
      via: "ShardState owns AggregateCache and DedupeCache"
      pattern: "AggregateCache.*DedupeCache"
    - from: "crates/es-runtime/src/shard.rs"
      to: "crates/es-runtime/src/disruptor_path.rs"
      via: "ShardHandle publishes a tenant-scoped unique token, then drains disruptor-released tokens before recording processable handoffs"
      pattern: "drain_released_handoffs"
---

<objective>
Prove shard-local state ownership and compile the disruptor execution handoff.

Purpose: Phase 03 research has medium confidence in the exact disruptor/async storage bridge, so this plan implements the bounded feasibility proof before commit-gated command processing depends on it.
Output: Shard-local cache, disruptor non-blocking publish wrapper, shard state handoff, tests for full-ring overload and no production global state locks.
</objective>

<execution_context>
@/Users/epikem/.codex/get-shit-done/workflows/execute-plan.md
@/Users/epikem/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-local-command-runtime-and-disruptor-execution/03-02-SUMMARY.md
@.planning/phases/03-local-command-runtime-and-disruptor-execution/03-RESEARCH.md
@.planning/phases/03-local-command-runtime-and-disruptor-execution/03-PATTERNS.md
@.planning/phases/03-local-command-runtime-and-disruptor-execution/03-VALIDATION.md
@crates/es-runtime/src/lib.rs
@crates/es-runtime/src/command.rs
@crates/es-runtime/src/error.rs
@crates/es-runtime/src/router.rs
@crates/es-kernel/src/lib.rs

<interfaces>
External disruptor API pattern from Phase 03 research:
```rust
match producer.try_publish(|slot| {
    *slot = runtime_event;
}) {
    Ok(sequence) => Ok(sequence),
    Err(disruptor::RingBufferFull) => Err(RuntimeError::ShardOverloaded { shard_id }),
}
```

From Plan 02:
```rust
pub struct ShardId(usize);
impl ShardId { pub const fn value(self) -> usize; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement shard-local aggregate and dedupe caches</name>
  <files>crates/es-runtime/src/lib.rs, crates/es-runtime/src/cache.rs, crates/es-runtime/tests/shard_disruptor.rs</files>
  <read_first>
    crates/es-runtime/src/lib.rs
    crates/es-runtime/src/cache.rs
    crates/es-runtime/tests/shard_disruptor.rs
    crates/es-kernel/src/lib.rs
    .planning/phases/03-local-command-runtime-and-disruptor-execution/03-RESEARCH.md
    .planning/phases/03-local-command-runtime-and-disruptor-execution/03-PATTERNS.md
  </read_first>
  <behavior>
    - Test: `AggregateCache::<A>::get_or_default(stream_id)` returns default state for a missing stream and stores it locally.
    - Test: `commit_state(stream_id, staged_state)` replaces the cached state only when called.
    - Test: `DedupeCache` records and returns committed append summaries by tenant/idempotency key while remaining shard-local.
    - Test: production runtime source contains no `Arc<Mutex<...State|Cache|HashMap...>` pattern.
  </behavior>
  <action>Add `mod cache;` and `pub use cache::{AggregateCache, DedupeCache, DedupeKey, DedupeRecord};` to `crates/es-runtime/src/lib.rs`. Create `crates/es-runtime/src/cache.rs` with `pub struct AggregateCache<A: es_kernel::Aggregate> { states: std::collections::HashMap<es_core::StreamId, A::State> }`. Implement `Default`, `pub fn new() -> Self`, `pub fn get_or_default(&mut self, stream_id: &es_core::StreamId) -> A::State` that inserts `A::State::default()` if missing and returns a cloned state, `pub fn commit_state(&mut self, stream_id: es_core::StreamId, state: A::State)`, `pub fn get(&self, stream_id: &es_core::StreamId) -> Option<&A::State>`, and `pub fn len(&self) -> usize`. In the same file create shard-owned dedupe cache types: `#[derive(Clone, Debug, Eq, Hash, PartialEq)] pub struct DedupeKey { pub tenant_id: es_core::TenantId, pub idempotency_key: String }`, `#[derive(Clone, Debug, PartialEq)] pub struct DedupeRecord { pub append: es_store_postgres::CommittedAppend }`, and `pub struct DedupeCache { records: std::collections::HashMap<DedupeKey, DedupeRecord> }` with `new`, `get`, `record`, and `len` methods. `DedupeCache` is only a shard-local optimization and must not bypass `RuntimeEventStore::append`; durable PostgreSQL dedupe remains authoritative. Do not add `moka`; do not add any production `Arc`, `Mutex`, or `RwLock` around either cache. Create `crates/es-runtime/tests/shard_disruptor.rs` with a local counter aggregate and cache tests named `shard_cache_inserts_default_state_locally`, `shard_cache_commits_only_explicit_state`, and `shard_dedupe_cache_records_tenant_scoped_committed_append`.</action>
  <verify>
    <automated>cargo test -p es-runtime shard_cache && ! rg 'Arc<Mutex<.*(State|Cache|HashMap)|RwLock<.*(State|Cache|HashMap)' crates/es-runtime/src</automated>
  </verify>
  <acceptance_criteria>
    `rg 'pub struct AggregateCache<A: Aggregate>' crates/es-runtime/src/cache.rs` returns a match.
    `rg 'HashMap<StreamId, A::State>' crates/es-runtime/src/cache.rs` returns a match.
    `rg 'pub struct DedupeCache' crates/es-runtime/src/cache.rs` returns a match.
    `rg 'pub struct DedupeKey' crates/es-runtime/src/cache.rs` returns a match.
    `rg 'pub struct DedupeRecord' crates/es-runtime/src/cache.rs` returns a match.
    `rg 'pub fn commit_state' crates/es-runtime/src/cache.rs` returns a match.
    `rg 'shard_cache_inserts_default_state_locally' crates/es-runtime/tests/shard_disruptor.rs` returns a match.
    `rg 'shard_cache_commits_only_explicit_state' crates/es-runtime/tests/shard_disruptor.rs` returns a match.
    `rg 'shard_dedupe_cache_records_tenant_scoped_committed_append' crates/es-runtime/tests/shard_disruptor.rs` returns a match.
    `! rg 'Arc<Mutex<.*(State|Cache|HashMap)|RwLock<.*(State|Cache|HashMap)' crates/es-runtime/src` exits successfully with no matches.
    `cargo test -p es-runtime shard_cache` exits 0.
  </acceptance_criteria>
  <done>Shard aggregate and dedupe cache state are processor-local data, not global mutable business-state locks, and runtime dedupe remains subordinate to durable store dedupe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compile disruptor non-blocking publication path</name>
  <files>crates/es-runtime/src/lib.rs, crates/es-runtime/src/disruptor_path.rs, crates/es-runtime/tests/shard_disruptor.rs</files>
  <read_first>
    crates/es-runtime/src/lib.rs
    crates/es-runtime/src/disruptor_path.rs
    crates/es-runtime/tests/shard_disruptor.rs
    crates/es-runtime/src/error.rs
    crates/es-runtime/src/router.rs
    .planning/phases/03-local-command-runtime-and-disruptor-execution/03-RESEARCH.md
    .planning/phases/03-local-command-runtime-and-disruptor-execution/03-PATTERNS.md
  </read_first>
  <behavior>
    - Test: `DisruptorPath::new(shard_id, 0, factory)` returns `RuntimeError::InvalidRingSize`.
    - Test: filling a small ring with `try_publish` returns `RuntimeError::ShardOverloaded { shard_id }` instead of blocking.
    - Test: a successfully published token is observable only through `DisruptorPath::poll_released` or equivalent consumer/poller drain API.
    - Test: source contains no `block_on`, `Handle::current().block_on`, or nested runtime creation in `disruptor_path.rs`.
  </behavior>
  <action>Add `mod disruptor_path;` and `pub use disruptor_path::{DisruptorPath, ReleasedHandoff};` to `crates/es-runtime/src/lib.rs`. Create `crates/es-runtime/src/disruptor_path.rs` as a narrow wrapper around `disruptor` 4.0.0. Define `#[derive(Clone, Debug, PartialEq)] pub struct ReleasedHandoff<E> { pub sequence: u64, pub event: E }` and `pub struct DisruptorPath<E: Clone + Send + 'static>` with fields for `shard_id: ShardId`, the crate producer type required by the actual `disruptor` API, and a private release queue populated only by the disruptor consumer/poller path. Implement `pub fn new(shard_id: ShardId, ring_size: usize, event_factory: impl Fn() -> E + Send + Sync + 'static) -> RuntimeResult<Self>` rejecting zero with `RuntimeError::InvalidRingSize` and building a single-producer disruptor using `disruptor::build_single_producer`, `disruptor::BusySpinWithSpinLoopHint`, and a stateful handler or event poller that pushes `ReleasedHandoff { sequence, event: slot.clone() }` into the private release queue. The handler/poller must not do async storage and must be the only code path that creates released handoffs. Implement `pub fn try_publish(&mut self, event: E) -> RuntimeResult<u64>` using `producer.try_publish(|slot| { *slot = event; })` and mapping `disruptor::RingBufferFull` to `RuntimeError::ShardOverloaded { shard_id: self.shard_id.value() }`; convert the successful sequence to `u64` only for runtime diagnostics/tests and never call it a durable position. Implement `pub fn poll_released(&mut self) -> Vec<ReleasedHandoff<E>>` or an equivalent drain method that advances the disruptor poller/consumer as required by the crate API and returns only tokens emitted by that path. If the exact producer/poller type names are private or generic-heavy, hide them behind private fields or wrappers inside this module while preserving the public `DisruptorPath` API. Extend `shard_disruptor.rs` with tests named `disruptor_path_rejects_zero_ring_size`, `disruptor_path_returns_shard_overloaded_when_ring_is_full`, and `disruptor_path_releases_published_tokens_through_consumer_path`. Do not use blocking `publish`.</action>
  <verify>
    <automated>cargo test -p es-runtime disruptor_path && ! rg 'block_on|Runtime::new|Handle::current' crates/es-runtime/src/disruptor_path.rs</automated>
  </verify>
  <acceptance_criteria>
    `rg 'build_single_producer' crates/es-runtime/src/disruptor_path.rs` returns a match.
    `rg 'BusySpinWithSpinLoopHint' crates/es-runtime/src/disruptor_path.rs` returns a match.
    `rg 'pub struct ReleasedHandoff' crates/es-runtime/src/disruptor_path.rs` returns a match.
    `rg '\.try_publish\(' crates/es-runtime/src/disruptor_path.rs` returns a match.
    `rg 'poll_released|drain_released' crates/es-runtime/src/disruptor_path.rs` returns a match.
    `! rg '\.publish\(' crates/es-runtime/src/disruptor_path.rs` exits successfully with no matches.
    `rg 'RuntimeError::ShardOverloaded' crates/es-runtime/src/disruptor_path.rs` returns a match.
    `rg 'disruptor_path_rejects_zero_ring_size' crates/es-runtime/tests/shard_disruptor.rs` returns a match.
    `rg 'disruptor_path_returns_shard_overloaded_when_ring_is_full' crates/es-runtime/tests/shard_disruptor.rs` returns a match.
    `rg 'disruptor_path_releases_published_tokens_through_consumer_path' crates/es-runtime/tests/shard_disruptor.rs` returns a match.
    `! rg 'block_on|Runtime::new|Handle::current' crates/es-runtime/src/disruptor_path.rs` exits successfully with no matches.
    `cargo test -p es-runtime disruptor_path` exits 0.
  </acceptance_criteria>
  <done>The disruptor integration compiles, uses non-blocking publication, exposes typed shard overload, and avoids hidden async blocking.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add shard state ordered handoff and disruptor-backed shard handle</name>
  <files>crates/es-runtime/src/lib.rs, crates/es-runtime/src/shard.rs, crates/es-runtime/src/gateway.rs, crates/es-runtime/tests/shard_disruptor.rs, .planning/phases/03-local-command-runtime-and-disruptor-execution/03-VALIDATION.md</files>
  <read_first>
    crates/es-runtime/src/lib.rs
    crates/es-runtime/src/shard.rs
    crates/es-runtime/tests/shard_disruptor.rs
    crates/es-runtime/src/cache.rs
    crates/es-runtime/src/command.rs
    crates/es-runtime/src/disruptor_path.rs
    crates/es-runtime/src/gateway.rs
    crates/es-runtime/src/router.rs
    .planning/phases/03-local-command-runtime-and-disruptor-execution/03-RESEARCH.md
    .planning/phases/03-local-command-runtime-and-disruptor-execution/03-PATTERNS.md
    .planning/phases/03-local-command-runtime-and-disruptor-execution/03-VALIDATION.md
  </read_first>
  <behavior>
    - Test: `ShardState::<A>::new(ShardId::new(1)).shard_id().value()` returns `1`.
    - Test: `record_released_handoff(sequence, envelope)` stores disruptor-released handoffs in ascending sequence order for later async processing.
    - Test: `ShardHandle::accept_routed(routed)` publishes through `DisruptorPath::try_publish` and stores the envelope as pending, but does not make it visible to `process_next_handoff`.
    - Test: `ShardHandle::drain_released_handoffs()` polls/drains `DisruptorPath` release output and only then records the matching envelope in `ShardState`.
    - Test: `process_next_handoff` or `pop_handoff` returns no work after `accept_routed` until a release has been drained from the disruptor path.
    - Test: two accepted commands with the same tenant, stream ID, and idempotency key remain distinct pending handoffs until their two unique release tokens are drained; neither accepted command overwrites the other in `pending`.
    - Test: two accepted commands with different tenants but the same stream ID and idempotency key remain distinct pending handoffs and drain back to their original tenant-specific envelopes.
    - Test: grep confirms production shard code contains no global business-state locks and no storage append call inside the disruptor handler.
  </behavior>
  <action>Add `mod shard;` and `pub use shard::{ShardHandle, ShardHandoff, ShardHandoffToken, ShardState};` to `crates/es-runtime/src/lib.rs`. Create `crates/es-runtime/src/shard.rs` with `pub struct ShardHandoff<A: Aggregate> { pub sequence: u64, pub envelope: CommandEnvelope<A> }`, `#[derive(Clone, Copy, Debug, Eq, Hash, PartialEq)] pub struct LocalHandoffId(u64)` with `pub const fn new(value: u64) -> Self` and `pub const fn value(self) -> u64`, `#[derive(Clone, Debug, Eq, Hash, PartialEq)] pub struct ShardHandoffToken { pub tenant_id: es_core::TenantId, pub stream_id: es_core::StreamId, pub idempotency_key: String, pub local_handoff_id: LocalHandoffId }`, and `pub struct ShardState<A: Aggregate> { shard_id: ShardId, cache: AggregateCache<A>, dedupe: DedupeCache, handoffs: std::collections::VecDeque<ShardHandoff<A>> }`. Implement `pub fn new(shard_id: ShardId) -> Self`, `pub const fn shard_id(&self) -> ShardId`, `pub fn cache(&self) -> &AggregateCache<A>`, `pub fn cache_mut(&mut self) -> &mut AggregateCache<A>`, `pub fn dedupe(&self) -> &DedupeCache`, `pub fn dedupe_mut(&mut self) -> &mut DedupeCache`, `pub fn record_released_handoff(&mut self, sequence: u64, envelope: CommandEnvelope<A>)`, `pub fn pop_handoff(&mut self) -> Option<ShardHandoff<A>>`, and `pub fn pending_handoffs(&self) -> usize`. Add `pub struct ShardHandle<A: Aggregate>` owning `shard_id: ShardId`, `state: ShardState<A>`, `path: DisruptorPath<ShardHandoffToken>`, `pending: std::collections::HashMap<ShardHandoffToken, CommandEnvelope<A>>`, and `next_local_handoff_id: u64`. Implement `pub fn new(shard_id: ShardId, ring_size: usize) -> RuntimeResult<Self>`, `pub fn state(&self) -> &ShardState<A>`, `pub fn state_mut(&mut self) -> &mut ShardState<A>`, `pub fn pending_len(&self) -> usize`, `pub fn accept_routed(&mut self, routed: RoutedCommand<A>) -> RuntimeResult<u64>`, and `pub fn drain_released_handoffs(&mut self) -> RuntimeResult<usize>`. `accept_routed` must verify `routed.shard_id == self.shard_id`, allocate a fresh `LocalHandoffId` from `self.next_local_handoff_id` before incrementing it, create a `ShardHandoffToken` from `routed.envelope.metadata.tenant_id.clone()`, `routed.envelope.stream_id.clone()`, `routed.envelope.idempotency_key.clone()`, and the fresh local ID, call `self.path.try_publish(token.clone())`, store the envelope in `self.pending` keyed by the full token, and return the local sequence without calling `record_released_handoff`. The local ID is required even when tenant/stream/idempotency are identical because duplicate in-flight submissions can exist before durable dedupe resolves them; `tenant_id` is required so same stream/idempotency strings in different tenants cannot collide. Do not key `pending` by `(stream_id, idempotency_key)` or any token that lacks tenant and unique local handoff identity. If `try_publish` returns an error, do not insert into `pending` and do not advance the local handoff ID unless the implementation has already published the token successfully. `drain_released_handoffs` must call `self.path.poll_released()` or the equivalent release drain method, look up each released full token in `self.pending`, remove the matching envelope, and call `self.state.record_released_handoff(released.sequence, envelope)`. This is the critical RUNTIME-04 contract: the processable handoff used by Plan 04's `process_next_handoff` is created only from the disruptor consumer/poller release path. A command accepted by `accept_routed` must not be processable by `process_next_handoff` until `drain_released_handoffs` observes its full tenant-scoped unique token from `DisruptorPath`. The disruptor handler in this plan must only release ordered tokens; it must not do async storage. Extend tests with `shard_state_records_ordered_handoffs`, `shard_handle_publishes_routed_command_before_release`, `shard_command_cannot_be_processed_until_disruptor_release_is_drained`, `shard_pending_keeps_duplicate_inflight_stream_and_idempotency_commands_distinct`, and `shard_pending_keeps_cross_tenant_same_key_commands_distinct`. Update `03-VALIDATION.md` so task `03-02-01` references `cargo test -p es-runtime shard_cache && cargo test -p es-runtime disruptor_path && cargo test -p es-runtime shard_handle`, notes the disruptor bridge compiles without `block_on`, and records that accepted routed commands become processable only through disruptor-released tenant-scoped unique handoffs.</action>
  <verify>
    <automated>cargo test -p es-runtime shard_state && ! rg 'append\(' crates/es-runtime/src/shard.rs && ! rg 'Arc<Mutex<.*(State|Cache|HashMap)|RwLock<.*(State|Cache|HashMap)' crates/es-runtime/src</automated>
  </verify>
  <acceptance_criteria>
    `rg 'pub struct ShardState<A: Aggregate>' crates/es-runtime/src/shard.rs` returns a match.
    `rg 'dedupe: DedupeCache' crates/es-runtime/src/shard.rs` returns a match.
    `rg 'pub struct ShardHandle<A: Aggregate>' crates/es-runtime/src/shard.rs` returns a match.
    `rg 'pub struct ShardHandoffToken' crates/es-runtime/src/shard.rs` returns a match.
    `rg 'pub struct LocalHandoffId' crates/es-runtime/src/shard.rs` returns a match.
    `rg 'tenant_id: es_core::TenantId' crates/es-runtime/src/shard.rs` returns a match.
    `rg 'local_handoff_id: LocalHandoffId' crates/es-runtime/src/shard.rs` returns a match.
    `rg 'DisruptorPath<ShardHandoffToken>' crates/es-runtime/src/shard.rs` returns a match.
    `rg 'VecDeque<ShardHandoff<A>>' crates/es-runtime/src/shard.rs` returns a match.
    `rg 'pending: .*HashMap<ShardHandoffToken, CommandEnvelope<A>>' crates/es-runtime/src/shard.rs` returns a match.
    `rg 'next_local_handoff_id' crates/es-runtime/src/shard.rs` returns a match.
    `rg 'pub fn record_released_handoff' crates/es-runtime/src/shard.rs` returns a match.
    `rg 'pub fn pop_handoff' crates/es-runtime/src/shard.rs` returns a match.
    `rg 'pub fn pending_len' crates/es-runtime/src/shard.rs` returns a match.
    `rg 'pub fn accept_routed' crates/es-runtime/src/shard.rs` returns a match.
    `rg 'pub fn drain_released_handoffs' crates/es-runtime/src/shard.rs` returns a match.
    `rg 'try_publish' crates/es-runtime/src/shard.rs` returns a match.
    `rg 'poll_released|drain_released' crates/es-runtime/src/shard.rs` returns a match.
    `rg 'shard_state_records_ordered_handoffs' crates/es-runtime/tests/shard_disruptor.rs` returns a match.
    `rg 'shard_handle_publishes_routed_command_before_release' crates/es-runtime/tests/shard_disruptor.rs` returns a match.
    `rg 'shard_command_cannot_be_processed_until_disruptor_release_is_drained' crates/es-runtime/tests/shard_disruptor.rs` returns a match.
    `rg 'shard_pending_keeps_duplicate_inflight_stream_and_idempotency_commands_distinct' crates/es-runtime/tests/shard_disruptor.rs` returns a match.
    `rg 'shard_pending_keeps_cross_tenant_same_key_commands_distinct' crates/es-runtime/tests/shard_disruptor.rs` returns a match.
    `! rg 'append\(' crates/es-runtime/src/shard.rs` exits successfully with no matches.
    `! rg 'Arc<Mutex<.*(State|Cache|HashMap)|RwLock<.*(State|Cache|HashMap)' crates/es-runtime/src` exits successfully with no matches.
    `cargo test -p es-runtime shard_state && cargo test -p es-runtime shard_handle` exits 0.
  </acceptance_criteria>
  <done>Shard-owned state can receive processable ordered handoffs only after non-blocking disruptor publication and consumer/poller release, while keeping async storage work outside the synchronous disruptor processor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gateway -> disruptor path | Accepted commands cross from bounded Tokio ingress into per-shard in-process sequencing. |
| disruptor path -> shard state | Ring events mutate shard-local handoff/cache state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Denial of Service | DisruptorPath | mitigate | Use `try_publish` and map `RingBufferFull` to `RuntimeError::ShardOverloaded`; forbid blocking `publish`. |
| T-03-11 | Tampering | AggregateCache | mitigate | Stage and commit cache state through explicit methods; do not expose global mutable cache handles. |
| T-03-12 | Denial of Service | disruptor async bridge | mitigate | Keep async storage out of the sync disruptor handler and grep for `block_on`, nested runtime creation, and `Handle::current`. |
| T-03-13 | Repudiation | disruptor sequence diagnostics | mitigate | Treat disruptor sequence only as local diagnostic handoff order; do not expose it as committed global position. |
| T-03-14 | Tampering | ShardHandle pending handoff map | mitigate | Include `tenant_id` and a fresh `LocalHandoffId` in each `ShardHandoffToken`; add duplicate in-flight and cross-tenant same-key tests so pending commands cannot overwrite each other before disruptor release. |
</threat_model>

<verification>
Run `cargo test -p es-runtime shard_cache`, `cargo test -p es-runtime disruptor_path`, `cargo test -p es-runtime shard_state`, `cargo test -p es-runtime shard_handle`, `! rg 'block_on|Runtime::new|Handle::current' crates/es-runtime/src/disruptor_path.rs`, and `! rg 'Arc<Mutex<.*(State|Cache|HashMap)|RwLock<.*(State|Cache|HashMap)' crates/es-runtime/src`.
</verification>

<success_criteria>
RUNTIME-03 and RUNTIME-04 are satisfied by shard-owned aggregate and dedupe `HashMap` cache state, accepted commands becoming processable only after non-blocking disruptor publication and consumer/poller release of tenant-scoped unique tokens, and a compiled bridge that keeps async storage outside the synchronous disruptor handler.
</success_criteria>

<output>
After completion, create `.planning/phases/03-local-command-runtime-and-disruptor-execution/03-03-SUMMARY.md`.
</output>
