---
phase: 03-local-command-runtime-and-disruptor-execution
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - crates/es-runtime/src/lib.rs
  - crates/es-runtime/src/engine.rs
  - crates/es-runtime/src/shard.rs
  - crates/es-runtime/src/gateway.rs
  - crates/es-runtime/src/command.rs
  - crates/es-runtime/tests/runtime_flow.rs
  - .planning/phases/03-local-command-runtime-and-disruptor-execution/03-VALIDATION.md
autonomous: true
requirements: [RUNTIME-01, RUNTIME-03, RUNTIME-05, RUNTIME-06]
must_haves:
  truths:
    - "Command replies are sent only after `RuntimeEventStore::append` returns committed or duplicate durable outcome."
    - "Cache misses rehydrate aggregate state from `RuntimeEventStore::load_rehydration` before `A::decide` runs on existing streams."
    - "Optimistic concurrency conflicts return typed runtime conflict errors and leave shard cache unchanged."
    - "Shard-local dedupe cache records committed or duplicate append summaries after durable append, but duplicate outcomes never apply newly decided events to aggregate cache."
    - "A production runtime engine owns gateway receive, shard handoff, disruptor release drain, store append, codec usage, and reply delivery end-to-end."
    - "Runtime flow tests cover success, overload, conflict, and unavailable/store-failure outcomes through the command path."
  artifacts:
    - path: "crates/es-runtime/src/engine.rs"
      provides: "CommandEngine runtime loop that connects gateway receiver, shard handles, store, codec, and commit-gated replies"
      contains: "pub struct CommandEngine"
    - path: "crates/es-runtime/src/shard.rs"
      provides: "Commit-gated command processor"
      contains: "process_next_handoff"
    - path: "crates/es-runtime/tests/runtime_flow.rs"
      provides: "Runtime flow tests for commit and conflict semantics"
      contains: "reply_is_sent_after_append_commit"
    - path: ".planning/phases/03-local-command-runtime-and-disruptor-execution/03-VALIDATION.md"
      provides: "Nyquist validation status for Phase 03"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "crates/es-runtime/src/shard.rs"
      to: "crates/es-runtime/src/store.rs"
      via: "RuntimeEventStore::append"
      pattern: "\\.append\\("
    - from: "crates/es-runtime/src/shard.rs"
      to: "crates/es-runtime/src/store.rs"
      via: "RuntimeEventStore::load_rehydration before decide on cache miss"
      pattern: "load_rehydration"
    - from: "crates/es-runtime/src/shard.rs"
      to: "crates/es-runtime/src/cache.rs"
      via: "commit aggregate cache only on fresh commit; record dedupe after committed or duplicate append success"
      pattern: "AppendOutcome::Committed.*commit_state"
    - from: "crates/es-runtime/src/engine.rs"
      to: "crates/es-runtime/src/gateway.rs"
      via: "owns gateway receiver and dispatches RoutedCommand to shard handles"
      pattern: "recv\\(\\)\\.await"
    - from: "crates/es-runtime/src/engine.rs"
      to: "crates/es-runtime/src/shard.rs"
      via: "accept_routed, drain_released_handoffs, and process_next_handoff"
      pattern: "accept_routed.*drain_released_handoffs.*process_next_handoff"
---

<objective>
Wire command processing so replies and cache mutation are gated by durable append.

Purpose: Phase 03 is only correct if the event store remains the source of truth. The runtime must not reply success or advance hot cache state before storage commit, and conflicts must be typed and cache-preserving.
Output: Commit-gated shard command processing, runtime flow coverage, and final validation status for RUNTIME-01 through RUNTIME-06.
</objective>

<execution_context>
@/Users/epikem/.codex/get-shit-done/workflows/execute-plan.md
@/Users/epikem/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-local-command-runtime-and-disruptor-execution/03-03-SUMMARY.md
@.planning/phases/03-local-command-runtime-and-disruptor-execution/03-RESEARCH.md
@.planning/phases/03-local-command-runtime-and-disruptor-execution/03-PATTERNS.md
@.planning/phases/03-local-command-runtime-and-disruptor-execution/03-VALIDATION.md
@crates/es-runtime/src/command.rs
@crates/es-runtime/src/error.rs
@crates/es-runtime/src/store.rs
@crates/es-runtime/src/cache.rs
@crates/es-runtime/src/shard.rs
@crates/es-runtime/src/gateway.rs
@crates/es-store-postgres/src/models.rs
@crates/es-store-postgres/src/error.rs
@crates/es-kernel/src/lib.rs

<interfaces>
From Plan 01:
```rust
pub trait RuntimeEventStore: Clone + Send + Sync + 'static {
    fn append(&self, request: AppendRequest) -> BoxFuture<'_, StoreResult<AppendOutcome>>;
    fn load_rehydration(&self, tenant_id: &TenantId, stream_id: &StreamId) -> BoxFuture<'_, StoreResult<RehydrationBatch>>;
}
```

From Plan 03:
```rust
pub struct ShardState<A: Aggregate> {
    shard_id: ShardId,
    cache: AggregateCache<A>,
    handoffs: VecDeque<ShardHandoff<A>>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement commit-gated shard command processing</name>
  <files>crates/es-runtime/src/shard.rs, crates/es-runtime/src/command.rs, crates/es-runtime/tests/runtime_flow.rs</files>
  <read_first>
    crates/es-runtime/src/shard.rs
    crates/es-runtime/src/command.rs
    crates/es-runtime/tests/runtime_flow.rs
    crates/es-runtime/src/cache.rs
    crates/es-runtime/src/error.rs
    crates/es-runtime/src/store.rs
    crates/es-runtime/tests/common/mod.rs
    crates/es-kernel/src/lib.rs
    crates/es-store-postgres/src/models.rs
    .planning/phases/03-local-command-runtime-and-disruptor-execution/03-RESEARCH.md
    .planning/phases/03-local-command-runtime-and-disruptor-execution/03-PATTERNS.md
  </read_first>
  <behavior>
    - Test: `reply_is_sent_after_append_commit` confirms the reply receiver resolves only after fake store append returns `AppendOutcome::Committed`.
    - Test: `cache_miss_rehydrates_before_decide` seeds fake store rehydration with existing stored events and confirms `A::decide` sees replayed state.
    - Test: successful committed append applies decided events to `AggregateCache` after append succeeds.
    - Test: `AppendOutcome::Duplicate` returns success with the stored `CommittedAppend` without applying newly decided events to `AggregateCache`.
    - Test: `duplicate_after_warmed_cache_does_not_apply_newly_decided_events` warms cache with the previously committed state, returns `AppendOutcome::Duplicate`, and asserts the cache remains exactly at the warmed durable state rather than advancing a second time.
    - Test: dropping the reply receiver after append success does not prevent aggregate cache or dedupe cache from advancing to the committed durable outcome.
  </behavior>
  <action>Extend `crates/es-runtime/src/command.rs` so `RuntimeEventCodec<A>` also exposes `fn decode_snapshot(&self, snapshot: &es_store_postgres::SnapshotRecord) -> RuntimeResult<A::State>;` in addition to `encode` and `decode`. Extend `crates/es-runtime/src/shard.rs` with `pub async fn process_next_handoff<S, C>(&mut self, store: &S, codec: &C) -> RuntimeResult<bool> where S: RuntimeEventStore, C: RuntimeEventCodec<A>, A::Error: std::fmt::Display`. The method must pop one handoff. Before calling `A::decide`, determine the current state as follows: if `self.cache.get(&envelope.stream_id)` has a value, clone it; otherwise call `store.load_rehydration(&envelope.metadata.tenant_id, &envelope.stream_id).await`, use `codec.decode_snapshot(snapshot)` when a snapshot exists or `A::State::default()` when it does not, decode each `StoredEvent` with `codec.decode`, replay those typed events into the state with `A::apply`, and commit the rehydrated state into `AggregateCache` before deciding. Then call `A::decide(&current_state, envelope.command, &envelope.metadata)`, map aggregate errors to `RuntimeError::Domain { message: error.to_string() }`, encode each decided event with `codec.encode(event, &envelope.metadata)`, build `es_store_postgres::AppendRequest::new(envelope.stream_id.clone(), envelope.expected_revision, envelope.metadata.clone(), envelope.idempotency_key.clone(), new_events)`, and call `store.append(append_request).await`. Handle `AppendOutcome::Committed(committed)` and `AppendOutcome::Duplicate(committed)` in separate match arms. On `Committed`, clone the current state into a staged state, apply the decided typed events to the staged state, call `self.cache.commit_state(envelope.stream_id.clone(), staged_state)`, record `DedupeKey { tenant_id: envelope.metadata.tenant_id.clone(), idempotency_key: envelope.idempotency_key.clone() }` with `DedupeRecord { append: committed.clone() }` in `self.dedupe_mut()`, then attempt `reply.send(Ok(CommandOutcome::new(decision.reply, committed)))`. On `Duplicate`, do not apply the newly decided typed events and do not call `commit_state` with a state derived from those newly decided events. Preserve the current cache value exactly: if the command started from a warmed cache, leave that warmed durable state unchanged; if the command started from a cache miss, leave the rehydrated state that storage supplied before decide. Record or refresh only the `DedupeRecord { append: committed.clone() }`, then reply with `CommandOutcome::new(decision.reply, committed)`. This duplicate branch is mandatory because durable PostgreSQL command dedupe is the source of truth and newly decided events from a repeated command are not evidence that additional events were committed. If the one-shot receiver was dropped after a fresh committed append, ignore the send error and still return `Ok(true)` because durable append success has already advanced the authoritative source of truth and shard caches must reflect that committed outcome. If the one-shot receiver was dropped after a duplicate append, ignore the send error, record the dedupe summary, and still do not apply newly decided events. On decision, codec, append request, `load_rehydration`, or `store.append` failure, send the typed error when possible and do not apply new decided events or record a new dedupe cache entry. Add `impl<R> CommandOutcome<R> { pub fn new(reply: R, append: es_store_postgres::CommittedAppend) -> Self }` in `command.rs` if not already present. Create `crates/es-runtime/tests/runtime_flow.rs` with a local counter aggregate, local codec including snapshot decode, fake store, deterministic metadata, and tests named `reply_is_sent_after_append_commit`, `cache_miss_rehydrates_before_decide`, `duplicate_append_returns_successful_command_outcome`, `duplicate_after_warmed_cache_does_not_apply_newly_decided_events`, and `reply_drop_after_append_still_advances_cache_and_dedupe`.</action>
  <verify>
    <automated>cargo test -p es-runtime reply_is_sent_after_append_commit && cargo test -p es-runtime cache_miss_rehydrates_before_decide && cargo test -p es-runtime duplicate_append_returns_successful_command_outcome && cargo test -p es-runtime duplicate_after_warmed_cache_does_not_apply_newly_decided_events && cargo test -p es-runtime reply_drop_after_append_still_advances_cache_and_dedupe</automated>
  </verify>
  <acceptance_criteria>
    `rg 'pub async fn process_next_handoff' crates/es-runtime/src/shard.rs` returns a match.
    `rg 'load_rehydration' crates/es-runtime/src/shard.rs` returns a match before the first `A::decide` occurrence.
    `rg 'decode_snapshot' crates/es-runtime/src/command.rs crates/es-runtime/src/shard.rs` returns matches.
    `rg 'store\.append\(append_request\)\.await' crates/es-runtime/src/shard.rs` returns a match.
    `rg 'AppendOutcome::Committed' crates/es-runtime/src/shard.rs` returns a match.
    `rg 'AppendOutcome::Duplicate' crates/es-runtime/src/shard.rs` returns a match.
    `rg 'commit_state' crates/es-runtime/src/shard.rs` returns a match inside the `AppendOutcome::Committed` arm.
    `rg 'AppendOutcome::Duplicate' crates/es-runtime/src/shard.rs` returns a match in a separate match arm from `AppendOutcome::Committed`.
    `rg 'DedupeKey|DedupeRecord' crates/es-runtime/src/shard.rs` returns matches.
    `rg 'reply_is_sent_after_append_commit' crates/es-runtime/tests/runtime_flow.rs` returns a match.
    `rg 'cache_miss_rehydrates_before_decide' crates/es-runtime/tests/runtime_flow.rs` returns a match.
    `rg 'duplicate_append_returns_successful_command_outcome' crates/es-runtime/tests/runtime_flow.rs` returns a match.
    `rg 'duplicate_after_warmed_cache_does_not_apply_newly_decided_events' crates/es-runtime/tests/runtime_flow.rs` returns a match.
    `rg 'reply_drop_after_append_still_advances_cache_and_dedupe' crates/es-runtime/tests/runtime_flow.rs` returns a match.
    `cargo test -p es-runtime reply_is_sent_after_append_commit && cargo test -p es-runtime cache_miss_rehydrates_before_decide && cargo test -p es-runtime duplicate_append_returns_successful_command_outcome && cargo test -p es-runtime duplicate_after_warmed_cache_does_not_apply_newly_decided_events && cargo test -p es-runtime reply_drop_after_append_still_advances_cache_and_dedupe` exits 0.
  </acceptance_criteria>
  <done>Existing streams are rehydrated before decision, fresh committed appends update aggregate cache after durable commit, duplicate append outcomes return the stored committed result without replaying newly decided events into cache, and reply receiver drops do not roll back committed cache advancement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve cache and return typed errors on conflicts and failures</name>
  <files>crates/es-runtime/src/shard.rs, crates/es-runtime/tests/runtime_flow.rs</files>
  <read_first>
    crates/es-runtime/src/shard.rs
    crates/es-runtime/tests/runtime_flow.rs
    crates/es-runtime/src/error.rs
    crates/es-runtime/src/cache.rs
    crates/es-runtime/src/store.rs
    crates/es-runtime/tests/common/mod.rs
    crates/es-store-postgres/src/error.rs
    .planning/phases/03-local-command-runtime-and-disruptor-execution/03-RESEARCH.md
    .planning/phases/03-local-command-runtime-and-disruptor-execution/03-PATTERNS.md
  </read_first>
  <behavior>
    - Test: `conflict_does_not_mutate_cache` stages events but leaves cached state unchanged when fake store returns `StoreError::StreamConflict`.
    - Test: the reply receives `RuntimeError::Conflict { stream_id, expected, actual }`.
    - Test: codec errors and domain errors return typed errors without append requests.
  </behavior>
  <action>In `process_next_handoff`, map `StoreError::StreamConflict { stream_id, expected, actual }` through `RuntimeError::from_store_error` and send the resulting `RuntimeError::Conflict` to the reply channel without mutating aggregate cache with the newly decided events and without recording a new dedupe cache entry. For every non-success branch, preserve the pre-call cache value after any legitimate cache-miss rehydration: domain decision error, event codec error, snapshot decode error, stored-event decode error, `AppendRequest::new` error, and `store.append` error. A dropped reply receiver is not a non-success branch after append returns success: after `Committed`, aggregate cache and dedupe must already be updated; after `Duplicate`, dedupe may be refreshed but aggregate cache must still not apply newly decided events. Extend `runtime_flow.rs` with tests named `conflict_does_not_mutate_cache`, `domain_error_does_not_append_or_mutate_cache`, `codec_error_does_not_append_or_mutate_cache`, and `rehydration_error_does_not_decide_append_or_mutate_cache`. Each test must assert the fake store append count, dedupe cache length, and cached state after processing. Use `StoreError::StreamConflict { stream_id: "counter-1".to_owned(), expected: "exact 99".to_owned(), actual: Some(1) }` in the conflict fake.</action>
  <verify>
    <automated>cargo test -p es-runtime conflict_does_not_mutate_cache && cargo test -p es-runtime domain_error_does_not_append_or_mutate_cache && cargo test -p es-runtime codec_error_does_not_append_or_mutate_cache</automated>
  </verify>
  <acceptance_criteria>
    `rg 'RuntimeError::from_store_error' crates/es-runtime/src/shard.rs` returns a match.
    `rg 'conflict_does_not_mutate_cache' crates/es-runtime/tests/runtime_flow.rs` returns a match.
    `rg 'domain_error_does_not_append_or_mutate_cache' crates/es-runtime/tests/runtime_flow.rs` returns a match.
    `rg 'codec_error_does_not_append_or_mutate_cache' crates/es-runtime/tests/runtime_flow.rs` returns a match.
    `rg 'rehydration_error_does_not_decide_append_or_mutate_cache' crates/es-runtime/tests/runtime_flow.rs` returns a match.
    `rg 'StoreError::StreamConflict' crates/es-runtime/tests/runtime_flow.rs` returns a match.
    `cargo test -p es-runtime conflict_does_not_mutate_cache && cargo test -p es-runtime domain_error_does_not_append_or_mutate_cache && cargo test -p es-runtime codec_error_does_not_append_or_mutate_cache` exits 0.
  </acceptance_criteria>
  <done>OCC conflicts and other failures surface as typed runtime errors without corrupting shard-local cache.</done>
</task>

<task type="auto">
  <name>Task 3: Implement production command engine loop and complete validation</name>
  <files>crates/es-runtime/src/lib.rs, crates/es-runtime/src/engine.rs, crates/es-runtime/src/gateway.rs, crates/es-runtime/tests/runtime_flow.rs, .planning/phases/03-local-command-runtime-and-disruptor-execution/03-VALIDATION.md</files>
  <read_first>
    crates/es-runtime/src/lib.rs
    crates/es-runtime/src/engine.rs
    crates/es-runtime/src/gateway.rs
    crates/es-runtime/src/shard.rs
    crates/es-runtime/src/router.rs
    crates/es-runtime/src/store.rs
    crates/es-runtime/src/command.rs
    crates/es-runtime/tests/runtime_flow.rs
    crates/es-runtime/tests/router_gateway.rs
    crates/es-runtime/tests/shard_disruptor.rs
    .planning/phases/03-local-command-runtime-and-disruptor-execution/03-VALIDATION.md
    .planning/phases/03-local-command-runtime-and-disruptor-execution/03-RESEARCH.md
  </read_first>
  <action>Add any missing facade re-exports needed by tests and future adapters: `AggregateCache`, `DedupeCache`, `DedupeKey`, `DedupeRecord`, `CommandEngine`, `CommandGateway`, `DisruptorPath`, `PartitionRouter`, `RoutedCommand`, `ShardHandle`, `ShardHandoff`, `ShardHandoffToken`, `ShardId`, and `ShardState`. Create `crates/es-runtime/src/engine.rs` and re-export it from `lib.rs` with `mod engine;` and `pub use engine::{CommandEngine, CommandEngineConfig};`. Implement `#[derive(Clone, Debug)] pub struct CommandEngineConfig { pub shard_count: usize, pub ingress_capacity: usize, pub ring_size: usize }` with `pub fn new(shard_count: usize, ingress_capacity: usize, ring_size: usize) -> RuntimeResult<Self>` that rejects zero shard count, zero ingress capacity, and zero ring size using existing typed runtime errors. Implement `pub struct CommandEngine<A, S, C>` where `A: Aggregate`, `S: RuntimeEventStore`, and `C: RuntimeEventCodec<A>`; it must own `gateway: CommandGateway<A>`, `receiver: tokio::sync::mpsc::Receiver<RoutedCommand<A>>`, `shards: Vec<ShardHandle<A>>`, `store: S`, and `codec: C`. Add `pub fn new(config: CommandEngineConfig, store: S, codec: C) -> RuntimeResult<Self>` that creates `PartitionRouter::new(config.shard_count)`, creates the bounded `CommandGateway::new(router, config.ingress_capacity)`, and creates exactly one `ShardHandle::new(ShardId::new(index), config.ring_size)` for each shard index. Add `pub fn gateway(&self) -> CommandGateway<A>` returning a clone for adapters. Add `pub async fn process_one(&mut self) -> RuntimeResult<bool> where A::Error: std::fmt::Display` that awaits `self.receiver.recv().await`, returns `Ok(false)` if closed and empty, otherwise uses the routed command's `shard_id.value()` to find the owning shard, calls `accept_routed(routed)`, then `drain_released_handoffs()`, then repeatedly calls `shard.state_mut().process_next_handoff(&self.store, &self.codec).await` until no processable handoff remains from the drain, and returns `Ok(true)`. `CommandEngine` is the production loop that connects gateway ingress, shard handoff, disruptor release, durable append, codec conversion, and one-shot replies; runtime-flow tests must no longer be the only place where those pieces are wired together. Ensure `gateway.rs` still contains `try_send` and no `.send().await`. Add a runtime-flow integration test named `runtime_engine_processes_submitted_command_end_to_end_after_durable_commit` that constructs `CommandEngine` with a fake committed store and local codec, submits a command through `engine.gateway().try_submit(envelope)`, calls `engine.process_one().await`, and asserts the reply includes `CommittedAppend.global_positions == vec![1]`. Add a runtime-flow integration test named `runtime_flow_covers_overload_disruptor_handoff_conflict_and_commit_paths` that submits through `CommandEngine::gateway()` with capacity 1, asserts overload on the second submit while no receiver drains, calls `process_one` for the accepted command, and asserts the reply includes `CommittedAppend.global_positions == vec![1]`; include a conflict subcase that uses a fake `StoreError::StreamConflict` and asserts the reply receives `RuntimeError::Conflict`. These tests must fail if gateway ingress, shard selection, disruptor release draining, append commit processing, codec usage, or reply delivery are not connected in production `CommandEngine` code. Update `03-VALIDATION.md`: set `nyquist_compliant: true`, set `wave_0_complete: true`, and mark the Phase 03 per-task statuses green only if `cargo test -p es-runtime`, `cargo test -p es-store-postgres`, and forbidden-pattern greps pass. Do not edit Phase 04+ roadmap requirements.</action>
  <verify>
    <automated>cargo test -p es-runtime runtime_engine -- --nocapture && cargo test -p es-runtime runtime_flow -- --nocapture && cargo test -p es-store-postgres && ! rg 'Arc<Mutex<.*(State|Cache|HashMap)|RwLock<.*(State|Cache|HashMap)' crates/es-runtime/src && ! rg 'block_on|Runtime::new|Handle::current' crates/es-runtime/src</automated>
  </verify>
  <acceptance_criteria>
    `rg 'pub struct CommandEngine' crates/es-runtime/src/engine.rs` returns a match.
    `rg 'pub struct CommandEngineConfig' crates/es-runtime/src/engine.rs` returns a match.
    `rg 'receiver: tokio::sync::mpsc::Receiver<RoutedCommand<A>>' crates/es-runtime/src/engine.rs` returns a match.
    `rg 'Vec<ShardHandle<A>>' crates/es-runtime/src/engine.rs` returns a match.
    `rg 'pub async fn process_one' crates/es-runtime/src/engine.rs` returns a match.
    `rg 'recv\\(\\)\\.await' crates/es-runtime/src/engine.rs` returns a match.
    `rg 'accept_routed' crates/es-runtime/src/engine.rs` returns a match.
    `rg 'drain_released_handoffs' crates/es-runtime/src/engine.rs` returns a match.
    `rg 'process_next_handoff' crates/es-runtime/src/engine.rs` returns a match.
    `rg 'store: S' crates/es-runtime/src/engine.rs` returns a match.
    `rg 'codec: C' crates/es-runtime/src/engine.rs` returns a match.
    `rg 'pub use .*CommandEngine' crates/es-runtime/src/lib.rs` returns a match.
    `rg 'pub use .*CommandGateway' crates/es-runtime/src/lib.rs` returns a match.
    `rg 'pub use .*ShardState' crates/es-runtime/src/lib.rs` returns a match.
    `rg 'runtime_engine_processes_submitted_command_end_to_end_after_durable_commit' crates/es-runtime/tests/runtime_flow.rs` returns a match.
    `rg 'runtime_flow_covers_overload_disruptor_handoff_conflict_and_commit_paths' crates/es-runtime/tests/runtime_flow.rs` returns a match.
    `rg 'CommandEngine::new' crates/es-runtime/tests/runtime_flow.rs` returns a match.
    `rg 'process_one\\(\\)\\.await' crates/es-runtime/tests/runtime_flow.rs` returns a match.
    `rg 'global_positions.*vec!\[1\]' crates/es-runtime/tests/runtime_flow.rs` returns a match.
    `rg 'nyquist_compliant: true' .planning/phases/03-local-command-runtime-and-disruptor-execution/03-VALIDATION.md` returns a match.
    `rg 'wave_0_complete: true' .planning/phases/03-local-command-runtime-and-disruptor-execution/03-VALIDATION.md` returns a match.
    `! rg '\.send\(\)\.await|send\(\s*.*\)\.await' crates/es-runtime/src/gateway.rs` exits successfully with no matches.
    `! rg 'Arc<Mutex<.*(State|Cache|HashMap)|RwLock<.*(State|Cache|HashMap)' crates/es-runtime/src` exits successfully with no matches.
    `cargo test -p es-runtime runtime_engine -- --nocapture` exits 0.
    `cargo test -p es-runtime runtime_flow -- --nocapture` exits 0.
    `cargo test --workspace` exits 0 before phase summary is written.
  </acceptance_criteria>
  <done>Production `CommandEngine` owns and runs the gateway receiver, shard handles, disruptor release drain, durable store, codec, commit processor, and reply delivery; Phase 03 runtime behavior is verified, validation metadata is green, and workspace tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shard state -> durable storage | Decided events cross from in-memory runtime into event-store append. |
| durable storage -> command reply | Committed append positions cross back to runtime callers as authoritative success evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14 | Repudiation | command replies | mitigate | Send success replies only with `CommittedAppend` returned by `RuntimeEventStore::append`. |
| T-03-15 | Tampering | shard cache | mitigate | Stage aggregate state and call `commit_state` only after fresh `AppendOutcome::Committed`; duplicate outcomes record dedupe summary but must not apply newly decided events. |
| T-03-16 | Tampering | OCC conflict path | mitigate | Map `StoreError::StreamConflict` to `RuntimeError::Conflict` and leave cached state unchanged. |
| T-03-17 | Denial of Service | runtime flow | mitigate | Preserve gateway `try_send` overload tests and shard full-ring overload tests from prior plans in the phase gate. |
| T-03-18 | Information Disclosure | tenant-scoped commands | mitigate | Build append requests from `CommandEnvelope.metadata.tenant_id`; do not introduce tenant-free append inputs. |
</threat_model>

<verification>
Run `cargo test -p es-runtime reply_after_commit`, `cargo test -p es-runtime cache_miss_rehydrates_before_decide`, `cargo test -p es-runtime duplicate_after_warmed_cache_does_not_apply_newly_decided_events`, `cargo test -p es-runtime conflict_does_not_mutate_cache`, `cargo test -p es-runtime domain_error_does_not_append_or_mutate_cache`, `cargo test -p es-runtime codec_error_does_not_append_or_mutate_cache`, `cargo test -p es-runtime reply_drop_after_append_still_advances_cache_and_dedupe`, `cargo test -p es-runtime runtime_engine -- --nocapture`, `cargo test -p es-runtime runtime_flow -- --nocapture`, `cargo test -p es-store-postgres`, forbidden-pattern greps for global business-state locks and nested runtime blocking, and final `cargo test --workspace`.
</verification>

<success_criteria>
RUNTIME-05 and RUNTIME-06 are satisfied, prior RUNTIME-01/RUNTIME-03 protections remain green, production `CommandEngine` connects ingress through durable commit and replies, Phase 03 validation status is complete, and successful command replies are anchored to durable append outcomes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-local-command-runtime-and-disruptor-execution/03-04-SUMMARY.md`.
</output>
