---
phase: 08-runtime-duplicate-command-replay
plan: 03
type: execute
wave: 3
depends_on:
  - 08-02
files_modified:
  - crates/adapter-http/tests/commerce_api.rs
  - crates/app/src/commerce_process_manager.rs
  - .planning/phases/08-runtime-duplicate-command-replay/08-VALIDATION.md
autonomous: true
requirements:
  - STORE-03
  - RUNTIME-05
  - INT-04
  - API-01
  - API-03
must_haves:
  truths:
    - "HTTP duplicate retries return the original stream revision, global positions, event IDs, correlation ID, and typed reply payload."
    - "HTTP duplicate retry coverage proves the second request does not receive a fresh domain validation error after state mutation."
    - "Process-manager deterministic follow-up retries return original committed outcomes instead of issuing duplicate downstream append/outbox effects."
    - "Phase 08 validation metadata records Nyquist-compliant automated coverage for runtime, storage, HTTP, and process-manager replay paths."
  artifacts:
    - path: "crates/adapter-http/tests/commerce_api.rs"
      provides: "HTTP duplicate retry response contract coverage"
      contains: "duplicate_place_order_retry_returns_original_response"
    - path: "crates/app/src/commerce_process_manager.rs"
      provides: "Process-manager follow-up retry replay test"
      contains: "process_manager_replayed_followups_return_original_outcomes"
    - path: ".planning/phases/08-runtime-duplicate-command-replay/08-VALIDATION.md"
      provides: "Nyquist validation sign-off for Phase 08"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "crates/adapter-http/tests/commerce_api.rs"
      to: "CommandGateway<Order>"
      via: "two duplicate HTTP requests use the same idempotency key and receive the same replayed outcome"
      pattern: "duplicate_place_order_retry_returns_original_response"
    - from: "crates/app/src/commerce_process_manager.rs"
      to: "CommandGateway<Product> and CommandGateway<Order>"
      via: "same source event is processed twice before offset advancement"
      pattern: "process_manager_replayed_followups_return_original_outcomes"
    - from: ".planning/phases/08-runtime-duplicate-command-replay/08-VALIDATION.md"
      to: "Phase 08 plan verification commands"
      via: "status and Wave 0 checklist updated after tests exist"
      pattern: "nyquist_compliant: true"
---

<objective>
Prove duplicate replay through the external consumers that exposed the milestone gap.

Purpose: Runtime/store replay is only complete when HTTP retries and process-manager follow-up retries preserve the original committed response shape and avoid duplicate downstream effects.
Output: Adapter and process-manager regression tests plus validation metadata update.
</objective>

<execution_context>
@/Users/epikem/.codex/get-shit-done/workflows/execute-plan.md
@/Users/epikem/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-runtime-duplicate-command-replay/08-RESEARCH.md
@.planning/phases/08-runtime-duplicate-command-replay/08-VALIDATION.md
@.planning/phases/08-runtime-duplicate-command-replay/08-PATTERNS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/08-runtime-duplicate-command-replay/08-02-SUMMARY.md
@crates/adapter-http/src/commerce.rs
@crates/adapter-http/tests/commerce_api.rs
@crates/app/src/commerce_process_manager.rs
@crates/es-outbox/src/process_manager.rs
@crates/es-runtime/src/shard.rs

<interfaces>
HTTP maps runtime outcomes with:

```rust
let outcome = receiver.await.map_err(|_| ApiError::ReplyDropped)??;
Ok(CommandSuccess::from_outcome(stream_id, outcome, map_reply))
```

Process-manager follow-up keys must keep the existing deterministic shapes:

```rust
pm:{process_manager}:{source_event_id}:reserve:{product_id}
pm:{process_manager}:{source_event_id}:confirm:{order_id}
pm:{process_manager}:{source_event_id}:reject:{order_id}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 08-03-01: Add HTTP duplicate retry response contract coverage</name>
  <files>crates/adapter-http/tests/commerce_api.rs</files>
  <read_first>
    - crates/adapter-http/tests/commerce_api.rs
    - crates/adapter-http/src/commerce.rs
    - crates/es-runtime/src/shard.rs
    - crates/es-runtime/src/command.rs
    - .planning/phases/08-runtime-duplicate-command-replay/08-RESEARCH.md
    - .planning/phases/08-runtime-duplicate-command-replay/08-PATTERNS.md
    - .planning/phases/08-runtime-duplicate-command-replay/08-VALIDATION.md
    - .planning/v1.0-MILESTONE-AUDIT.md
  </read_first>
  <behavior>
    - Two identical `POST /commands/orders/place` requests with idempotency key `idem-place-duplicate-1` return HTTP 200.
    - The second response body contains the first committed append fields: stream revision, first revision, last revision, global positions, event IDs, and typed `placed` reply.
    - The test fails if the second request receives a fresh domain error body instead of the original success body.
    - The test drives both HTTP requests through `commerce_routes` -> `CommandGateway<Order>` -> `CommandEngine<Order, _, _>::process_one` -> runtime store replay; it must not manually send a fabricated `CommandOutcome` to the routed envelope.
  </behavior>
  <action>In `crates/adapter-http/tests/commerce_api.rs`, add a test named `duplicate_place_order_retry_returns_original_response`. Reuse `commerce_routes(HttpState { ... })`, `place_order_request`, `body_string`, and local fake-store patterns from runtime tests, but the duplicate proof must be integrated: construct a real `CommandEngine<Order, _, _>` with a test `RuntimeEventStore` and a test `RuntimeEventCodec<Order>`, pass `order_engine.gateway()` into the HTTP router, submit both identical `place_order_request("idem-place-duplicate-1")` requests through the Axum app, and drive each accepted command by calling `order_engine.process_one().await`.

Add test-only helpers in this file as needed:
- `TestOrderCodec` implementing `RuntimeEventCodec<Order>` with `encode` for `OrderEvent::OrderPlaced`, `decode`, `decode_snapshot`, `encode_reply`, and `decode_reply`. `encode_reply` must use `CommandReplyPayload::new("order_reply", 1, serde_json::to_value(reply)?)`; `decode_reply` must require `reply_type == "order_reply"` and `schema_version == 1` before decoding `OrderReply`.
- `ReplayAwareOrderStore` implementing `RuntimeEventStore`, recording append requests and lookup calls. It must return `AppendOutcome::Committed(committed_append("order-order-1", 10))` for the first append and, after Plan 08-02 stores the reply payload on the append request, expose the same `CommandReplayRecord` through `lookup_command_replay`.
- Accessors `append_count()` and `lookup_count()` or equivalent assertions that prove the second HTTP request was answered by pre-decision runtime dedupe: after both requests, `append_count() == 1`; if the duplicate is served by durable lookup rather than warm shard cache, `lookup_count() >= 1` is acceptable, but a second append is not.

Implementation constraints: do not receive directly from `order_rx`, do not call `routed.envelope.reply.send(...)`, and do not construct the duplicate response by manually sending `CommandOutcome`. The route must use the real `CommandGateway<Order>` returned by the runtime engine, and the reply must come from `CommandEngine::process_one()` executing the Phase 08 runtime replay branches. The test may force the first request to complete before the second request starts so the second request sees the already-mutated shard cache; under the old ordering this should produce a fresh domain error, so the test must fail without Plan 08-02.

Assert both response statuses are `StatusCode::OK`. Assert the second body contains these exact substrings: `"stream_id":"order-order-1"`, `"stream_revision":1`, `"first_revision":1`, `"last_revision":1`, `"global_positions":[10]`, `"event_ids":["00000000-0000-0000-0000-00000000000a"]`, and `"reply":{"type":"placed","order_id":"order-1"}`. If `place_order_request` is extended to accept command/correlation IDs, also assert the duplicate response preserves the original correlation ID. Add `assert_eq!(first_body, second_body)` only if the current helper produces deterministic correlation/event IDs for both responses; otherwise assert the listed fields directly.</action>
  <verify>
    <automated>cargo test -p adapter-http duplicate_place_order_retry_returns_original_response -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'duplicate_place_order_retry_returns_original_response|CommandEngine<.*Order|process_one\\(\\)\\.await|ReplayAwareOrderStore|append_count\\(\\)|lookup_command_replay|CommandReplyPayload::new\\("order_reply", 1|idem-place-duplicate-1|place_order_request\\("idem-place-duplicate-1"\\)|StatusCode::OK|stream_revision\":1|first_revision\":1|last_revision\":1|global_positions\":\\[10\\]|reply\":\\{\"type\":\"placed\",\"order_id\":\"order-1\"\\}' crates/adapter-http/tests/commerce_api.rs` finds all listed patterns.
    - `! rg 'DedupeCache|HashMap<.*idempotency' crates/adapter-http/tests/commerce_api.rs crates/adapter-http/src/commerce.rs` exits 0.
    - `! rg 'lookup_command_replay|DedupeCache|HashMap<.*idempotency' crates/adapter-http/src/commerce.rs` exits 0.
    - The new `duplicate_place_order_retry_returns_original_response` test body contains no `order_rx.recv` and no `envelope.reply.send(Ok(CommandOutcome::new(...)))`; older adapter tests may keep their direct gateway-boundary pattern.
    - `cargo test -p adapter-http duplicate_place_order_retry_returns_original_response -- --nocapture` exits 0.
  </acceptance_criteria>
  <done>HTTP duplicate retry coverage proves the adapter preserves runtime replayed success shape through the real gateway/runtime/store replay path without implementing adapter-local idempotency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 08-03-02: Add process-manager deterministic follow-up retry replay coverage</name>
  <files>crates/app/src/commerce_process_manager.rs</files>
  <read_first>
    - crates/app/src/commerce_process_manager.rs
    - crates/es-outbox/src/process_manager.rs
    - crates/es-runtime/src/shard.rs
    - crates/es-runtime/src/gateway.rs
    - .planning/phases/08-runtime-duplicate-command-replay/08-RESEARCH.md
    - .planning/phases/08-runtime-duplicate-command-replay/08-PATTERNS.md
    - .planning/phases/08-runtime-duplicate-command-replay/08-VALIDATION.md
    - .planning/v1.0-MILESTONE-AUDIT.md
  </read_first>
  <behavior>
    - Processing the same source event twice before offset advancement submits the same deterministic reserve and confirm/reject idempotency keys.
    - The second processing receives the original follow-up command outcomes through real product/order runtime engines and does not require fresh downstream domain decisions.
    - The test proves duplicate process-manager retry behavior through `CommandGateway` -> `CommandEngine::process_one` -> runtime store replay, not by fake gateway reply injection or an in-memory set inside the process manager.
  </behavior>
  <action>In the existing test module in `crates/app/src/commerce_process_manager.rs`, add a test named `process_manager_replayed_followups_return_original_outcomes`. Keep the existing deterministic key-format assertions, but they cannot be the primary proof. The duplicate proof must execute follow-up commands through real runtime engines: build `CommandEngine<Product, _, _>` and `CommandEngine<Order, _, _>` with test runtime stores/codecs, pass `product_engine.gateway()` and `order_engine.gateway()` into `CommerceOrderProcessManager::new(ProcessManagerName::new("commerce-order-pm")?, ...)`, call `manager.process(&event).await` in spawned tasks, and drive each follow-up by calling the corresponding engine's `process_one().await`.

Add test-only helpers in this module as needed:
- `TestProductCodec` implementing `RuntimeEventCodec<Product>` with `encode`, `decode`, `decode_snapshot`, `encode_reply`, and `decode_reply` for `ProductReply::InventoryReserved`; typed replies must use `CommandReplyPayload::new("product_reply", 1, serde_json::to_value(reply)?)`.
- `TestOrderCodec` implementing `RuntimeEventCodec<Order>` with `encode`, `decode`, `decode_snapshot`, `encode_reply`, and `decode_reply` for `OrderReply::Confirmed`; typed replies must use `CommandReplyPayload::new("order_reply", 1, serde_json::to_value(reply)?)`.
- `ReplayAwareProductStore` and `ReplayAwareOrderStore` implementing `RuntimeEventStore`, recording append requests and replay lookups, returning committed appends for the first reserve/confirm, and returning the original `CommandReplayRecord` on duplicate lookup/cache replay.

The test must:
1. Build one deterministic `OrderPlaced` source event with stable `event.event_id`, `product_id`, and `order_id`.
2. Process it once. While the process task awaits follow-up replies, call `product_engine.process_one().await` for reserve and `order_engine.process_one().await` for confirm. Assert the first product idempotency key equals `format!("pm:commerce-order-pm:{}:reserve:{}", event.event_id, product_id.as_str())`, the first order idempotency key equals `format!("pm:commerce-order-pm:{}:confirm:{}", event.event_id, order_id.as_str())`, and the first process outcome is `ProcessOutcome::CommandsSubmitted { global_position: event.global_position, command_count: 2 }`.
3. Process the same event a second time before any process-manager offset advancement helper is called. Drive the duplicate reserve/confirm through `product_engine.process_one().await` and `order_engine.process_one().await` again. Assert the second process outcome is the same success path.
4. Assert duplicate follow-up commands return the original committed outcomes and create no duplicate downstream effects: `product_store.append_count() == 1`, `order_store.append_count() == 1`, and any captured replay/append global positions remain `20` for product and `21` for order. If the duplicate hits warm runtime cache, store lookup counts may remain zero; if it hits durable replay, lookup counts may increase, but append counts must not.
5. Assert no process-manager-local `HashSet`, `BTreeSet`, or `HashMap` of processed idempotency keys was added to `CommerceOrderProcessManager`.

Implementation constraints: do not receive from `product_rx`/`order_rx` and manually complete replies in this test; do not send `CommandOutcome` directly from fake gateways; do not add process-manager memory dedupe; do not advance offsets in this test; and do not create outbox rows from the process manager. The proof must rely on deterministic follow-up keys entering the same runtime/store replay contract implemented by Plans 08-01 and 08-02.</action>
  <verify>
    <automated>cargo test -p app process_manager_replayed_followups_return_original_outcomes -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'process_manager_replayed_followups_return_original_outcomes|CommandEngine<.*Product|CommandEngine<.*Order|process_one\\(\\)\\.await|ReplayAwareProductStore|ReplayAwareOrderStore|append_count\\(\\)|CommandReplyPayload::new\\("product_reply", 1|CommandReplyPayload::new\\("order_reply", 1|pm:commerce-order-pm|:reserve:|:confirm:|InventoryReserved|OrderReply::Confirmed|global_positions.*20|global_positions.*21' crates/app/src/commerce_process_manager.rs` finds all listed patterns.
    - `! rg 'processed_idempotency|HashSet<.*idempotency|BTreeSet<.*idempotency|HashMap<.*idempotency' crates/app/src/commerce_process_manager.rs` exits 0.
    - The new `process_manager_replayed_followups_return_original_outcomes` test body contains no calls to `receive_product`, `receive_order`, or `envelope.reply.send(Ok(CommandOutcome::new(...)))`; existing older tests may keep their current helper-driven gateway pattern outside this new test.
    - `cargo test -p app process_manager_replayed_followups_return_original_outcomes -- --nocapture` exits 0.
  </acceptance_criteria>
  <done>Process-manager retry coverage proves deterministic follow-up commands are replayed through real command runtime/store paths without duplicate downstream append/outbox effects or process-manager-local dedupe state.</done>
</task>

<task type="auto">
  <name>Task 08-03-03: Mark Phase 08 validation Nyquist-compliant after tests exist</name>
  <files>.planning/phases/08-runtime-duplicate-command-replay/08-VALIDATION.md</files>
  <read_first>
    - .planning/phases/08-runtime-duplicate-command-replay/08-VALIDATION.md
    - .planning/phases/08-runtime-duplicate-command-replay/08-RESEARCH.md
    - .planning/phases/08-runtime-duplicate-command-replay/08-PATTERNS.md
    - crates/es-runtime/tests/runtime_flow.rs
    - crates/es-store-postgres/tests/dedupe.rs
    - crates/adapter-http/tests/commerce_api.rs
    - crates/app/src/commerce_process_manager.rs
  </read_first>
  <action>Update `.planning/phases/08-runtime-duplicate-command-replay/08-VALIDATION.md` only after Tasks 08-03-01 and 08-03-02 pass. Set frontmatter `nyquist_compliant: true` and `wave_0_complete: true`.

Rename the Per-Task Verification Map heading to `Requirement-Level Verification Sampling Map` unless the executor expands it to list every task-level command. Use requirement-level sampling rows that accurately reflect the dependency graph:
- Plan 01 / Wave 1 covers durable replay storage checks for STORE-03 and RUNTIME-05.
- Plan 02 / Wave 2 covers runtime codec, local cache replay, durable lookup replay, and duplicate append branch checks for STORE-03, RUNTIME-03, and RUNTIME-05.
- Plan 03 / Wave 3 covers HTTP duplicate retry, process-manager duplicate follow-up retry, and validation metadata checks for STORE-03, RUNTIME-05, INT-04, API-01, and API-03.

The sampling map must include all automated commands from the three plans, either as separate rows or as a single row per plan with newline-separated commands. At minimum it must include:
`cargo test -p es-store-postgres command_reply_payload -- --nocapture`,
`cargo test -p es-store-postgres command_replay -- --nocapture`,
`cargo test -p es-store-postgres duplicate_idempotency_key_returns_original_result -- --nocapture`,
`cargo test -p es-runtime command_replay_contract -- --nocapture`,
`cargo test -p app single_service_stress_smoke -- --nocapture`,
`cargo test -p es-runtime runtime_duplicate -- --nocapture`,
`cargo test -p es-runtime duplicate_replay_returns_original_reply_after_state_mutation -- --nocapture`,
`cargo test -p adapter-http duplicate_place_order_retry_returns_original_response -- --nocapture`, and
`cargo test -p app process_manager_replayed_followups_return_original_outcomes -- --nocapture`.

Replace every `File Exists` value of `W0` with `yes`, replace every `Status` value of `pending` with `green`, and correct plan/wave rows so Plan 08-02 is Wave 2 and Plan 08-03 is Wave 3. In the Wave 0 Requirements checklist, mark bullets complete with `[x]`. In the Validation Sign-Off checklist, mark all bullets complete with `[x]` and change `**Approval:** pending` to `**Approval:** automated Phase 08 requirement-level sampling coverage present`.</action>
  <verify>
    <automated>cargo test -p es-runtime runtime_duplicate -- --nocapture</automated>
    <automated>cargo test -p es-store-postgres command_replay -- --nocapture</automated>
    <automated>cargo test -p adapter-http duplicate_place_order_retry_returns_original_response -- --nocapture</automated>
    <automated>cargo test -p app process_manager_replayed_followups_return_original_outcomes -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'nyquist_compliant: true|wave_0_complete: true|Requirement-Level Verification Sampling Map|\\| 08-01 .* \\| 1 \\| STORE-03, RUNTIME-05|\\| 08-02 .* \\| 2 \\| STORE-03, RUNTIME-03, RUNTIME-05|\\| 08-03 .* \\| 3 \\| STORE-03, RUNTIME-05, INT-04, API-01, API-03|cargo test -p es-store-postgres command_reply_payload|cargo test -p es-store-postgres command_replay|cargo test -p es-store-postgres duplicate_idempotency_key_returns_original_result|cargo test -p es-runtime command_replay_contract|cargo test -p app single_service_stress_smoke|cargo test -p es-runtime runtime_duplicate|cargo test -p es-runtime duplicate_replay_returns_original_reply_after_state_mutation|cargo test -p adapter-http duplicate_place_order_retry_returns_original_response|cargo test -p app process_manager_replayed_followups_return_original_outcomes|\\[x\\] Existing Rust test infrastructure|\\[x\\] Phase plans must add or update tests|Approval:\\*\\* automated Phase 08 requirement-level sampling coverage present' .planning/phases/08-runtime-duplicate-command-replay/08-VALIDATION.md` finds all listed patterns.
    - `! rg 'nyquist_compliant: false|wave_0_complete: false|W0|pending' .planning/phases/08-runtime-duplicate-command-replay/08-VALIDATION.md` exits 0.
    - All four automated verification commands in this task exit 0.
  </acceptance_criteria>
  <done>Phase 08 validation metadata accurately reflects that runtime warm replay, durable replay, HTTP duplicate retry, and process-manager retry have automated `cargo test` coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client -> adapter -> runtime gateway | External JSON requests cross into typed command envelopes and runtime replies cross back into JSON responses. |
| Process manager -> command gateways | Durable event replay can cause deterministic follow-up commands to be submitted again. |
| Validation artifacts -> executor/checker | Validation metadata determines whether Nyquist-required replay paths are considered covered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | HTTP duplicate retry flow | mitigate | `duplicate_place_order_retry_returns_original_response` proves duplicate retry receives original committed outcome instead of fresh domain validation. |
| T-08-02 | Tampering | HTTP idempotency key reuse | mitigate | Adapter coverage keeps idempotency key on the envelope and does not add adapter-local caches; storage/runtime tenant-key mismatch handling remains in Plans 08-01 and 08-02. |
| T-08-03 | Tampering / Denial of Service | replay payload typed response | mitigate | HTTP and process-manager tests assert decoded runtime outcomes render as typed replies with committed positions; codec mismatch behavior is covered by Plan 08-02. |
| T-08-04 | Tampering / Repudiation | `CommerceOrderProcessManager` follow-up retry | mitigate | `process_manager_replayed_followups_return_original_outcomes` processes the same source event twice before offset advancement and asserts deterministic keys plus original outcomes without process-manager-local dedupe. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | HTTP duplicate retries replay original committed result | Covered by Task 08-03-01. |
| GOAL | Process-manager follow-up retries replay original committed result | Covered by Task 08-03-02. |
| REQ STORE-03 | Prior committed result returned for repeated key | Covered through HTTP and process-manager replay tests. |
| REQ RUNTIME-05 | Duplicate replies are commit-backed | Covered by asserted committed append fields in both tests. |
| REQ INT-04 | Process manager issues follow-up through same gateway | Covered by Task 08-03-02 using `CommandGateway`. |
| REQ API-01 | HTTP adapter decodes, attaches metadata, sends through ingress, awaits replies | Covered by Task 08-03-01 through `commerce_routes` and routed command capture. |
| REQ API-03 | API responses include stream revision, global position, correlation ID, typed reply | Covered by Task 08-03-01 response body assertions. |
| RESEARCH | Process-manager replay uses same idempotency contract | Covered by exact `pm:{manager}:{source_event_id}:...` key assertions. |
| VALIDATION | HTTP duplicate retry | Covered by `cargo test -p adapter-http duplicate_place_order_retry_returns_original_response -- --nocapture`. |
| VALIDATION | Process-manager deterministic follow-up retry | Covered by `cargo test -p app process_manager_replayed_followups_return_original_outcomes -- --nocapture`. |
| AUDIT | Duplicate command retry through API/runtime/store breaks at runtime aggregate decision | Closed by downstream regression coverage depending on Plan 08-02. |
| CONTEXT | No Phase 08 CONTEXT.md exists | No locked or deferred user decisions to implement. |
</source_audit>

<verification>
Run:

```bash
cargo test -p adapter-http duplicate_place_order_retry_returns_original_response -- --nocapture
cargo test -p app process_manager_replayed_followups_return_original_outcomes -- --nocapture
cargo test --workspace duplicate -- --nocapture
```
</verification>

<success_criteria>
Plan 08-03 succeeds when HTTP duplicate retry and process-manager deterministic follow-up retry have automated tests proving original committed outcomes are replayed, no adapter/process-manager-local idempotency cache is introduced, and Phase 08 validation metadata is marked Nyquist-compliant only after the required tests exist.
</success_criteria>

<output>
After completion, create `.planning/phases/08-runtime-duplicate-command-replay/08-03-SUMMARY.md`.
</output>
