---
phase: 13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st
plan: 01
subsystem: runtime
tags: [rust, tokio, disruptor, runtime, event-sourcing, postgres]
requires:
  - phase: 03-local-command-runtime-and-disruptor-execution
    provides: bounded gateway ingress, shard routing, shard-local caches, and commit-gated runtime flow
provides:
  - per-shard runtime workers behind one ingress dispatcher
  - commit-gated concurrency coverage for distinct shard routes
  - cold-cache `ExpectedRevision::NoStream` rehydration bypass with durable OCC enforcement
affects: [runtime, app-serve, stress, process-manager]
tech-stack:
  added: []
  patterns: [dispatcher-plus-workers topology, no-stream cold-cache fast path, commit-gated shard reply tests]
key-files:
  created: []
  modified:
    - crates/es-runtime/src/engine.rs
    - crates/es-runtime/src/shard.rs
    - crates/es-runtime/tests/runtime_flow.rs
    - crates/app/src/serve.rs
key-decisions:
  - "CommandEngine now owns one dispatcher and one Tokio worker per shard, while process_one remains a dispatch helper for tests and in-process harnesses."
  - "Cold-cache commands only skip rehydration when Aggregate::expected_revision(command) is exactly ExpectedRevision::NoStream; PostgreSQL append OCC still proves stream non-existence."
patterns-established:
  - "Accepted ingress is dispatched onto shard-owned workers instead of one serial process_one loop."
  - "Create-style commands can decide from default state on a cold cache miss when NoStream already makes durable append the source-of-truth existence check."
requirements-completed: [RUNTIME-01, RUNTIME-02, RUNTIME-05]
duration: 8 min
completed: 2026-04-26
---

# Phase 13.1 Plan 01 Summary

**Dispatcher-backed shard workers with commit-gated parallel runtime execution and a safe `NoStream` cold-cache fast path**

## Performance

- **Duration:** 8 min
- **Started:** 2026-04-26T09:46:49Z
- **Completed:** 2026-04-26T09:54:29Z
- **Tasks:** 3
- **Files modified:** 4

## Accomplishments

- Added runtime regression coverage for shard-parallel dispatch, append-gated replies, and `NoStream` cold-cache behavior.
- Replaced the effective single-consumer runtime loop with a dispatcher plus one worker task per shard while preserving bounded gateway ingress semantics.
- Skipped redundant rehydration on cold-cache `ExpectedRevision::NoStream` commands without bypassing durable append validation.

## Task Commits

1. **Task 13.1-01-01: Add failing runtime tests for per-shard concurrency and no-stream rehydration bypass** - `f6694e4` (`test`)
2. **Task 13.1-01-02: Replace the serial engine loop with a dispatcher plus one worker per shard** - `e7dacec` (`feat`)
3. **Task 13.1-01-03: Add the safe `ExpectedRevision::NoStream` cold-cache fast path** - `e3775ee` (`feat`)

## Files Created/Modified

- `crates/es-runtime/tests/runtime_flow.rs` - Adds the red/green regressions for shard-parallel progress, append-gated replies, and `NoStream` cache-miss behavior.
- `crates/es-runtime/src/engine.rs` - Moves runtime execution to a dispatcher plus shard-owned worker tasks and keeps `process_one()` as a dispatch helper.
- `crates/app/src/serve.rs` - Starts the refactored engine runner instead of looping on `process_one()`.
- `crates/es-runtime/src/shard.rs` - Starts exact `NoStream` cold-cache commands from `A::State::default()` while leaving other cache misses on durable rehydration.

## Decisions Made

- `CommandEngine::new` now starts shard-owned workers immediately so production `run()` and test/stress `process_one()` can share the same parallel execution topology.
- The fast path keys off `Aggregate::expected_revision(&command)` instead of cache state or in-memory stream tracking, so PostgreSQL remains the authoritative existence check.

## Deviations from Plan

None - plan executed as written.

## Issues Encountered

- `cargo test -p app serve -- --nocapture` hit a pre-existing compile failure in [crates/app/src/http_stress.rs](/Users/epikem/dev/projects/disruptor-es/crates/app/src/http_stress.rs:1285), where a `StressReport` test initializer is missing newer metrics fields. This was outside the plan's file scope, so serve wiring was verified with `cargo check -p app --bin app` and logged in `deferred-items.md`.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The runtime can now realize real shard-level parallelism without weakening bounded ingress or durable reply gating.
- Plan 13.1-02 can build on this by fixing the remaining live stress harness trust gaps without revisiting the core runtime topology.

## Threat Flags

None.

## Known Stubs

- `crates/es-runtime/src/shard.rs:56` - `ShardHandoffToken::placeholder` remains the intentional preallocated disruptor sentinel introduced in Phase 03. It is overwritten before release and does not affect this plan's runtime correctness.

## Self-Check: PASSED

- Summary file exists: `.planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-01-SUMMARY.md`
- Task commits exist: `f6694e4`, `e7dacec`, `e3775ee`
