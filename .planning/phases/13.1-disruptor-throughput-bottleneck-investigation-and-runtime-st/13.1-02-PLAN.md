---
phase: 13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/app/src/http_stress.rs
  - crates/app/src/main.rs
  - crates/app/src/stress.rs
  - benches/external_process_http.rs
autonomous: true
requirements:
  - TEST-03
  - TEST-04
  - OBS-02
must_haves:
  truths:
    - "The live HTTP harness can saturate offered load instead of sleeping one millisecond between submissions."
    - "Profile names and explicit workload shapes accurately control whether requests use unique keys, a bounded hot set, or one single hot key."
    - "Live reports expose trustworthy metric-scrape counts and sample counts instead of silently treating missing metrics as zero."
  artifacts:
    - path: "crates/app/src/http_stress.rs"
      provides: "offered-load saturation, workload-shape routing, and trustworthy scrape accounting"
      contains: "workload_shape"
    - path: "crates/app/src/main.rs"
      provides: "CLI flags for workload-shape selection"
      contains: "--workload-shape"
    - path: "crates/app/src/stress.rs"
      provides: "report fields for workload shape and metrics scrape accounting"
      contains: "metrics_scrape_successes"
    - path: "benches/external_process_http.rs"
      provides: "external HTTP bench reusing the updated stress config shape"
      contains: "HttpStressConfig"
  key_links:
    - from: "crates/app/src/main.rs"
      to: "crates/app/src/http_stress.rs"
      via: "CLI builds `HttpStressConfig` with workload-shape values"
      pattern: "workload_shape|hot_set_size|run_external_process_http_stress"
    - from: "crates/app/src/http_stress.rs"
      to: "crates/app/src/stress.rs"
      via: "live run populates report fields for scrape counts and workload metadata"
      pattern: "StressReport|metrics_scrape_successes|workload_shape"
    - from: "benches/external_process_http.rs"
      to: "crates/app/src/http_stress.rs"
      via: "bench reuses the same validated config path as the CLI"
      pattern: "HttpStressConfig|HttpStressProfile"
---

<objective>
Fix the live HTTP harness so it can actually stress the runtime, model hot-key traffic correctly, and report enough scrape/measurement metadata to classify the bottleneck by layer.

Purpose: Phase 13.1 must prove whether throughput is lost in the runtime or in the measurement path. The harness is part of the measurement path, so incorrect offered-load pacing or false hot-key traffic would invalidate the diagnosis.
Output: updated external HTTP harness, richer report schema, and CLI support for explicit workload shapes and trustworthy metrics accounting.
</objective>

<execution_context>
@/Users/epikem/.codex/get-shit-done/workflows/execute-plan.md
@/Users/epikem/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/RESEARCH.md
@.planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-VALIDATION.md
@.planning/phases/12-external-process-http-e2e-stress-and-benchmark-closure/12-02-SUMMARY.md
@.planning/phases/13-live-external-process-http-steady-state-stress-testing/13-02-SUMMARY.md
@crates/app/src/http_stress.rs
@crates/app/src/main.rs
@crates/app/src/stress.rs
@benches/external_process_http.rs
@crates/app/src/observability.rs

<interfaces>
From `crates/app/src/http_stress.rs`:
```rust
pub enum HttpStressProfile { Smoke, Baseline, Burst, HotKey }

pub struct HttpStressConfig {
    pub profile: HttpStressProfile,
    pub warmup_seconds: u64,
    pub measurement_seconds: u64,
    pub command_count: usize,
    pub concurrency: usize,
    pub shard_count: usize,
    pub ingress_capacity: usize,
    pub ring_size: usize,
}
```

From `crates/app/src/stress.rs`:
```rust
pub struct StressReport {
    pub throughput_per_second: f64,
    pub ingress_depth_max: usize,
    pub shard_depth_max: usize,
    pub append_latency_p95_micros: u64,
    pub projection_lag: i64,
    pub outbox_lag: i64,
}
```

From `crates/app/src/observability.rs`:
```rust
pub const PHASE7_METRIC_NAMES: &[&str] = &[
    "es_ingress_depth",
    "es_shard_queue_depth",
    "es_ring_wait_seconds",
    "es_append_latency_seconds",
];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 13.1-02-01: Add failing harness tests for workload shape, saturation, and scrape accounting</name>
  <files>crates/app/src/http_stress.rs, crates/app/src/main.rs</files>
  <read_first>
    - crates/app/src/http_stress.rs
    - crates/app/src/main.rs
    - crates/app/src/stress.rs
    - .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/RESEARCH.md
    - .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: the `hot-key` profile reuses one stable partition key instead of generating unique order IDs.
    - Test 2: `Unique`, `HotSet(N)`, and `SingleHotKey` shapes produce the intended key reuse pattern.
    - Test 3: metric scrape failures increment failure counters instead of silently leaving trusted-looking zero values.
  </behavior>
  <action>
    Extend the existing unit tests in `crates/app/src/http_stress.rs` and the CLI tests in `crates/app/src/main.rs` with red cases for the precise measurement gaps identified in research:
    - Add tests named `hot_key_profile_reuses_one_partition_key`, `hot_set_shape_reuses_bounded_key_set`, and `metrics_scrape_failures_are_counted`.
    - Add a CLI parser test that exercises `--workload-shape hot-set --hot-set-size 8` and another for `--workload-shape single-hot-key`.
    - Add a fast harness-level test that fails if `execute_http_window` still depends on `interval(Duration::from_millis(1))` pacing for baseline/burst submission.
  </action>
  <verify>
    <automated>cargo test -p app hot_key_profile_reuses_one_partition_key -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'hot_key_profile_reuses_one_partition_key|hot_set_shape_reuses_bounded_key_set|metrics_scrape_failures_are_counted' crates/app/src/http_stress.rs` finds the new regression tests.
    - `rg -n -- '--workload-shape|--hot-set-size' crates/app/src/main.rs` shows the new CLI coverage target.
    - `cargo test -p app hot_key_profile_reuses_one_partition_key -- --nocapture` fails before implementation and passes after implementation.
  </acceptance_criteria>
  <done>The harness bugs are pinned by automated tests before code changes land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 13.1-02-02: Replace fixed 1 ms pacing with saturation and add explicit workload-shape modes</name>
  <files>crates/app/src/http_stress.rs, crates/app/src/main.rs, benches/external_process_http.rs</files>
  <read_first>
    - crates/app/src/http_stress.rs
    - crates/app/src/main.rs
    - benches/external_process_http.rs
    - crates/app/src/stress.rs
    - .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/RESEARCH.md
    - .planning/phases/13-live-external-process-http-steady-state-stress-testing/13-02-SUMMARY.md
  </read_first>
  <behavior>
    - Baseline and burst profiles submit as fast as the configured concurrency budget allows.
    - `HotKey` traffic really reuses one partition key, and `HotSet(N)` reuses exactly `N` keys.
    - CLI and bench paths reuse the same validated config surface.
  </behavior>
  <action>
    Implement a concrete workload-shape model in `crates/app/src/http_stress.rs`:
    - Add `HttpWorkloadShape::{Unique, HotSet(usize), SingleHotKey}`.
    - Add `workload_shape` and `hot_set_size` fields to `HttpStressConfig`.
    - Map defaults as follows: `Smoke`, `Baseline`, and `Burst` use `Unique`; `HotKey` uses `SingleHotKey`.

    Update request generation so `canonical_place_order_request(...)` is fed from a new helper that derives request identity by shape:
    - `Unique` uses the current per-index `order_id`, `user_id`, `product_id`, `sku`, and idempotency suffix.
    - `HotSet(8)` reuses the same 8 `order_id` plus partition-key families by computing `index % 8`.
    - `SingleHotKey` reuses one exact `order_id`, `user_id`, `product_id`, and `sku` family for every request.

    Remove the fixed `interval(Duration::from_millis(1))` gating from `execute_http_window`. The loop should keep the in-flight set filled up to `config.concurrency` immediately, stop admitting new requests only at the measured deadline or `command_count`, and keep the existing bounded drain policy.

    Extend `crates/app/src/main.rs` to accept these exact flags:
    - `--workload-shape unique|hot-set|single-hot-key`
    - `--hot-set-size <usize>`

    Update `benches/external_process_http.rs` so the smoke bench still uses the shared config constructor and compiles after the config shape grows.
  </action>
  <verify>
    <automated>cargo run -q -p app -- http-stress --profile smoke --workload-shape single-hot-key --warmup-seconds 1 --measure-seconds 2 --concurrency 2 --command-count 8 --shard-count 2 --ingress-capacity 8 --ring-size 16</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'HttpWorkloadShape|Unique|HotSet|SingleHotKey|workload_shape|hot_set_size' crates/app/src/http_stress.rs crates/app/src/main.rs` finds the new shape model and CLI flags.
    - `rg -n 'interval\\(Duration::from_millis\\(1\\)\\)' crates/app/src/http_stress.rs` returns no matches.
    - `rg -n 'index % 8|SingleHotKey|single-hot-key|hot-set' crates/app/src/http_stress.rs` shows concrete key-reuse logic.
    - `cargo run -q -p app -- http-stress --profile smoke --workload-shape single-hot-key --warmup-seconds 1 --measure-seconds 2 --concurrency 2 --command-count 8 --shard-count 2 --ingress-capacity 8 --ring-size 16` exits 0 and prints JSON.
  </acceptance_criteria>
  <done>The live HTTP lane can saturate the service and model unique, hot-set, and true hot-key traffic explicitly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 13.1-02-03: Make live reports trustworthy with scrape counts, sample counts, and ring-wait visibility</name>
  <files>crates/app/src/http_stress.rs, crates/app/src/main.rs, crates/app/src/stress.rs</files>
  <read_first>
    - crates/app/src/http_stress.rs
    - crates/app/src/main.rs
    - crates/app/src/stress.rs
    - crates/app/src/observability.rs
    - .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/RESEARCH.md
  </read_first>
  <behavior>
    - Reports show whether metrics were scraped successfully often enough to trust append-latency and queue-depth fields.
    - Reports surface ring-wait data from `es_ring_wait_seconds` in addition to append latency and queue depth.
    - Missing scrapes are explicit counters, not zero-valued false negatives.
  </behavior>
  <action>
    Extend the report path across `crates/app/src/http_stress.rs`, `crates/app/src/stress.rs`, and `crates/app/src/main.rs` with these exact fields:
    - `metrics_scrape_successes`
    - `metrics_scrape_failures`
    - `metrics_sample_count`
    - `ring_wait_p95_micros`
    - `workload_shape`
    - `hot_set_size`

    Implement the accounting concretely:
    - Increment `metrics_scrape_successes` every time `scrape_metrics(...)` succeeds during the measured window.
    - Increment `metrics_scrape_failures` every time the scrape request or parse path fails during the measured window.
    - Set `metrics_sample_count = metrics_scrape_successes + metrics_scrape_failures`.
    - Parse `es_ring_wait_seconds` from the Prometheus body and convert the measured-window p95 delta to `ring_wait_p95_micros` using the same delta method already used for `es_append_latency_seconds`.
    - Keep `append_latency_p95_micros`, `ingress_depth_max`, `shard_depth_max`, `projection_lag`, and `outbox_lag`, but treat them as trustworthy only when `metrics_scrape_successes > 0`; the report must expose the scrape counters either way.

    Update `stress_report_json(...)` so the emitted JSON includes all six new keys.
  </action>
  <verify>
    <automated>cargo test -p app external_process_http_stress_smoke -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'metrics_scrape_successes|metrics_scrape_failures|metrics_sample_count|ring_wait_p95_micros|workload_shape|hot_set_size' crates/app/src/http_stress.rs crates/app/src/stress.rs crates/app/src/main.rs` finds the new report fields end to end.
    - `rg -n 'es_ring_wait_seconds|histogram_p95_delta_micros' crates/app/src/http_stress.rs` shows ring-wait extraction is wired from Prometheus metrics.
    - `cargo test -p app external_process_http_stress_smoke -- --nocapture` exits 0.
    - `cargo run -q -p app -- http-stress --profile smoke --workload-shape hot-set --hot-set-size 4 --warmup-seconds 1 --measure-seconds 2 --concurrency 2 --command-count 8 --shard-count 2 --ingress-capacity 8 --ring-size 16` prints JSON containing `"metrics_scrape_successes"`, `"metrics_scrape_failures"`, `"metrics_sample_count"`, and `"ring_wait_p95_micros"`.
  </acceptance_criteria>
  <done>Live reports include enough trustworthy metadata to distinguish runtime bottlenecks from measurement-path failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI flags -> external stress harness | Invalid shape or size inputs can create misleading load patterns or unsafe local runs. |
| Prometheus scrape output -> archive-facing report JSON | Missing or partial metrics must not be represented as trustworthy zeroes. |
| workload generator -> runtime admission path | Saturating the harness must still respect bounded service admission rather than hiding overload behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.1-04 | R | `crates/app/src/http_stress.rs` report generation | mitigate | Add explicit `metrics_scrape_successes`, `metrics_scrape_failures`, and `metrics_sample_count` fields so missing telemetry cannot be mistaken for zero queue depth or zero append latency. |
| T-13.1-05 | D | `crates/app/src/http_stress.rs` offered-load pacing | mitigate | Remove the artificial 1 ms client tick, keep the in-flight cap at `config.concurrency`, and continue to classify `429 TOO MANY REQUESTS` as rejects rather than retrying automatically. |
| T-13.1-06 | T | `crates/app/src/main.rs` workload-shape parsing | mitigate | Restrict CLI values to `unique`, `hot-set`, and `single-hot-key`; validate `hot_set_size` as a bounded positive integer before any container or child process starts. |
</threat_model>

<verification>
- `cargo test -p app hot_key_profile_reuses_one_partition_key -- --nocapture`
- `cargo test -p app external_process_http_stress_smoke -- --nocapture`
- `cargo run -q -p app -- http-stress --profile smoke --workload-shape single-hot-key --warmup-seconds 1 --measure-seconds 2 --concurrency 2 --command-count 8 --shard-count 2 --ingress-capacity 8 --ring-size 16`
- `cargo run -q -p app -- http-stress --profile smoke --workload-shape hot-set --hot-set-size 4 --warmup-seconds 1 --measure-seconds 2 --concurrency 2 --command-count 8 --shard-count 2 --ingress-capacity 8 --ring-size 16`
</verification>

<success_criteria>
- The harness can submit enough load to expose real runtime ceilings instead of a client-side 1 kHz pacing ceiling.
- Hot-key and hot-set traffic are truthful workload shapes, not unique-key traffic mislabeled as hot.
- Live reports include scrape-accounting fields and ring-wait visibility so OBS-02 metrics are trustworthy enough for diagnosis.
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-02-SUMMARY.md`
</output>
