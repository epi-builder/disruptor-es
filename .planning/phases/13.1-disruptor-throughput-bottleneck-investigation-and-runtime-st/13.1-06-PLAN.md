---
phase: 13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st
plan: 6
type: execute
wave: 2
depends_on:
  - 13.1-05
files_modified:
  - benches/storage_only.rs
  - scripts/compare-stress-layers.sh
  - docs/stress-results.md
  - docs/template-guide.md
autonomous: true
gap_closure: true
requirements:
  - TEST-03
  - TEST-04
  - OBS-02
must_haves:
  truths:
    - "The archived layer comparison includes a real storage-only measurement without requiring manual `DATABASE_URL` setup."
    - "Baseline live HTTP evidence is regenerated only after the corrected report semantics are in place."
    - "Any ceiling classification narrower than `inconclusive` is backed by successful baseline metric scrapes, while repeated-stream diagnostic lanes stay excluded from throughput-baseline claims."
  artifacts:
    - path: "benches/storage_only.rs"
      provides: "self-contained storage benchmark execution path with automatic PostgreSQL fallback"
      contains: "connect_storage_pool"
    - path: "scripts/compare-stress-layers.sh"
      provides: "repeatable comparison workflow that writes baseline and diagnostic artifacts under corrected names"
      contains: "live-http-single-hot-key-diagnostic.json"
    - path: "target/phase-13.1/layer-comparison/storage-only.txt"
      provides: "archived storage-only benchmark output"
      contains: "storage_only_append"
    - path: "target/phase-13.1/layer-comparison/live-http-shard-1.json"
      provides: "regenerated baseline shard-count evidence under corrected report semantics"
      contains: "metrics_scrape_successes"
  key_links:
    - from: "scripts/compare-stress-layers.sh"
      to: "benches/storage_only.rs"
      via: "storage-only benchmark execution"
      pattern: "storage_only"
    - from: "scripts/compare-stress-layers.sh"
      to: "target/phase-13.1/layer-comparison/live-http-shard-1.json"
      via: "baseline rerun"
      pattern: "live-http-shard-1.json"
    - from: "docs/stress-results.md"
      to: "scripts/compare-stress-layers.sh"
      via: "operator instructions and interpretation rules"
      pattern: "compare-stress-layers"
---

<objective>
Regenerate the missing evidence chain under the corrected report semantics and restate Phase 13.1 only as strongly as the new measurements allow.

Purpose: Gap 1 remains open because the archived storage-only lane did not measure successfully and the baseline live HTTP archive had zero successful scrapes. This plan fixes the storage benchmark path, reruns the layer comparison after Plan 13.1-05, and updates the operator docs so future ceiling claims are scrape-gated and diagnostic lanes stay clearly separated.
Output: automated storage-only fallback, rerunnable comparison script with corrected filenames, regenerated baseline artifacts, and updated docs that encode the new evidence rules.
</objective>

<execution_context>
@/Users/epikem/.codex/get-shit-done/workflows/execute-plan.md
@/Users/epikem/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/RESEARCH.md
@.planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-VERIFICATION.md
@.planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-REVIEW.md
@.planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-VALIDATION.md
@benches/storage_only.rs
@scripts/compare-stress-layers.sh
@docs/stress-results.md
@docs/template-guide.md

<interfaces>
From `benches/storage_only.rs`:
```rust
fn database_url() -> Option<String>;
async fn connect_storage_pool() -> anyhow::Result<PgPool>;
```

From `scripts/compare-stress-layers.sh`:
```bash
PHASE13_1_COMPARE_MODE=smoke|baseline
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 13.1-06-01: Make the storage-only lane self-contained and rename repeated-stream evidence as diagnostic-only</name>
  <files>benches/storage_only.rs, scripts/compare-stress-layers.sh</files>
  <read_first>
    - benches/storage_only.rs
    - scripts/compare-stress-layers.sh
    - .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-VERIFICATION.md
    - .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-REVIEW.md
    - .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-VALIDATION.md
  </read_first>
  <behavior>
    - `cargo bench --bench storage_only -- --sample-size 10` produces real benchmark output without requiring a manually exported `DATABASE_URL`.
    - The comparison script archives repeated-stream output only under a diagnostic filename.
    - The script fails loudly if the storage lane still did not produce benchmark names.
  </behavior>
  <action>
    Implement one automated storage-only path and one corrected artifact naming rule:
    - In `benches/storage_only.rs`, replace the hard dependency on pre-set `DATABASE_URL` with a helper such as `StorageBenchHarness::connect_or_spawn()`. Keep `DATABASE_URL` override support, but when it is absent, start an ephemeral PostgreSQL `testcontainers_modules::postgres::Postgres` container, connect with `PgPoolOptions`, run `sqlx::migrate!("./migrations")`, and keep the container alive for the lifetime of the Criterion process.
    - Keep the existing benchmark function names (`storage_only_append`, `storage_only_occ_conflict`, `storage_only_dedupe`, `storage_only_global_read`) unchanged so archive comparisons stay stable.
    - In `scripts/compare-stress-layers.sh`, rename `live-http-single-hot-key.json` to `live-http-single-hot-key-diagnostic.json`.
    - After the storage bench command finishes, make the script check `target/phase-13.1/layer-comparison/storage-only.txt` for `storage_only_append`; if the pattern is absent, exit non-zero instead of silently accepting the lane as measured.

    Do not add any in-memory fallback or fabricated storage metrics. The lane must either benchmark a real PostgreSQL path or fail clearly.
  </action>
  <verify>
    <automated>cargo check --bench storage_only</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'storage_only_append|storage_only_occ_conflict|storage_only_dedupe|storage_only_global_read' benches/storage_only.rs` still finds the benchmark names.
    - `rg -n 'Postgres|AsyncRunner|sqlx::migrate|connect_storage_pool|connect_or_spawn' benches/storage_only.rs` shows the automated PostgreSQL fallback path.
    - `rg -n 'live-http-single-hot-key-diagnostic.json|storage-only.txt' scripts/compare-stress-layers.sh` shows the corrected diagnostic filename and storage-output validation.
    - `cargo check --bench storage_only` exits 0 as the fast task-local guard.
    - `cargo bench --bench storage_only -- --sample-size 10` emits benchmark output instead of `storage_only requires DATABASE_URL`.
  </acceptance_criteria>
  <done>The storage-only lane becomes self-contained and the repeated-stream live artifact is explicitly diagnostic-only.</done>
</task>

<task type="auto">
  <name>Task 13.1-06-02: Regenerate baseline comparison artifacts and document scrape-gated ceiling rules</name>
  <files>scripts/compare-stress-layers.sh, docs/stress-results.md, docs/template-guide.md</files>
  <read_first>
    - scripts/compare-stress-layers.sh
    - docs/stress-results.md
    - docs/template-guide.md
    - .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-VERIFICATION.md
    - .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-VALIDATION.md
    - target/phase-13.1/layer-comparison/live-http-unique.json
    - target/phase-13.1/layer-comparison/storage-only.txt
  </read_first>
  <behavior>
    - Baseline evidence is rerun only after the Plan 13.1-05 report-semantics changes are present.
    - The script writes `live-http-unique.json`, `live-http-shard-1.json`, `live-http-shard-8.json`, and `live-http-single-hot-key-diagnostic.json`.
    - Docs state that non-`inconclusive` ceiling claims require successful metric scrapes in the cited baseline files.
  </behavior>
  <action>
    Update the comparison workflow and operator docs, then regenerate the evidence:
    - Extend `scripts/compare-stress-layers.sh` baseline mode so one `PHASE13_1_COMPARE_MODE=baseline bash scripts/compare-stress-layers.sh` invocation writes all four live JSON artifacts: `live-http-unique.json`, `live-http-shard-1.json`, `live-http-shard-8.json`, and `live-http-single-hot-key-diagnostic.json`.
    - Keep the baseline unique lane on `--workload-shape unique`, and add explicit shard-count reruns for `--shard-count 1` and `--shard-count 8` using the corrected JSON/report contract from Plan 13.1-05.
    - Run `PHASE13_1_COMPARE_MODE=baseline bash scripts/compare-stress-layers.sh` after the script update so `target/phase-13.1/layer-comparison/` is regenerated under the new semantics.
    - Update `docs/stress-results.md` and `docs/template-guide.md` with three exact rules:
      1. `ingress_depth_max` and other scrape-derived fields are observed-only values.
      2. `ingress_depth_estimated_max` is a fallback hint, not a measured queue-depth result.
      3. Any ceiling classification narrower than `inconclusive` requires `metrics_scrape_successes > 0` in each cited baseline live HTTP artifact, while `live-http-single-hot-key-diagnostic.json` is diagnostic-only and excluded from success-throughput comparisons.

    The executor should record the exact `metrics_scrape_successes` values and final ceiling classification in the new `13.1-06-SUMMARY.md`. Do not modify `13.1-03-SUMMARY.md`.
  </action>
  <verify>
    <automated>PHASE13_1_COMPARE_MODE=smoke bash scripts/compare-stress-layers.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -f target/phase-13.1/layer-comparison/live-http-unique.json && test -f target/phase-13.1/layer-comparison/live-http-shard-1.json && test -f target/phase-13.1/layer-comparison/live-http-shard-8.json && test -f target/phase-13.1/layer-comparison/live-http-single-hot-key-diagnostic.json` succeeds.
    - `rg -n 'observed-only|ingress_depth_estimated_max|inconclusive|live-http-single-hot-key-diagnostic.json|metrics_scrape_successes' docs/stress-results.md docs/template-guide.md` finds the new interpretation rules.
    - `rg -n 'storage_only_append' target/phase-13.1/layer-comparison/storage-only.txt` succeeds.
    - `PHASE13_1_COMPARE_MODE=smoke bash scripts/compare-stress-layers.sh` exits 0 as the faster task-local guard.
    - `PHASE13_1_COMPARE_MODE=baseline bash scripts/compare-stress-layers.sh` exits 0.
  </acceptance_criteria>
  <done>The archived evidence is regenerated under corrected semantics and the docs encode the exact rules for future ceiling claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| storage benchmark -> PostgreSQL | The storage-only lane must measure a real durable store path, not an empty placeholder. |
| comparison script -> archived artifacts | File naming and failure behavior determine what future summaries and audits treat as valid evidence. |
| docs -> operator interpretation | The written guidance must prevent overclaiming when live scrapes are missing or repeated-stream lanes are diagnostic-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.1-06-01 | T | `benches/storage_only.rs` | mitigate | Use a real PostgreSQL path through explicit `DATABASE_URL` or an ephemeral testcontainer and fail instead of fabricating a storage result. |
| T-13.1-06-02 | R | `scripts/compare-stress-layers.sh` | mitigate | Validate storage output, write shard-count baseline artifacts explicitly, and keep repeated-stream output under a diagnostic filename. |
| T-13.1-06-03 | I | docs and final evidence interpretation | mitigate | Encode scrape-gated `inconclusive` rules and diagnostic-only workload handling directly in docs so later summaries cannot overstate the measurements. |
</threat_model>

<verification>
- `cargo bench --bench storage_only -- --sample-size 10`
- `PHASE13_1_COMPARE_MODE=baseline bash scripts/compare-stress-layers.sh`
- `rg -n 'storage_only_append' target/phase-13.1/layer-comparison/storage-only.txt`
</verification>

<success_criteria>
- Gap 1 is closed by a real storage-only benchmark path plus regenerated baseline artifacts under the corrected semantics.
- Baseline live HTTP evidence is only used for narrow ceiling claims when successful scrapes exist; otherwise the new summary must record `inconclusive`.
- WR-01 remains visible as diagnostic evidence without contaminating throughput-baseline interpretation.
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-06-SUMMARY.md`
</output>
