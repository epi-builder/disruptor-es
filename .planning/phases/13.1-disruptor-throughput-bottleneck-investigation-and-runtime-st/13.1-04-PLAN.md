---
phase: 13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st
plan: 4
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/es-runtime/src/engine.rs
  - crates/es-runtime/tests/runtime_flow.rs
  - crates/app/src/serve.rs
autonomous: true
gap_closure: true
requirements:
  - RUNTIME-01
  - RUNTIME-02
  - RUNTIME-05
must_haves:
  truths:
    - "Accepted ingress commands are never stranded during `app serve` shutdown."
    - "Already-dispatched shard work remains commit-gated even when shutdown has started."
    - "Stable shard routing and bounded ingress semantics remain unchanged while shutdown behavior becomes deterministic."
  artifacts:
    - path: "crates/es-runtime/src/engine.rs"
      provides: "deterministic shutdown drain-or-fail behavior for accepted routed commands"
      contains: "RuntimeError::Unavailable"
    - path: "crates/es-runtime/tests/runtime_flow.rs"
      provides: "shutdown regression coverage for undispatched and dispatched commands"
      contains: "accepted_but_undispatched_commands_receive_unavailable_on_shutdown"
    - path: "crates/app/src/serve.rs"
      provides: "official `app serve` lifecycle wiring through the corrected engine runner"
      contains: "engine.run"
  key_links:
    - from: "crates/app/src/serve.rs"
      to: "crates/es-runtime/src/engine.rs"
      via: "Notify-driven service shutdown"
      pattern: "Notify|engine.run"
    - from: "crates/es-runtime/src/engine.rs"
      to: "crates/es-runtime/tests/runtime_flow.rs"
      via: "runtime shutdown regressions"
      pattern: "Unavailable|shutdown"
---

<objective>
Close the runtime correctness gap from the verifier and code review by making shutdown deterministic for accepted ingress commands.

Purpose: Gap 3 and WR-03 show that `CommandEngine::run` can stop dispatching immediately on shutdown and strand accepted-but-undispatched commands. This plan restores the runtime reply contract without weakening bounded ingress or durable append gating.
Output: corrected `CommandEngine::run` shutdown semantics, runtime regression tests, and `app serve` wiring verified against the corrected lifecycle.
</objective>

<execution_context>
@/Users/epikem/.codex/get-shit-done/workflows/execute-plan.md
@/Users/epikem/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/RESEARCH.md
@.planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-VERIFICATION.md
@.planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-REVIEW.md
@crates/es-runtime/src/engine.rs
@crates/es-runtime/tests/runtime_flow.rs
@crates/app/src/serve.rs

<interfaces>
From `crates/es-runtime/src/engine.rs`:
```rust
pub async fn process_one(&mut self) -> RuntimeResult<bool>;
pub async fn run(mut self, shutdown: Arc<Notify>) -> RuntimeResult<()>;
async fn dispatch_routed(&self, routed: RoutedCommand<A>) -> RuntimeResult<()>;
```

From `crates/app/src/serve.rs`:
```rust
tokio::spawn(async move {
    engine.run(shutdown).await
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 13.1-04-01: Add deterministic shutdown regressions for accepted and dispatched commands</name>
  <files>crates/es-runtime/tests/runtime_flow.rs</files>
  <read_first>
    - crates/es-runtime/tests/runtime_flow.rs
    - crates/es-runtime/src/engine.rs
    - crates/app/src/serve.rs
    - .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-VERIFICATION.md
    - .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-REVIEW.md
  </read_first>
  <behavior>
    - Test 1: a command accepted into ingress but not yet dispatched resolves to `Err(RuntimeError::Unavailable)` when shutdown begins.
    - Test 2: a command already dispatched to a shard worker still completes after its append gate opens, even if shutdown was signaled earlier.
  </behavior>
  <action>
    Extend `crates/es-runtime/tests/runtime_flow.rs` with two explicit regressions that reproduce the verifier gap and WR-03:
    - Add `accepted_but_undispatched_commands_receive_unavailable_on_shutdown`, using `Notify`, `oneshot`, and a controlled runner start so one accepted command remains in `CommandEngine::receiver` when shutdown fires. Assert the reply resolves to `Err(RuntimeError::Unavailable)` instead of hanging.
    - Add `dispatched_commands_finish_before_engine_shutdown_returns`, using the existing fake-store append gate so the command is already inside `run_shard_worker` when shutdown fires. Assert `engine.run(...)` stays pending until the append gate opens, then returns `Ok(())` after the durable reply is delivered.

    Reuse the file’s existing fake-store helpers and synchronization gates. Do not add sleep-based timing guesses; use explicit `Notify`, append gates, or receiver coordination so the regressions are deterministic.
  </action>
  <verify>
    <automated>cargo test -p es-runtime accepted_but_undispatched_commands_receive_unavailable_on_shutdown -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'accepted_but_undispatched_commands_receive_unavailable_on_shutdown|dispatched_commands_finish_before_engine_shutdown_returns' crates/es-runtime/tests/runtime_flow.rs` finds both regression names.
    - `rg -n 'RuntimeError::Unavailable|Notify|oneshot' crates/es-runtime/tests/runtime_flow.rs` shows explicit shutdown assertions and deterministic coordination.
    - `cargo test -p es-runtime accepted_but_undispatched_commands_receive_unavailable_on_shutdown -- --nocapture` fails before implementation and exits 0 after implementation.
  </acceptance_criteria>
  <done>The shutdown gap is reproduced by automated tests before runtime code is changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 13.1-04-02: Drain undispatched ingress and preserve commit-gated worker completion during shutdown</name>
  <files>crates/es-runtime/src/engine.rs, crates/app/src/serve.rs, crates/es-runtime/tests/runtime_flow.rs</files>
  <read_first>
    - crates/es-runtime/src/engine.rs
    - crates/app/src/serve.rs
    - crates/es-runtime/tests/runtime_flow.rs
    - .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-VERIFICATION.md
    - .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-REVIEW.md
  </read_first>
  <behavior>
    - `CommandGateway::try_submit` remains bounded and unchanged.
    - Shutdown stops new dispatch, drains `self.receiver`, and replies `RuntimeError::Unavailable` to accepted-but-undispatched commands.
    - Already-dispatched shard work continues to use the durable append gate, and `run()` does not return until worker tasks finish.
  </behavior>
  <action>
    Update `crates/es-runtime/src/engine.rs` with one explicit shutdown policy:
    1. In `CommandEngine::run`, flip into a `shutting_down` state the first time `shutdown.notified()` resolves instead of breaking immediately.
    2. While `shutting_down` is true, stop awaiting new ingress and drain the existing `self.receiver` with `try_recv()`, sending `Err(RuntimeError::Unavailable)` on each drained envelope reply channel.
    3. Clear `self.shard_senders` only after the undispatched drain completes so shard workers stop receiving new work.
    4. Await every `worker_tasks` join handle and return `RuntimeError::Unavailable` only if a worker task panics or join fails.

    Keep these exact invariants:
    - Do not reroute commands or alter `PartitionRouter`.
    - Do not turn already-dispatched commands into shutdown failures.
    - Keep `crates/app/src/serve.rs` on the existing `Notify`-driven shutdown path, but make sure it continues to rely on the corrected `engine.run(shutdown)` lifecycle rather than any custom drain logic in `serve.rs`.
  </action>
  <verify>
    <automated>cargo test -p es-runtime runtime_flow -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'shutting_down|try_recv|RuntimeError::Unavailable|worker_tasks' crates/es-runtime/src/engine.rs` shows the drain-or-fail shutdown logic and worker joins.
    - `rg -n 'engine.run|Notify' crates/app/src/serve.rs` shows the official service path still flows through the corrected runner lifecycle.
    - `cargo test -p es-runtime dispatched_commands_finish_before_engine_shutdown_returns -- --nocapture` exits 0.
    - `cargo test -p es-runtime runtime_flow -- --nocapture` exits 0.
  </acceptance_criteria>
  <done>The runtime no longer strands accepted ingress commands during shutdown, and `app serve` uses the corrected lifecycle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter ingress -> runtime dispatcher | Accepted commands must receive a determinate result during shutdown. |
| runtime dispatcher -> shard workers | Ownership transfers here and must not silently drop accepted work. |
| shard workers -> durable event store | In-flight commands must stay commit-gated until append outcome is known. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.1-04-01 | D | `crates/es-runtime/src/engine.rs` | mitigate | Drain undispatched ingress and fail it explicitly instead of letting accepted commands hang during shutdown. |
| T-13.1-04-02 | T | `crates/es-runtime/src/engine.rs` + shard workers | mitigate | Join worker tasks after sender shutdown so already-dispatched commands keep their durable append gate and are not truncated mid-flight. |
| T-13.1-04-03 | R | `crates/app/src/serve.rs` | mitigate | Cover the exact `engine.run(shutdown)` service lifecycle with runtime regressions tied to the official serve wiring. |
</threat_model>

<verification>
- `cargo test -p es-runtime accepted_but_undispatched_commands_receive_unavailable_on_shutdown -- --nocapture`
- `cargo test -p es-runtime dispatched_commands_finish_before_engine_shutdown_returns -- --nocapture`
- `cargo test -p es-runtime runtime_flow -- --nocapture`
</verification>

<success_criteria>
- Accepted ingress commands never disappear during shutdown.
- Already-dispatched shard work remains commit-gated and can finish before engine exit.
- Gap 3 and WR-03 are directly closed by code plus regression tests.
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-04-SUMMARY.md`
</output>
