---
phase: 13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st
plan: 3
type: execute
wave: 2
depends_on:
  - 13.1-01
  - 13.1-02
files_modified:
  - scripts/compare-stress-layers.sh
  - docs/stress-results.md
  - docs/template-guide.md
  - .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-03-SUMMARY.md
autonomous: true
requirements:
  - TEST-03
  - TEST-04
  - OBS-02
must_haves:
  truths:
    - "One repeatable command can run ring-only, adapter-only, storage-only, in-process/runtime, and live HTTP lanes under one naming scheme."
    - "The comparison workflow preserves separation between ring capability and full-service durable HTTP throughput."
    - "Docs explain the remaining throughput ceiling and when projection/outbox pressure is or is not part of the measured lane."
  artifacts:
    - path: "scripts/compare-stress-layers.sh"
      provides: "repeatable layer-comparison runner with smoke and baseline modes"
      contains: "PHASE13_1_COMPARE_MODE"
    - path: "docs/stress-results.md"
      provides: "interpretation rules for Phase 13.1 layer comparisons"
      contains: "Layer comparison"
    - path: "docs/template-guide.md"
      provides: "exact operator commands for Phase 13.1 comparison runs"
      contains: "compare-stress-layers.sh"
    - path: ".planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-03-SUMMARY.md"
      provides: "phase-closing bottleneck classification with baseline layer and shard-count evidence"
      contains: "Dominant throughput ceiling"
  key_links:
    - from: "scripts/compare-stress-layers.sh"
      to: "crates/app/src/http_stress.rs"
      via: "script calls the updated live HTTP CLI with explicit workload shapes"
      pattern: "--workload-shape|http-stress"
    - from: "scripts/compare-stress-layers.sh"
      to: "docs/stress-results.md"
      via: "docs explain how to read the generated lane outputs"
      pattern: "layer comparison|ring-only|live HTTP"
    - from: "docs/template-guide.md"
      to: "scripts/compare-stress-layers.sh"
      via: "template guide points operators at the single comparison entrypoint"
      pattern: "compare-stress-layers.sh"
    - from: ".planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-03-SUMMARY.md"
      to: "target/phase-13.1/layer-comparison"
      via: "summary records generated evidence paths and classifies the measured ceiling by layer"
      pattern: "Dominant throughput ceiling|shard-count comparison|baseline"
---

<objective>
Add one repeatable layer-comparison entrypoint and document exactly how to interpret the resulting evidence so Phase 13.1 can close the throughput diagnosis with before/after artifacts.

Purpose: After the runtime and harness fixes land, the project still needs one consistent workflow that reproduces the layer comparison and states where disruptor ring capability ends and full-service durable HTTP throughput begins.
Output: repeatable comparison script plus updated operator docs for Phase 13.1 evidence and remaining throughput ceilings.
</objective>

<execution_context>
@/Users/epikem/.codex/get-shit-done/workflows/execute-plan.md
@/Users/epikem/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/RESEARCH.md
@.planning/phases/07-adapters-observability-stress-and-template-guidance/07-06-SUMMARY.md
@.planning/phases/12-external-process-http-e2e-stress-and-benchmark-closure/12-02-SUMMARY.md
@.planning/phases/13-live-external-process-http-steady-state-stress-testing/13-02-SUMMARY.md
@docs/stress-results.md
@docs/template-guide.md
@benches/ring_only.rs
@benches/adapter_only.rs
@benches/storage_only.rs
@crates/app/src/main.rs
@crates/app/src/stress.rs
@crates/app/src/http_stress.rs

<interfaces>
From `crates/app/src/main.rs` after Plan 13.1-02:
```text
app http-stress --profile <profile> --workload-shape <shape> --hot-set-size <N>
```

From `crates/app/src/stress.rs`:
```rust
pub struct StressReport {
    pub throughput_per_second: f64,
    pub append_latency_p95_micros: u64,
    pub ring_wait_p95_micros: u64,
    pub metrics_scrape_successes: usize,
    pub metrics_scrape_failures: usize,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 13.1-03-01: Create one repeatable layer-comparison script with smoke and baseline modes</name>
  <files>scripts/compare-stress-layers.sh</files>
  <read_first>
    - benches/ring_only.rs
    - benches/adapter_only.rs
    - benches/storage_only.rs
    - crates/app/src/main.rs
    - crates/app/src/stress.rs
    - crates/app/src/http_stress.rs
    - .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/RESEARCH.md
    - .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-VALIDATION.md
  </read_first>
  <action>
    Create `scripts/compare-stress-layers.sh` as the single Phase 13.1 comparison entrypoint. The script must:
    - default `PHASE13_1_COMPARE_MODE` to `smoke`
    - accept only `smoke` or `baseline`
    - create `target/phase-13.1/layer-comparison`
    - write each lane to a separate labeled file under that directory

    Implement these exact lane commands:
    - ring-only: `cargo bench --bench ring_only -- --sample-size 10`
    - adapter-only: `cargo bench --bench adapter_only -- --sample-size 10`
    - storage-only: `cargo bench --bench storage_only -- --sample-size 10`
    - in-process/runtime: `cargo run -q -p app -- stress-smoke`
    - live HTTP unique-key lane:
      - smoke mode: `cargo run -q -p app -- http-stress --profile smoke --workload-shape unique --warmup-seconds 1 --measure-seconds 2 --concurrency 2 --command-count 8 --shard-count 2 --ingress-capacity 8 --ring-size 16`
      - baseline mode: `cargo run -q -p app -- http-stress --profile baseline --workload-shape unique --warmup-seconds 5 --measure-seconds 30 --concurrency 8 --shard-count 8 --ingress-capacity 256 --ring-size 256`
    - live HTTP hot-key lane:
      - smoke mode: `cargo run -q -p app -- http-stress --profile smoke --workload-shape single-hot-key --warmup-seconds 1 --measure-seconds 2 --concurrency 2 --command-count 8 --shard-count 2 --ingress-capacity 8 --ring-size 16`
      - baseline mode: `cargo run -q -p app -- http-stress --profile hot-key --workload-shape single-hot-key --warmup-seconds 3 --measure-seconds 20 --concurrency 16 --shard-count 8 --ingress-capacity 128 --ring-size 256`

    Label the output files exactly:
    - `ring-only.txt`
    - `adapter-only.txt`
    - `storage-only.txt`
    - `in-process-runtime.json`
    - `live-http-unique.json`
    - `live-http-single-hot-key.json`
  </action>
  <verify>
    <automated>rg -n 'PHASE13_1_COMPARE_MODE|ring_only|adapter_only|storage_only|stress-smoke|http-stress|single-hot-key|target/phase-13.1/layer-comparison' scripts/compare-stress-layers.sh && test -x scripts/compare-stress-layers.sh</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'PHASE13_1_COMPARE_MODE|ring_only|adapter_only|storage_only|stress-smoke|http-stress|single-hot-key|target/phase-13.1/layer-comparison' scripts/compare-stress-layers.sh` finds the exact lane commands and output directory.
    - `test -f scripts/compare-stress-layers.sh` succeeds and `test -x scripts/compare-stress-layers.sh` succeeds after implementation.
    - `PHASE13_1_COMPARE_MODE=smoke bash scripts/compare-stress-layers.sh` exits 0 and creates all six expected output files in `target/phase-13.1/layer-comparison`.
  </acceptance_criteria>
  <done>One command can regenerate the layer comparison with consistent labels and output paths.</done>
</task>

<task type="auto">
  <name>Task 13.1-03-02: Update docs to explain the comparison workflow and the remaining throughput ceiling</name>
  <files>docs/stress-results.md, docs/template-guide.md</files>
  <read_first>
    - docs/stress-results.md
    - docs/template-guide.md
    - scripts/compare-stress-layers.sh
    - .planning/phases/07-adapters-observability-stress-and-template-guidance/07-06-SUMMARY.md
    - .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/RESEARCH.md
  </read_first>
  <action>
    Update `docs/stress-results.md` with a new `## Layer comparison` section that:
    - lists the six output files from `scripts/compare-stress-layers.sh`
    - states that `ring-only.txt` measures local disruptor handoff cost and must not be read as durable-service throughput
    - states that `in-process-runtime.json` and the two `live-http-*.json` files include durable PostgreSQL append, HTTP overhead, and bounded admission behavior
    - states that projector and outbox lag stay observational in these command-path runs and that extra projector/outbox pressure must be enabled separately if the operator wants an active projection/outbox bottleneck lane
    - names these report keys as required for live HTTP comparison review: `throughput_per_second`, `p50_micros`, `p95_micros`, `p99_micros`, `max_micros`, `commands_rejected`, `commands_failed`, `ingress_depth_max`, `shard_depth_max`, `append_latency_p95_micros`, `ring_wait_p95_micros`, `metrics_scrape_successes`, `metrics_scrape_failures`, `metrics_sample_count`, `workload_shape`, and `hot_set_size`

    Update `docs/template-guide.md` with exact operator commands for:
    - `PHASE13_1_COMPARE_MODE=smoke bash scripts/compare-stress-layers.sh`
    - `PHASE13_1_COMPARE_MODE=baseline bash scripts/compare-stress-layers.sh`
    - direct live HTTP runs for `--workload-shape unique`, `--workload-shape hot-set --hot-set-size 8`, and `--workload-shape single-hot-key`

    Add explicit guidance that the throughput ceiling after Phase 13.1 must be explained in terms of the slowest measured layer, for example durable PostgreSQL append, HTTP request/response overhead, bounded admission, or command-path side effects, instead of attributing every shortfall to the disruptor ring.
  </action>
  <verify>
    <automated>rg -n 'Layer comparison|compare-stress-layers.sh|ring-only|single-hot-key|hot-set|projection|outbox|throughput_per_second|ring_wait_p95_micros|metrics_scrape_successes|throughput ceiling|PostgreSQL append|HTTP overhead' docs/stress-results.md docs/template-guide.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Layer comparison|ring-only.txt|adapter-only.txt|storage-only.txt|in-process-runtime.json|live-http-unique.json|live-http-single-hot-key.json' docs/stress-results.md` finds the script output inventory.
    - `rg -n 'projection|outbox|separately|command-path runs' docs/stress-results.md` shows projector/outbox separation guidance.
    - `rg -n 'PHASE13_1_COMPARE_MODE=smoke|PHASE13_1_COMPARE_MODE=baseline|--workload-shape unique|--workload-shape hot-set|--workload-shape single-hot-key' docs/template-guide.md` finds the exact operator commands.
    - `rg -n 'PostgreSQL append|HTTP overhead|bounded admission|throughput ceiling' docs/stress-results.md docs/template-guide.md` finds the remaining-ceiling explanation language.
  </acceptance_criteria>
  <done>Operators have one comparison workflow and unambiguous guidance for interpreting the resulting throughput ceiling.</done>
</task>

<task type="auto">
  <name>Task 13.1-03-03: Run post-fix evidence comparison and record the dominant throughput ceiling</name>
  <files>.planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-03-SUMMARY.md, docs/stress-results.md</files>
  <read_first>
    - scripts/compare-stress-layers.sh
    - docs/stress-results.md
    - docs/template-guide.md
    - .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/RESEARCH.md
    - .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-01-SUMMARY.md
    - .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-02-SUMMARY.md
  </read_first>
  <action>
    After Plans `13.1-01` and `13.1-02` complete, run the phase-closing measurements and write a concrete bottleneck classification artifact.

    Run these exact evidence commands:
    - baseline layer comparison: `PHASE13_1_COMPARE_MODE=baseline bash scripts/compare-stress-layers.sh`
    - shard-count single-shard lane: `cargo run -q -p app -- http-stress --profile baseline --workload-shape unique --warmup-seconds 3 --measure-seconds 10 --concurrency 8 --shard-count 1 --ingress-capacity 256 --ring-size 256 > target/phase-13.1/layer-comparison/live-http-shard-1.json`
    - shard-count multi-shard lane: `cargo run -q -p app -- http-stress --profile baseline --workload-shape unique --warmup-seconds 3 --measure-seconds 10 --concurrency 8 --shard-count 8 --ingress-capacity 256 --ring-size 256 > target/phase-13.1/layer-comparison/live-http-shard-8.json`

    Create `.planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-03-SUMMARY.md` with these exact sections:
    - `## Evidence Commands`
    - `## Layer Outputs`
    - `## Shard-Count Comparison`
    - `## Dominant Throughput Ceiling`
    - `## Before/After Interpretation`
    - `## Remaining Limits`

    In `## Layer Outputs`, list all expected files under `target/phase-13.1/layer-comparison`: `ring-only.txt`, `adapter-only.txt`, `storage-only.txt`, `in-process-runtime.json`, `live-http-unique.json`, `live-http-single-hot-key.json`, `live-http-shard-1.json`, and `live-http-shard-8.json`.

    In `## Shard-Count Comparison`, record `throughput_per_second`, `p50_micros`, `p95_micros`, `p99_micros`, `commands_rejected`, `commands_failed`, `append_latency_p95_micros`, `ring_wait_p95_micros`, `metrics_scrape_successes`, `metrics_scrape_failures`, `cpu_utilization_percent`, and `core_count` for both `shard_count=1` and `shard_count=8`.

    In `## Dominant Throughput Ceiling`, classify the measured ceiling as exactly one of: `ring-only`, `adapter-only`, `storage-only`, `runtime/in-process`, `live HTTP`, `measurement/harness`, or `inconclusive`. Include one sentence explaining why the chosen layer is the current ceiling.

    Update `docs/stress-results.md` with a `### Phase 13.1 result` subsection that links to the summary file and states the classified ceiling.
  </action>
  <verify>
    <automated>rg -n 'Evidence Commands|Layer Outputs|Shard-Count Comparison|Dominant Throughput Ceiling|Before/After Interpretation|Remaining Limits|live-http-shard-1.json|live-http-shard-8.json|throughput_per_second|cpu_utilization_percent|core_count' .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-03-SUMMARY.md docs/stress-results.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f target/phase-13.1/layer-comparison/live-http-shard-1.json` succeeds and `test -f target/phase-13.1/layer-comparison/live-http-shard-8.json` succeeds after execution.
    - `rg -n 'Evidence Commands|Layer Outputs|Shard-Count Comparison|Dominant Throughput Ceiling|Before/After Interpretation|Remaining Limits' .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-03-SUMMARY.md` finds all required sections.
    - `rg -n 'ring-only|adapter-only|storage-only|runtime/in-process|live HTTP|measurement/harness|inconclusive' .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-03-SUMMARY.md` shows the ceiling classification vocabulary is used.
    - `rg -n 'live-http-shard-1.json|live-http-shard-8.json|throughput_per_second|p95_micros|p99_micros|append_latency_p95_micros|ring_wait_p95_micros|metrics_scrape_successes|metrics_scrape_failures|cpu_utilization_percent|core_count' .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-03-SUMMARY.md` finds the shard-count evidence fields.
    - `rg -n 'Phase 13.1 result|Dominant throughput ceiling|13.1-03-SUMMARY.md' docs/stress-results.md` finds the archived result pointer and classification.
  </acceptance_criteria>
  <done>The phase records actual post-fix evidence and classifies the throughput ceiling instead of only preparing the tools to do so.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator command -> comparison script | The script chooses which evidence lanes run and where outputs are written. |
| comparison outputs -> archive-facing documentation | Mislabeling the lane outputs would create false conclusions about disruptor throughput. |
| docs -> future template users | Guidance must preserve the boundary between ring capability and full-service durable throughput. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.1-07 | R | `scripts/compare-stress-layers.sh` output inventory | mitigate | Emit one file per lane with fixed names and document the mapping in `docs/stress-results.md` so evidence provenance is grep-verifiable. |
| T-13.1-08 | T | `docs/stress-results.md` interpretation guidance | mitigate | State explicitly that ring-only output is not durable-service throughput and that full-service JSON lanes include PostgreSQL append and HTTP overhead. |
| T-13.1-09 | D | `scripts/compare-stress-layers.sh` execution mode | mitigate | Restrict `PHASE13_1_COMPARE_MODE` to `smoke` or `baseline`, keep smoke fast for automation, and avoid any unbounded or background loop that could hang CI or local verification. |
| T-13.1-10 | R | `13.1-03-SUMMARY.md` bottleneck classification | mitigate | Record exact evidence commands, output file paths, shard-count metrics, and the classified ceiling so final archive sign-off is auditable. |
</threat_model>

<verification>
- `PHASE13_1_COMPARE_MODE=smoke bash scripts/compare-stress-layers.sh`
- `PHASE13_1_COMPARE_MODE=baseline bash scripts/compare-stress-layers.sh`
- `rg -n 'Layer comparison|compare-stress-layers.sh|ring-only|single-hot-key|hot-set|throughput ceiling' docs/stress-results.md docs/template-guide.md`
- `rg -n 'Shard-Count Comparison|Dominant Throughput Ceiling|live-http-shard-1.json|live-http-shard-8.json' .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-03-SUMMARY.md`
</verification>

<success_criteria>
- The project has one repeatable layer-comparison command for Phase 13.1.
- Documentation separates ring-only capability from in-process and live HTTP durable-service throughput.
- Operators know how to interpret remaining bottlenecks without conflating command-path and projector/outbox pressure.
- Phase evidence records a baseline layer comparison, a `shard_count=1` versus `shard_count=8` comparison, and one explicit dominant throughput ceiling classification.
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-03-SUMMARY.md`
</output>
