---
phase: 13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st
plan: 3
type: execute
wave: 2
depends_on:
  - 13.1-01
  - 13.1-02
files_modified:
  - benches/storage_only.rs
  - scripts/compare-stress-layers.sh
  - docs/stress-results.md
  - docs/template-guide.md
  - .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-03-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - TEST-03
  - TEST-04
  - OBS-02
must_haves:
  truths:
    - "The archived comparison includes a successful storage-only measurement or explicitly records an inconclusive storage lane after an automated attempt."
    - "At least one regenerated baseline live HTTP run records successful metric scrapes, or the final ceiling classification is explicitly narrowed to `inconclusive`."
    - "Repeated-stream hot-key evidence is archived and named as diagnostic-only, not as a throughput baseline."
  artifacts:
    - path: "benches/storage_only.rs"
      provides: "self-sufficient storage benchmark execution path"
      contains: "DATABASE_URL"
    - path: "scripts/compare-stress-layers.sh"
      provides: "repeatable comparison workflow with diagnostic lane naming"
      contains: "live-http-single-hot-key-diagnostic.json"
    - path: ".planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-03-SUMMARY.md"
      provides: "updated evidence classification with scrape-success facts"
      contains: "Dominant Throughput Ceiling"
  key_links:
    - from: "scripts/compare-stress-layers.sh"
      to: "benches/storage_only.rs"
      via: "storage-only benchmark execution"
      pattern: "storage_only"
    - from: "scripts/compare-stress-layers.sh"
      to: ".planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-03-SUMMARY.md"
      via: "regenerated evidence paths"
      pattern: "layer-comparison"
    - from: "docs/stress-results.md"
      to: "scripts/compare-stress-layers.sh"
      via: "operator instructions"
      pattern: "compare-stress-layers"
---

<objective>
Regenerate the missing evidence chain after Plans 01 and 02 land, then restate the Phase 13.1 ceiling only as strongly as the new evidence supports.

Purpose: The current archive still lacks a real storage-only measurement, baseline live HTTP scrape success, and clear separation between throughput lanes and repeated-stream diagnostics. This plan closes the evidence gap instead of assuming the earlier classification still holds.
Output: successful or explicitly inconclusive storage-only evidence, regenerated layer-comparison artifacts with successful scrape expectations, updated docs, and a corrected phase summary.
</objective>

<execution_context>
@/Users/epikem/.codex/get-shit-done/workflows/execute-plan.md
@/Users/epikem/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/RESEARCH.md
@.planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-VERIFICATION.md
@.planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-03-SUMMARY.md
@benches/storage_only.rs
@scripts/compare-stress-layers.sh
@docs/stress-results.md
@docs/template-guide.md

<interfaces>
From `benches/storage_only.rs`:
```rust
fn database_url() -> Option<String>;
fn connect_storage_pool() -> anyhow::Result<PgPool>;
```

From `scripts/compare-stress-layers.sh`:
```bash
PHASE13_1_COMPARE_MODE=smoke|baseline
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 13.1-03-01: Make the storage-only comparison lane produce real archived measurements</name>
  <files>benches/storage_only.rs, scripts/compare-stress-layers.sh</files>
  <read_first>
    - benches/storage_only.rs
    - scripts/compare-stress-layers.sh
    - .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-VERIFICATION.md
    - .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-03-SUMMARY.md
    - crates/app/src/http_stress.rs
  </read_first>
  <behavior>
    - `cargo bench --bench storage_only -- --sample-size 10` produces actual benchmark output in a normal repo run instead of only `storage_only requires DATABASE_URL`.
    - The comparison script treats repeated-stream hot-key output as diagnostic-only and writes it to a filename that says so.
    - The script fails loudly if the storage-only lane did not produce benchmark lines.
  </behavior>
  <action>
    Implement one deterministic storage-only execution path:
    - Update `benches/storage_only.rs` so the benchmark can run without a pre-exported `DATABASE_URL`. Preferred path: when `DATABASE_URL` is absent, start an ephemeral PostgreSQL testcontainer, run migrations, and then execute the Criterion cases against that temporary database. Preserve support for an explicit `DATABASE_URL` override.
    - Update `scripts/compare-stress-layers.sh` to expect the storage benchmark to succeed and to fail if `target/phase-13.1/layer-comparison/storage-only.txt` lacks benchmark names such as `storage_only_append`.
    - Rename the repeated-stream lane output from `live-http-single-hot-key.json` to `live-http-single-hot-key-diagnostic.json` in the script so the filename itself no longer reads as a baseline throughput artifact.

    Do not add a fake storage fallback or suppress failures. The script must either capture real storage-only measurements or stop the run so the later summary can mark the lane inconclusive with evidence.
  </action>
  <verify>
    <automated>cargo bench --bench storage_only -- --sample-size 10</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'storage_only_append|storage_only_occ_conflict|storage_only_dedupe|storage_only_global_read' benches/storage_only.rs` still finds the benchmark cases.
    - `rg -n 'live-http-single-hot-key-diagnostic.json|storage-only.txt' scripts/compare-stress-layers.sh` shows the renamed diagnostic lane and storage output handling.
    - `cargo bench --bench storage_only -- --sample-size 10` emits benchmark output instead of `storage_only requires DATABASE_URL`.
  </acceptance_criteria>
  <done>The layer-comparison workflow can archive a real storage-only measurement and no longer presents repeated-stream hot-key output as a baseline file.</done>
</task>

<task type="auto">
  <name>Task 13.1-03-02: Regenerate baseline evidence and restate the ceiling only as strongly as the new data allows</name>
  <files>scripts/compare-stress-layers.sh, docs/stress-results.md, docs/template-guide.md, .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-03-SUMMARY.md</files>
  <read_first>
    - scripts/compare-stress-layers.sh
    - docs/stress-results.md
    - docs/template-guide.md
    - .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-VERIFICATION.md
    - .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-03-SUMMARY.md
    - target/phase-13.1/layer-comparison/live-http-unique.json
    - target/phase-13.1/layer-comparison/storage-only.txt
  </read_first>
  <behavior>
    - One baseline comparison command regenerates `live-http-unique.json`, `live-http-shard-1.json`, and `live-http-shard-8.json` under the corrected report semantics.
    - Any non-`inconclusive` ceiling claim is backed by at least one successful scrape in the unique baseline and both shard-count reruns.
    - Repeated-stream evidence remains archived only as `live-http-single-hot-key-diagnostic.json`.
  </behavior>
  <action>
    After Plans 01 and 02 are complete, regenerate the archived evidence and update the written interpretation:
    - Update `scripts/compare-stress-layers.sh` baseline mode so one `PHASE13_1_COMPARE_MODE=baseline bash scripts/compare-stress-layers.sh` invocation writes all baseline artifacts: `live-http-unique.json`, `live-http-shard-1.json`, and `live-http-shard-8.json`, each generated under the corrected report semantics from Plan 13.1-02.
    - Run `PHASE13_1_COMPARE_MODE=baseline bash scripts/compare-stress-layers.sh` after that script update so the archived evidence is regenerated in one deterministic pass.
    - Require at least one successful metrics scrape for `live-http-unique.json` and the shard-count comparison JSONs before asserting a layer ceiling narrower than `inconclusive`.
    - If baseline live HTTP scrapes still fail after the documented rerun attempt, update `.planning/.../13.1-03-SUMMARY.md` so `Dominant Throughput Ceiling` is exactly `inconclusive`, with the reason tied to `metrics_scrape_successes = 0`.
    - Update `docs/stress-results.md` and `docs/template-guide.md` to document the new observed-versus-estimated fields, the diagnostic-only repeated-stream filename, and the rule that only successful scrape runs may support append/ring/queue-depth conclusions.

    Keep the final summary precise:
    - Cite the successful storage-only lane if present.
    - Cite the exact `metrics_scrape_successes` values from regenerated live HTTP artifacts.
    - Treat `live-http-single-hot-key-diagnostic.json` as a contention diagnostic, not a throughput baseline.
  </action>
  <verify>
    <automated>PHASE13_1_COMPARE_MODE=baseline bash scripts/compare-stress-layers.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -f target/phase-13.1/layer-comparison/live-http-single-hot-key-diagnostic.json` succeeds.
    - `test -f target/phase-13.1/layer-comparison/live-http-shard-1.json && test -f target/phase-13.1/layer-comparison/live-http-shard-8.json` succeeds.
    - `rg -n 'Dominant Throughput Ceiling|inconclusive|metrics_scrape_successes|live-http-single-hot-key-diagnostic.json' .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-03-SUMMARY.md docs/stress-results.md docs/template-guide.md` finds the updated interpretation rules.
    - `rg -n 'storage_only_append' target/phase-13.1/layer-comparison/storage-only.txt` succeeds, or the summary explicitly records the storage lane as inconclusive with the failed command and reason.
    - `rg -n '\"metrics_scrape_successes\": [1-9]' target/phase-13.1/layer-comparison/live-http-unique.json target/phase-13.1/layer-comparison/live-http-shard-1.json target/phase-13.1/layer-comparison/live-http-shard-8.json` succeeds before the summary claims a non-`inconclusive` ceiling.
  </acceptance_criteria>
  <done>The archived comparison either proves the remaining ceiling with successful storage and scripted unique plus shard-count scrape evidence or explicitly narrows the classification to `inconclusive` without overstating what was measured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| benchmark harness -> storage database | The storage-only lane must use a real database path so archived measurements reflect durable append cost. |
| comparison script -> archived evidence files | The script controls which artifacts are later cited as proof, so filenames and failure behavior matter. |
| archived evidence -> docs and summary | Final written claims must not exceed what regenerated measurements actually support. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.1-07 | T | `benches/storage_only.rs` | mitigate | Use a real PostgreSQL target, either explicit `DATABASE_URL` or an ephemeral testcontainer, and fail instead of fabricating a storage result. |
| T-13.1-08 | R | `scripts/compare-stress-layers.sh` | mitigate | Make the script fail when storage-only output is missing and rename repeated-stream output to a diagnostic filename so the evidence set is self-describing. |
| T-13.1-09 | I | summary/docs evidence claims | mitigate | Gate any non-`inconclusive` ceiling claim on successful metric scrapes and a successful storage-only lane, with exact archived values cited in the summary. |
</threat_model>

<verification>
- `cargo bench --bench storage_only -- --sample-size 10`
- `PHASE13_1_COMPARE_MODE=baseline bash scripts/compare-stress-layers.sh`
- `rg -n '"metrics_scrape_successes": [1-9]' target/phase-13.1/layer-comparison/live-http-unique.json target/phase-13.1/layer-comparison/live-http-shard-1.json target/phase-13.1/layer-comparison/live-http-shard-8.json`
</verification>

<success_criteria>
- The archived layer comparison includes a real storage-only measurement or an explicit inconclusive storage record.
- The final Phase 13.1 summary does not classify the ceiling more narrowly than the regenerated scrape evidence allows.
- Repeated-stream hot-key output is preserved as diagnostic evidence but excluded from throughput-baseline claims.
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-03-SUMMARY.md`
</output>
