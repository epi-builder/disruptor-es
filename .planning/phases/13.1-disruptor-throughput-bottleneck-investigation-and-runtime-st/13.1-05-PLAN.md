---
phase: 13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st
plan: 5
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/app/src/http_stress.rs
  - crates/app/src/stress.rs
  - crates/app/src/main.rs
  - benches/external_process_http.rs
autonomous: true
gap_closure: true
requirements:
  - TEST-04
  - OBS-02
must_haves:
  truths:
    - "Live HTTP reports never present fabricated queue-depth or latency fields as observed metrics."
    - "Observed scrape values are distinguishable from estimates or missing scrapes in both Rust structs and emitted JSON."
    - "Repeated-stream order workloads are marked as diagnostic-only evidence so WR-01 cannot be mistaken for success-throughput proof."
  artifacts:
    - path: "crates/app/src/stress.rs"
      provides: "report schema with observed-only metrics and explicit fallback/diagnostic fields"
      contains: "ingress_depth_estimated_max"
    - path: "crates/app/src/http_stress.rs"
      provides: "live runner logic that preserves observed-versus-estimated semantics"
      contains: "metrics_scrape_successes"
    - path: "crates/app/src/main.rs"
      provides: "JSON output that exposes the corrected field names and workload-purpose label"
      contains: "workload_purpose"
  key_links:
    - from: "crates/app/src/http_stress.rs"
      to: "crates/app/src/stress.rs"
      via: "StressReport construction"
      pattern: "StressReport"
    - from: "crates/app/src/main.rs"
      to: "crates/app/src/stress.rs"
      via: "serialized JSON report fields"
      pattern: "ingress_depth|workload_purpose"
---

<objective>
Close the report-semantics gaps before any new evidence is regenerated.

Purpose: Gaps 1 and 2, plus WR-01 and WR-02, show that the current live HTTP report can mislabel estimated queue depth as observed data and can present repeated-stream order failures as if they were normal throughput lanes. This plan fixes the report contract first so later evidence reruns are trustworthy.
Output: corrected `StressReport` schema, live harness logic that preserves observed-versus-estimated data boundaries, JSON regression coverage, and explicit diagnostic-only labeling for repeated-stream order lanes.
</objective>

<execution_context>
@/Users/epikem/.codex/get-shit-done/workflows/execute-plan.md
@/Users/epikem/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/RESEARCH.md
@.planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-VERIFICATION.md
@.planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-REVIEW.md
@crates/app/src/http_stress.rs
@crates/app/src/stress.rs
@crates/app/src/main.rs
@benches/external_process_http.rs

<interfaces>
From `crates/app/src/stress.rs`:
```rust
pub struct StressReport {
    pub ingress_depth_max: usize,
    pub shard_depth_max: usize,
    pub append_latency_p95_micros: u64,
    pub ring_wait_p95_micros: u64,
    pub projection_lag: i64,
    pub outbox_lag: i64,
    pub metrics_scrape_successes: u64,
}
```

From `crates/app/src/http_stress.rs`:
```rust
Ok(StressReport {
    ingress_depth_max: measured.metrics.ingress_depth_max.max(...),
    shard_depth_max: measured.metrics.shard_depth_max,
    append_latency_p95_micros: measured.metrics.append_latency_p95_micros,
    workload_shape: config.workload_shape.as_str().to_string(),
    ...
})
```

From `crates/app/src/main.rs`:
```rust
fn stress_report_json(report: &app::stress::StressReport) -> serde_json::Value
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 13.1-05-01: Add regressions for observed-only metrics and repeated-stream diagnostic labeling</name>
  <files>crates/app/src/http_stress.rs, crates/app/src/main.rs</files>
  <read_first>
    - crates/app/src/http_stress.rs
    - crates/app/src/stress.rs
    - crates/app/src/main.rs
    - .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-VERIFICATION.md
    - .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-REVIEW.md
  </read_first>
  <behavior>
    - Test 1: `ingress_depth_max` is absent or `None` when `metrics_scrape_successes == 0`; it is never synthesized from concurrency.
    - Test 2: the JSON report exposes a separate `ingress_depth_estimated_max` field when a fallback estimate is emitted.
    - Test 3: repeated-stream order lanes emit `workload_purpose = "repeat-stream-diagnostic"` instead of looking like baseline success-throughput evidence.
  </behavior>
  <action>
    Add focused regressions that fail against the current report contract:
    - In `crates/app/src/http_stress.rs`, add `observed_ingress_depth_is_not_synthesized_from_concurrency`, constructing a `MeasuredState` with zero successful scrapes and asserting the produced `StressReport.ingress_depth_max` stays absent while `ingress_depth_estimated_max` carries any fallback estimate.
    - In `crates/app/src/main.rs`, add `stress_report_json_separates_estimated_ingress_depth`, asserting the JSON output contains `ingress_depth_estimated_max` and does not serialize an estimated value as the observed `ingress_depth_max`.
    - Add `repeated_stream_shapes_are_marked_diagnostic_only`, asserting that `HttpWorkloadShape::HotSet(_)` and `HttpWorkloadShape::SingleHotKey` on the current `PlaceOrder` path serialize `workload_purpose` as `repeat-stream-diagnostic`.

    Use the exact regression names above so the gap closure is easy to verify and trace.
  </action>
  <verify>
    <automated>cargo test -p app observed_ingress_depth_is_not_synthesized_from_concurrency -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'observed_ingress_depth_is_not_synthesized_from_concurrency|stress_report_json_separates_estimated_ingress_depth|repeated_stream_shapes_are_marked_diagnostic_only' crates/app/src/http_stress.rs crates/app/src/main.rs` finds all three regressions.
    - `rg -n 'ingress_depth_estimated_max|workload_purpose|repeat-stream-diagnostic' crates/app/src/http_stress.rs crates/app/src/main.rs` shows the exact field names under test.
    - `cargo test -p app observed_ingress_depth_is_not_synthesized_from_concurrency -- --nocapture` fails before implementation and exits 0 after implementation.
  </acceptance_criteria>
  <done>The required report semantics are pinned by automated regressions before production code changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 13.1-05-02: Implement observed-only scrape fields and explicit diagnostic metadata in the live HTTP report</name>
  <files>crates/app/src/http_stress.rs, crates/app/src/stress.rs, crates/app/src/main.rs, benches/external_process_http.rs</files>
  <read_first>
    - crates/app/src/http_stress.rs
    - crates/app/src/stress.rs
    - crates/app/src/main.rs
    - benches/external_process_http.rs
    - .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-VERIFICATION.md
    - .planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-REVIEW.md
  </read_first>
  <behavior>
    - Scrape-derived report fields are observed values or explicit `None`, never silent zero-valued placeholders.
    - `ingress_depth_max` is reserved for observed values only.
    - Repeated-stream order lanes carry an explicit machine-readable diagnostic label.
  </behavior>
  <action>
    Implement one concrete report contract across `crates/app/src/stress.rs`, `crates/app/src/http_stress.rs`, and `crates/app/src/main.rs`:
    - Change scrape-derived fields in `StressReport` to observed-only optional types: `ingress_depth_max`, `shard_depth_max`, `append_latency_p95_micros`, `ring_wait_p95_micros`, `projection_lag`, and `outbox_lag` should become `Option<...>`.
    - Add `ingress_depth_estimated_max: Option<usize>` and populate it only when `metrics_scrape_successes == 0`, using the existing `config.concurrency.min(counters.commands_submitted)` heuristic as an explicitly estimated fallback rather than mutating `ingress_depth_max`.
    - Add `workload_purpose: String` with exact values `success-throughput` for `HttpWorkloadShape::Unique` and `repeat-stream-diagnostic` for `HotSet(_)` and `SingleHotKey` on the current repeated-order-stream path called out by WR-01.
    - Update `stress_report_json(...)` to emit those exact field names and optional values.
    - Update `benches/external_process_http.rs` so the smoke benchmark continues to compile against the revised `StressReport` shape without adding any new benchmark behavior.

    Do not change load generation, routing, or business responses in this plan. This plan is only for report semantics and machine-readable evidence labeling.
  </action>
  <verify>
    <automated>cargo test -p app http_stress -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Option<usize>|Option<u64>|Option<i64>|ingress_depth_estimated_max|workload_purpose' crates/app/src/stress.rs crates/app/src/http_stress.rs crates/app/src/main.rs` shows the revised schema and serialization fields.
    - `rg -n 'metrics_scrape_successes == 0|ingress_depth_estimated_max|repeat-stream-diagnostic|success-throughput' crates/app/src/http_stress.rs` shows the explicit fallback and workload-purpose logic.
    - `cargo test -p app stress_report_json_separates_estimated_ingress_depth -- --nocapture` exits 0.
    - `cargo test -p app http_stress -- --nocapture` exits 0.
  </acceptance_criteria>
  <done>The live HTTP report cleanly separates observed data, estimated fallbacks, and repeated-stream diagnostics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prometheus metrics -> `StressReport` | Observability data must not be mislabeled when archived as throughput evidence. |
| `StressReport` -> JSON output | Downstream scripts and summaries treat these fields as source-of-truth and must see observed-versus-estimated differences explicitly. |
| workload-shape config -> evidence interpretation | Reused order streams must not masquerade as normal success-throughput lanes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.1-05-01 | T | `crates/app/src/http_stress.rs` | mitigate | Keep observed scrape fields separate from estimated fallbacks so queue-depth evidence cannot be fabricated. |
| T-13.1-05-02 | I | `crates/app/src/main.rs` JSON output | mitigate | Serialize optional observed fields and explicitly named fallback fields so missing scrapes do not look like zero queueing or zero append latency. |
| T-13.1-05-03 | R | repeated-stream order workloads | mitigate | Emit `workload_purpose = repeat-stream-diagnostic` so WR-01 traffic cannot be cited as success-throughput evidence. |
</threat_model>

<verification>
- `cargo test -p app observed_ingress_depth_is_not_synthesized_from_concurrency -- --nocapture`
- `cargo test -p app stress_report_json_separates_estimated_ingress_depth -- --nocapture`
- `cargo test -p app repeated_stream_shapes_are_marked_diagnostic_only -- --nocapture`
- `cargo test -p app http_stress -- --nocapture`
</verification>

<success_criteria>
- Gap 2 is closed because `ingress_depth_max` is no longer synthesized from concurrency.
- OBS-02 and TEST-04 evidence fields clearly distinguish observed values from missing data.
- WR-01 is accounted for in machine-readable output without changing domain behavior or fabricating “successful” hot-key throughput.
</success_criteria>

<output>
After completion, create `.planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-05-SUMMARY.md`
</output>
