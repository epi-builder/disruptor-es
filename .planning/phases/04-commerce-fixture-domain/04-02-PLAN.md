---
phase: 04-commerce-fixture-domain
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - crates/example-commerce/src/user.rs
autonomous: true
requirements:
  - DOM-01
  - DOM-02
  - DOM-05
must_haves:
  truths:
    - "Developer can register a user and replay the emitted user event into user state."
    - "Developer can activate and deactivate a registered user through typed commands."
    - "Invalid user lifecycle transitions return typed errors instead of panics or string errors."
  artifacts:
    - path: "crates/example-commerce/src/user.rs"
      provides: "User aggregate state machine"
      exports: ["User", "UserStatus", "UserState", "UserCommand", "UserEvent", "UserReply", "UserError"]
  key_links:
    - from: "crates/example-commerce/src/user.rs"
      to: "crates/es-kernel/src/lib.rs"
      via: "impl Aggregate for User"
      pattern: "impl Aggregate for User"
    - from: "crates/example-commerce/src/user.rs"
      to: "crates/example-commerce/src/ids.rs"
      via: "UserId in commands/events/state"
      pattern: "UserId"
---

<objective>
Implement the user lifecycle aggregate.

Purpose: The commerce fixture must prove a related user model with replayable lifecycle events and typed invalid-transition errors while staying inside deterministic domain logic.
Output: `User` aggregate implementation and user-specific unit/property tests.
</objective>

<execution_context>
@/Users/epikem/.codex/get-shit-done/workflows/execute-plan.md
@/Users/epikem/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-commerce-fixture-domain/04-RESEARCH.md
@.planning/phases/04-commerce-fixture-domain/04-PATTERNS.md
@.planning/phases/04-commerce-fixture-domain/04-VALIDATION.md
@.planning/phases/04-commerce-fixture-domain/04-01-SUMMARY.md
@crates/example-commerce/src/user.rs
@crates/example-commerce/src/ids.rs
@crates/es-kernel/src/lib.rs

<interfaces>
Plan 04-01 creates:
```rust
pub struct UserId(String);
impl UserId {
    pub fn new(value: impl Into<String>) -> Result<Self, CommerceIdError>;
    pub fn as_str(&self) -> &str;
    pub fn into_inner(self) -> String;
}
```

Aggregate contract from `es-kernel`:
```rust
fn stream_id(command: &Self::Command) -> es_core::StreamId;
fn partition_key(command: &Self::Command) -> es_core::PartitionKey;
fn expected_revision(command: &Self::Command) -> es_core::ExpectedRevision;
fn decide(
    state: &Self::State,
    command: Self::Command,
    metadata: &es_core::CommandMetadata,
) -> Result<Decision<Self::Event, Self::Reply>, Self::Error>;
fn apply(state: &mut Self::State, event: &Self::Event);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add user lifecycle decision tests</name>
  <files>crates/example-commerce/src/user.rs</files>
  <read_first>
    <file>crates/example-commerce/src/user.rs</file>
    <file>crates/example-commerce/src/ids.rs</file>
    <file>crates/es-kernel/src/lib.rs</file>
    <file>.planning/phases/04-commerce-fixture-domain/04-VALIDATION.md</file>
    <file>.planning/phases/04-commerce-fixture-domain/04-PATTERNS.md</file>
  </read_first>
  <behavior>
    - `RegisterUser { user_id, email: "a@example.test", display_name: "Ada" }` on default state emits `UserEvent::UserRegistered { user_id, email, display_name }` and returns `UserReply::Registered { user_id }`.
    - `ActivateUser { user_id }` after registration emits `UserEvent::UserActivated { user_id }`.
    - `DeactivateUser { user_id }` after activation emits `UserEvent::UserDeactivated { user_id }`.
    - Duplicate registration returns `UserError::AlreadyRegistered`.
    - Activation before registration returns `UserError::NotRegistered`.
    - Duplicate activation returns `UserError::AlreadyActive`.
    - Duplicate deactivation returns `UserError::AlreadyInactive`.
  </behavior>
  <action>Add module-local tests in `crates/example-commerce/src/user.rs` before implementing behavior. Include deterministic `metadata()` using `CommandMetadata { command_id: Uuid::from_u128(11), correlation_id: Uuid::from_u128(12), causation_id: None, tenant_id: TenantId::new("tenant-a").expect("tenant id"), requested_at: OffsetDateTime::from_unix_timestamp(1_700_000_000).expect("timestamp") }`. Add tests named exactly `register_user_emits_registered_event`, `user_lifecycle_activate_and_deactivate_is_replayable`, and `user_rejects_invalid_lifecycle_transitions`. Use `es_kernel::replay::<User>` in the replay test. Do not use async, storage, global maps, or process-manager helpers.</action>
  <verify>
    <automated>cargo test -p example-commerce user -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg "register_user_emits_registered_event" crates/example-commerce/src/user.rs` returns a match.
    - `rg "user_lifecycle_activate_and_deactivate_is_replayable" crates/example-commerce/src/user.rs` returns a match.
    - `rg "user_rejects_invalid_lifecycle_transitions" crates/example-commerce/src/user.rs` returns a match.
    - `rg "es_kernel::replay::<User>" crates/example-commerce/src/user.rs` returns a match.
    - `cargo test -p example-commerce user -- --nocapture` exits 0 after Task 2 is complete.
  </acceptance_criteria>
  <done>User lifecycle expectations are captured in executable tests with exact typed events, replies, and errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement user aggregate state machine</name>
  <files>crates/example-commerce/src/user.rs</files>
  <read_first>
    <file>crates/example-commerce/src/user.rs</file>
    <file>crates/example-commerce/src/ids.rs</file>
    <file>crates/es-core/src/lib.rs</file>
    <file>crates/es-kernel/src/lib.rs</file>
    <file>.planning/phases/04-commerce-fixture-domain/04-RESEARCH.md</file>
  </read_first>
  <action>Implement `User` as an `es_kernel::Aggregate`. Define `UserStatus` as `Unregistered`, `Active`, and `Inactive`, with `Default` returning `Unregistered`. Define `UserState { pub user_id: Option<UserId>, pub email: Option<String>, pub display_name: Option<String>, pub status: UserStatus }`. Define `UserCommand` variants exactly `RegisterUser { user_id: UserId, email: String, display_name: String }`, `ActivateUser { user_id: UserId }`, and `DeactivateUser { user_id: UserId }`. Define `UserEvent` variants exactly `UserRegistered { user_id: UserId, email: String, display_name: String }`, `UserActivated { user_id: UserId }`, and `UserDeactivated { user_id: UserId }`. Define `UserReply` variants exactly `Registered { user_id: UserId }`, `Activated { user_id: UserId }`, and `Deactivated { user_id: UserId }`. Define `UserError` variants exactly `EmptyEmail`, `EmptyDisplayName`, `AlreadyRegistered`, `NotRegistered`, `AlreadyActive`, and `AlreadyInactive`, deriving `thiserror::Error`. `stream_id` and `partition_key` must be `StreamId::new(format!("user-{}", user_id.as_str()))` and `PartitionKey::new(format!("user-{}", user_id.as_str()))`. `expected_revision` must return `ExpectedRevision::NoStream` for `RegisterUser` and `ExpectedRevision::Any` for activation/deactivation. `decide` must validate empty email/display name, duplicate registration, not-registered operations, duplicate activation, and duplicate deactivation before emitting one event. `apply` must be the only state mutator and must set state from events.</action>
  <verify>
    <automated>cargo test -p example-commerce user</automated>
  </verify>
  <acceptance_criteria>
    - `rg "impl Aggregate for User" crates/example-commerce/src/user.rs` returns a match.
    - `rg "pub enum UserStatus" crates/example-commerce/src/user.rs` returns a match.
    - `rg "RegisterUser \\{ user_id: UserId, email: String, display_name: String \\}" crates/example-commerce/src/user.rs` returns a match.
    - `rg "UserRegistered \\{ user_id: UserId, email: String, display_name: String \\}" crates/example-commerce/src/user.rs` returns a match.
    - `rg "ExpectedRevision::NoStream" crates/example-commerce/src/user.rs` returns a match.
    - `rg "ExpectedRevision::Any" crates/example-commerce/src/user.rs` returns a match.
    - `rg "AlreadyRegistered" crates/example-commerce/src/user.rs` returns a match.
    - `rg "AlreadyActive" crates/example-commerce/src/user.rs` returns a match.
    - `rg "AlreadyInactive" crates/example-commerce/src/user.rs` returns a match.
    - `cargo test -p example-commerce user` exits 0.
  </acceptance_criteria>
  <done>User commands register, activate, and deactivate users; replay rebuilds user state; invalid lifecycle transitions return typed `UserError` variants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| command caller -> `User::decide` | Untrusted user command fields enter deterministic domain validation. |
| user aggregate -> future order/process-manager logic | User lifecycle status becomes an assumption source for later order placement and workflow coordination. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | `UserCommand::RegisterUser` | mitigate | Reject empty `email` with `UserError::EmptyEmail` and empty `display_name` with `UserError::EmptyDisplayName`. |
| T-04-06 | Tampering | user lifecycle state machine | mitigate | Reject duplicate registration, activation before registration, duplicate activation, and duplicate deactivation through typed errors. |
| T-04-07 | Repudiation | replayable user events | mitigate | Emit explicit `UserRegistered`, `UserActivated`, and `UserDeactivated` events; tests replay those events with `es_kernel::replay::<User>`. |
| T-04-08 | Information Disclosure | tenant isolation | accept | User aggregate has no global storage or shared mutable state; tenant isolation remains in command metadata/runtime/store boundaries from prior phases. |
</threat_model>

<verification>
Run `cargo test -p example-commerce user`.
Run `rg "Arc<Mutex|static mut|lazy_static" crates/example-commerce/src/user.rs` and confirm there are no matches.
</verification>

<success_criteria>
Plan 02 succeeds when the user aggregate implements the kernel contract, emits replayable lifecycle events, rejects invalid lifecycle transitions with typed errors, and passes `cargo test -p example-commerce user`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-commerce-fixture-domain/04-02-SUMMARY.md`.
</output>
