---
phase: 07-adapters-observability-stress-and-template-guidance
plan: 07
type: execute
gap_closure: true
wave: 5
depends_on:
  - 07-02
  - 07-05
files_modified:
  - crates/es-store-postgres/src/projection.rs
  - crates/es-store-postgres/tests/phase7_integration.rs
  - crates/es-runtime/src/engine.rs
  - crates/app/src/stress.rs
autonomous: true
requirements:
  - OBS-02
  - TEST-02
  - TEST-04
must_haves:
  truths:
    - "Projection lag is computed from the tenant's durable maximum event-store global position, including idle catch-up paths."
    - "A backlog-sized PostgreSQL integration test fails if es_projection_lag is batch-local or always zero."
    - "Single-service stress reports append latency, shard depth, and projection lag from measured sources instead of synthetic proxies."
    - "Controlled stress coverage fails if projection_lag is always zero while a durable backlog remains."
  artifacts:
    - path: "crates/es-store-postgres/src/projection.rs"
      provides: "Tenant durable max-position projection lag computation"
      contains: "SELECT COALESCE(max(global_position), 0) FROM events WHERE tenant_id = $1"
    - path: "crates/es-store-postgres/tests/phase7_integration.rs"
      provides: "Backlog-sized projection lag integration coverage"
    - path: "crates/es-runtime/src/engine.rs"
      provides: "Read-only shard depth sampling for stress reports"
      exports: ["shard_depths"]
    - path: "crates/app/src/stress.rs"
      provides: "Measured append latency, shard depth, and projection lag in StressReport"
      exports: ["StressReport", "run_single_service_stress"]
  key_links:
    - from: "crates/es-store-postgres/src/projection.rs"
      to: "events.global_position"
      via: "tenant_latest_global_position query before lag gauge update"
      pattern: "max\\(global_position\\).*events.*tenant_id"
    - from: "crates/app/src/stress.rs"
      to: "crates/es-runtime/src/engine.rs"
      via: "run_single_service_stress samples CommandEngine::shard_depths"
      pattern: "shard_depths\\("
    - from: "crates/app/src/stress.rs"
      to: "RuntimeEventStore::append"
      via: "MeasuredRuntimeEventStore records append duration around inner.append"
      pattern: "append_latency.*record"
---

<objective>
Close Phase 07 verifier gaps for OBS-02 and TEST-04 without rewriting completed Plans 07-01 through 07-06.

Purpose: Projection lag and single-service stress fields must be trustworthy operational signals. They must come from durable event-store positions, runtime queue state, and measured append calls, not batch-local math or placeholder values.
Output: One gap-closure implementation plan covering projection lag, measured stress signals, and regression tests.
</objective>

<execution_context>
@/Users/epikem/.codex/get-shit-done/workflows/execute-plan.md
@/Users/epikem/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-adapters-observability-stress-and-template-guidance/07-VERIFICATION.md
@.planning/phases/07-adapters-observability-stress-and-template-guidance/07-REVIEW.md
@.planning/phases/07-adapters-observability-stress-and-template-guidance/07-RESEARCH.md
@.planning/phases/07-adapters-observability-stress-and-template-guidance/07-PATTERNS.md
@crates/es-store-postgres/src/projection.rs
@crates/es-store-postgres/tests/phase7_integration.rs
@crates/es-runtime/src/engine.rs
@crates/es-runtime/src/shard.rs
@crates/es-runtime/src/store.rs
@crates/app/src/stress.rs

<interfaces>
Use these existing contracts directly:

```rust
impl PostgresProjectionStore {
    pub async fn projector_offset(
        &self,
        tenant_id: &TenantId,
        projector_name: &ProjectorName,
    ) -> ProjectionResult<Option<ProjectorOffset>>;

    pub async fn catch_up(
        &self,
        tenant_id: &TenantId,
        projector_name: &ProjectorName,
        limit: ProjectionBatchLimit,
    ) -> ProjectionResult<CatchUpOutcome>;
}

pub trait RuntimeEventStore: Clone + Send + Sync + 'static {
    fn append(
        &self,
        request: es_store_postgres::AppendRequest,
    ) -> futures::future::BoxFuture<'_, es_store_postgres::StoreResult<es_store_postgres::AppendOutcome>>;

    fn load_rehydration(
        &self,
        tenant_id: &es_core::TenantId,
        stream_id: &es_core::StreamId,
    ) -> futures::future::BoxFuture<'_, es_store_postgres::StoreResult<es_store_postgres::RehydrationBatch>>;
}

impl<A, S, C> CommandEngine<A, S, C>
where
    A: Aggregate,
    S: RuntimeEventStore,
    C: RuntimeEventCodec<A>,
{
    pub fn gateway(&self) -> CommandGateway<A>;
    pub async fn process_one(&mut self) -> RuntimeResult<bool>;
}

impl<A: Aggregate> ShardHandle<A> {
    pub fn pending_len(&self) -> usize;
    pub fn state(&self) -> &ShardState<A>;
}

impl<A: Aggregate> ShardState<A> {
    pub fn pending_handoffs(&self) -> usize;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 07-07-01: Compute projection lag from tenant durable max global position</name>
  <files>crates/es-store-postgres/src/projection.rs, crates/es-store-postgres/tests/phase7_integration.rs</files>
  <read_first>
    - crates/es-store-postgres/src/projection.rs
    - crates/es-store-postgres/tests/phase7_integration.rs
    - crates/es-store-postgres/src/event_store.rs
    - crates/es-store-postgres/src/sql.rs
    - crates/es-store-postgres/tests/projections.rs
    - .planning/phases/07-adapters-observability-stress-and-template-guidance/07-VERIFICATION.md
    - .planning/phases/07-adapters-observability-stress-and-template-guidance/07-REVIEW.md
  </read_first>
  <behavior>
    - With 250 tenant events, a first `catch_up(..., ProjectionBatchLimit::new(25)?)` applies 25 events and the captured `es_projection_lag` gauge equals `tenant_latest_global_position - projector_offset.last_global_position`, not the fetched batch size and not zero.
    - When `catch_up` returns `CatchUpOutcome::Idle`, the captured `es_projection_lag` gauge is still computed from `tenant_latest_global_position - current_offset`, not unconditionally set to zero.
    - Tenant A lag ignores events committed under Tenant B.
  </behavior>
  <action>In `PostgresProjectionStore::catch_up`, add a private async helper named `tenant_latest_global_position(pool: &sqlx::PgPool, tenant_id: &TenantId) -> ProjectionResult<i64>` that runs this exact SQL through `sqlx::query_scalar::<_, i64>`: `SELECT COALESCE(max(global_position), 0) FROM events WHERE tenant_id = $1`. Call this helper after loading `current_offset` and before returning from either the empty-batch or applied-batch path. Replace the empty-batch `gauge!("es_projection_lag", ...).set(0.0)` with `(tenant_latest_position - current_offset).max(0) as f64`. After the projection transaction commits for a non-empty batch, set `es_projection_lag` to `(tenant_latest_position - last_global_position).max(0) as f64`; do not set it before commit. Keep `CatchUpOutcome::Applied { event_count, last_global_position }` unchanged.

In `phase7_integration.rs`, add a helper to append a sequence of valid `OrderPlaced` events for one tenant. Add test-only metric capture helpers named `ProjectionLagRecorder`, `ProjectionLagGauge`, and `observed_projection_lag_value`. `ProjectionLagRecorder` must implement `metrics::Recorder`; `register_gauge` must return `metrics::Gauge::from_arc(...)` only when `key.name() == "es_projection_lag"` and the key labels include `projector = phase7-commerce-read-models`, with counters/histograms returning no-op handles. `ProjectionLagGauge` must implement `metrics::GaugeFn` and store the last `set` value so the test can read the exact emitted gauge. Use `metrics::set_default_local_recorder(&recorder)` in the test, and make the test `#[tokio::test(flavor = "current_thread")]` so the local recorder captures the async `gauge!` calls.

Add an integration test named `phase7_projection_lag_uses_tenant_durable_backlog_not_batch_size` that appends at least 250 events for `tenant-lag-a`, appends at least 3 events for `tenant-lag-b`, installs `ProjectionLagRecorder`, calls `catch_up` for `tenant-lag-a` with `ProjectionBatchLimit::new(25)?`, reads `projector_offset`, reads `SELECT COALESCE(max(global_position), 0) FROM events WHERE tenant_id = $1` for `tenant-lag-a`, computes `expected_lag = (tenant_latest - offset.last_global_position).max(0) as f64`, asserts `expected_lag > 25.0`, and asserts `observed_projection_lag_value(&gauge) == expected_lag`. This gauge-path assertion is mandatory; the test must fail against the current batch-local implementation even if the offset math query is correct. The test must then call `catch_up` repeatedly until idle and assert final offset equals tenant A latest and the last observed gauge value is `0.0`. Do not include WR-03 panic hardening in this task; leave event type/payload mismatch handling for a separate non-blocking follow-up.</action>
  <verify>
    <automated>cargo test -p es-store-postgres phase7_projection_lag_uses_tenant_durable_backlog_not_batch_size -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'tenant_latest_global_position|COALESCE\\(max\\(global_position\\), 0\\)|FROM events|WHERE tenant_id = \\$1|tenant_latest_position - current_offset|tenant_latest_position - last_global_position' crates/es-store-postgres/src/projection.rs` finds all listed patterns.
    - `! rg 'es_projection_lag.*set\\(0\\.0\\)' crates/es-store-postgres/src/projection.rs` exits 0.
    - `rg 'ProjectionLagRecorder|ProjectionLagGauge|observed_projection_lag_value|metrics::Recorder|metrics::GaugeFn|metrics::set_default_local_recorder|Gauge::from_arc|key\\.name\\(\\) == "es_projection_lag"|flavor = "current_thread"' crates/es-store-postgres/tests/phase7_integration.rs` finds all listed patterns.
    - `rg 'phase7_projection_lag_uses_tenant_durable_backlog_not_batch_size|ProjectionBatchLimit::new\\(25\\)|tenant-lag-a|tenant-lag-b|max\\(global_position\\)|expected_lag|observed_projection_lag_value\\(&gauge\\)|assert_eq!\\(.*expected_lag|assert_eq!\\(.*0\\.0' crates/es-store-postgres/tests/phase7_integration.rs` finds all listed patterns.
    - `cargo test -p es-store-postgres phase7_projection_lag_uses_tenant_durable_backlog_not_batch_size -- --nocapture` exits 0.
  </acceptance_criteria>
  <done>Projection lag no longer underreports backlog from batch-local math or idle zeroing, and PostgreSQL coverage proves the emitted `es_projection_lag` gauge receives `tenant_latest_global_position - projector_offset.last_global_position` for tenant-scoped durable backlog behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 07-07-02: Expose measured runtime shard depth and append latency to stress</name>
  <files>crates/es-runtime/src/engine.rs, crates/app/src/stress.rs</files>
  <read_first>
    - crates/es-runtime/src/engine.rs
    - crates/es-runtime/src/shard.rs
    - crates/es-runtime/src/store.rs
    - crates/app/src/stress.rs
    - crates/es-store-postgres/src/event_store.rs
    - .planning/phases/07-adapters-observability-stress-and-template-guidance/07-VERIFICATION.md
    - .planning/phases/07-adapters-observability-stress-and-template-guidance/07-REVIEW.md
  </read_first>
  <behavior>
    - `StressReport.append_latency_p95_micros` is populated from measured `RuntimeEventStore::append` duration, not command round-trip elapsed.
    - `StressReport.shard_depth_max` is populated from real runtime shard state using accepted-but-not-released and processable handoff depths, not `ingress_depth_max.min(config.ring_size)`.
    - Existing `single_service_stress_smoke` still passes and the report fields remain populated.
  </behavior>
  <action>In `crates/es-runtime/src/engine.rs`, add a public method `pub fn shard_depths(&self) -> Vec<usize>` on `CommandEngine<A, S, C>` that returns one entry per shard: `shard.pending_len() + shard.state().pending_handoffs()`. Do not expose mutable shard internals. In `crates/app/src/stress.rs`, replace `PostgresRuntimeEventStore::new(event_store.clone())` with a local `MeasuredRuntimeEventStore` wrapper type that implements `RuntimeEventStore` by delegating `load_rehydration` to an inner `PostgresRuntimeEventStore` and wrapping `append` with `Instant::now()` around `inner.append(request).await`. Store successful append durations in an `Arc<std::sync::Mutex<Vec<u64>>>` or `Arc<parking_lot::Mutex<Vec<u64>>>` using `micros(started.elapsed())`. Build the `append_latency` histogram from those measured append durations after command processing; remove `append_latency.record(elapsed)` from the command-reply loop. During submit and process loops, update `shard_depth_max` by sampling `engine.shard_depths().into_iter().max().unwrap_or(0)` before processing, after each `process_one().await?`, and after all replies are collected. Return that sampled value in `StressReport { shard_depth_max, ... }`. Do not derive shard depth from ingress depth or ring size.</action>
  <verify>
    <automated>cargo test -p app single_service_stress_smoke -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'pub fn shard_depths|pending_len\\(\\) \\+ shard\\.state\\(\\)\\.pending_handoffs\\(\\)' crates/es-runtime/src/engine.rs` finds both patterns.
    - `rg 'MeasuredRuntimeEventStore|impl RuntimeEventStore for MeasuredRuntimeEventStore|append_durations|Instant::now\\(\\)|inner\\.append\\(request\\)|engine\\.shard_depths\\(' crates/app/src/stress.rs` finds all listed patterns.
    - `! rg 'append_latency\\.record\\(elapsed\\)|shard_depth_max: ingress_depth_max\\.min\\(config\\.ring_size\\)' crates/app/src/stress.rs` exits 0.
    - `cargo test -p app single_service_stress_smoke -- --nocapture` exits 0.
  </acceptance_criteria>
  <done>Single-service stress report append latency and shard depth fields are measured from runtime/store sources rather than synthetic proxies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 07-07-03: Return measured stress projection lag and add backlog regression coverage</name>
  <files>crates/app/src/stress.rs</files>
  <read_first>
    - crates/app/src/stress.rs
    - crates/es-store-postgres/src/projection.rs
    - crates/es-store-postgres/src/event_store.rs
    - crates/es-runtime/src/engine.rs
    - .planning/phases/07-adapters-observability-stress-and-template-guidance/07-VERIFICATION.md
    - .planning/phases/07-adapters-observability-stress-and-template-guidance/07-REVIEW.md
  </read_first>
  <behavior>
    - `sample_projection_lag` returns the maximum tenant lag observed after projection catch-up, not `Ok(0)`.
    - A controlled backlog test fails if `sample_projection_lag` is always zero.
    - `run_single_service_stress(StressConfig::smoke())` continues to report nonnegative lag outside the command success gate.
  </behavior>
  <action>Change `sample_projection_lag` in `crates/app/src/stress.rs` so it computes tenant latest global position per tenant with SQL `SELECT COALESCE(max(global_position), 0) FROM events WHERE tenant_id = $1`, calls `projection_store.catch_up(&tenant, &projector, ProjectionBatchLimit::new(100)?)`, reloads the projector offset, computes `post_catch_up_lag = (tenant_latest_position - after).max(0)`, and returns the maximum lag across tenants. Remove the unused `_lag_sample` and `_post_catch_up_lag` locals and remove the final `Ok(0)`. Add a private helper `tenant_latest_global_position(pool: &PgPool, tenant: &TenantId) -> anyhow::Result<i64>` if Task 07-07-01's helper is not public. Add a test named `stress_projection_lag_reports_controlled_backlog` that starts the existing stress PostgreSQL harness, appends at least 105 valid `OrderPlaced` events for `tenant-0` through `PostgresEventStore`, calls `sample_projection_lag(&projection_store, 1, latest_global_position(&pool).await?)` with the existing batch size of 100, and asserts the returned lag is greater than zero. Keep projection and outbox lag sampling after command replies; do not gate command success on catch-up or dispatch.</action>
  <verify>
    <automated>cargo test -p app stress_projection_lag_reports_controlled_backlog -- --nocapture</automated>
    <automated>cargo test -p app single_service_stress_smoke -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'stress_projection_lag_reports_controlled_backlog|sample_projection_lag|tenant_latest_global_position|ProjectionBatchLimit::new\\(100\\)|post_catch_up_lag|max_projection_lag' crates/app/src/stress.rs` finds all listed patterns.
    - `rg 'Ok\\(max_projection_lag\\)|return Ok\\(max_projection_lag\\)' crates/app/src/stress.rs` finds one of the listed projection-lag return patterns.
    - `rg '_lag_sample|_post_catch_up_lag' crates/app/src/stress.rs` returns no matches.
    - `cargo test -p app stress_projection_lag_reports_controlled_backlog -- --nocapture` exits 0.
    - `cargo test -p app single_service_stress_smoke -- --nocapture` exits 0.
  </acceptance_criteria>
  <done>Stress projection lag is a measured durable backlog signal, and controlled backlog coverage prevents an always-zero regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PostgreSQL events -> projection metrics | Durable event-store positions are converted into freshness metrics used by operators. |
| Runtime/store internals -> stress report | Internal queue and append timing measurements are exposed as benchmark/stress signals. |
| Stress report -> template users | Users may make operational decisions from reported latency, depth, and lag values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-20 | Tampering | `es_projection_lag` gauge | mitigate | Compute lag from tenant-scoped durable `events.max(global_position)` and saved projector offsets; test tenant isolation. |
| T-07-21 | Repudiation | `StressReport.append_latency_p95_micros` | mitigate | Record append duration inside a `RuntimeEventStore` wrapper around the actual `append` call. |
| T-07-22 | Information Disclosure | stress/projection SQL | mitigate | SQL binds tenant ID values and does not print raw tenant IDs, command IDs, event IDs, or database URLs in report fields. |
| T-07-23 | Denial of Service | stress queue sampling | accept | Read-only `shard_depths()` samples bounded in-memory queue lengths and does not add blocking synchronization to the hot path. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Phase 07 observable metrics and stress credibility | Covered by Tasks 07-07-01 through 07-07-03. |
| REQ | OBS-02 projection lag and measured load metrics | Covered by Tasks 07-07-01, 07-07-02, and 07-07-03. |
| REQ | TEST-02 PostgreSQL integration coverage | Covered by Task 07-07-01. |
| REQ | TEST-04 single-service stress measured append latency, queue depths, and projection lag | Covered by Tasks 07-07-02 and 07-07-03. |
| RESEARCH | Event store is source of truth; projectors read committed global positions | Covered by tenant durable max-position SQL in Task 07-07-01. |
| REVIEW | WR-04 projection lag underreports backlog | Covered by Task 07-07-01. |
| REVIEW | WR-05 stress report emits synthetic/zero signals | Covered by Tasks 07-07-02 and 07-07-03. |
| REVIEW | WR-03 projection panic on mismatch | Excluded from this gap plan: advisory warning, not required for OBS-02/TEST-04 blocker closure; no task may modify event type/payload handling except as needed for lag tests. |
</source_audit>

<verification>
Run:

```bash
cargo test -p es-store-postgres phase7_projection_lag_uses_tenant_durable_backlog_not_batch_size -- --nocapture
cargo test -p app stress_projection_lag_reports_controlled_backlog -- --nocapture
cargo test -p app single_service_stress_smoke -- --nocapture
cargo test --workspace --no-run
```
</verification>

<success_criteria>
Plan succeeds when `es_projection_lag` is no longer batch-local or idle-zeroed, stress append latency is measured around event-store append calls, stress shard depth is read from runtime shard state, stress projection lag returns durable backlog after catch-up, and both new backlog regression tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/07-adapters-observability-stress-and-template-guidance/07-07-SUMMARY.md`.
</output>
