---
phase: 07-adapters-observability-stress-and-template-guidance
plan: 04
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - benches/ring_only.rs
  - benches/domain_only.rs
  - benches/adapter_only.rs
  - benches/storage_only.rs
  - benches/projector_outbox.rs
autonomous: true
requirements:
  - TEST-03
must_haves:
  truths:
    - "Benchmark artifacts separate ring-only, domain-only, adapter-only, storage-only, and projector/outbox scenarios."
    - "Ring-only benchmark output cannot be mistaken for service throughput."
  artifacts:
    - path: "benches/ring_only.rs"
      provides: "Disruptor publication/polling benchmark only"
    - path: "benches/domain_only.rs"
      provides: "In-memory commerce decide/apply benchmark only"
    - path: "benches/adapter_only.rs"
      provides: "HTTP DTO/envelope/gateway admission benchmark only"
    - path: "benches/storage_only.rs"
      provides: "PostgreSQL append/OCC/dedupe/read benchmark only"
    - path: "benches/projector_outbox.rs"
      provides: "Projection and outbox benchmark scenarios"
  key_links:
    - from: "benches/ring_only.rs"
      to: "crates/es-runtime/src/disruptor_path.rs"
      via: "DisruptorPath only"
      pattern: "DisruptorPath"
---

<objective>
Create layer-separated benchmark harnesses for TEST-03.

Purpose: The template needs benchmark artifacts that isolate bottlenecks instead of collapsing every number into one E2E score.
Output: Criterion-compatible benchmark files with clear scenario names and smoke commands.
</objective>

<execution_context>
@/Users/epikem/.codex/get-shit-done/workflows/execute-plan.md
@/Users/epikem/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-adapters-observability-stress-and-template-guidance/07-RESEARCH.md
@.planning/phases/07-adapters-observability-stress-and-template-guidance/07-PATTERNS.md
@crates/es-runtime/src/disruptor_path.rs
@crates/example-commerce/src/product.rs
@crates/es-runtime/tests/router_gateway.rs
@crates/es-store-postgres/tests/append_occ.rs
@crates/es-store-postgres/tests/projections.rs
@crates/es-store-postgres/tests/outbox.rs
</context>

<tasks>

<task type="auto">
  <name>Task 07-04-01: Add isolated microbenchmark harnesses</name>
  <files>benches/ring_only.rs, benches/domain_only.rs, benches/adapter_only.rs</files>
  <read_first>
    - benches/ring_only.rs
    - benches/domain_only.rs
    - benches/adapter_only.rs
    - crates/es-runtime/src/disruptor_path.rs
    - crates/example-commerce/src/product.rs
    - crates/es-runtime/tests/router_gateway.rs
    - .planning/phases/07-adapters-observability-stress-and-template-guidance/07-PATTERNS.md
  </read_first>
  <action>Create root `benches/` if missing. In `ring_only.rs`, define Criterion group `ring_only` and benchmark functions `ring_only_publish_poll` and `ring_only_hot_key_publish_poll` using `es_runtime::DisruptorPath` only; do not import `example_commerce`, `PostgresEventStore`, `axum`, or `adapter_http`. In `domain_only.rs`, define Criterion group `domain_only` with `domain_only_product_decide_apply` and `domain_only_order_lifecycle_decide_apply` using only `example_commerce`, `es_core`, and `es_kernel`. In `adapter_only.rs`, define Criterion group `adapter_only` with `adapter_only_decode_envelope_submit` and `adapter_only_burst_overload` using HTTP DTO construction, `CommandEnvelope`, and `CommandGateway::try_submit`; do not call `CommandEngine::process_one` or any PostgreSQL repository.</action>
  <verify>
    <automated>cargo bench --bench ring_only -- --warm-up-time 1 --measurement-time 3</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'criterion_group!|ring_only_publish_poll|ring_only_hot_key_publish_poll' benches/ring_only.rs` finds all listed patterns.
    - `rg 'domain_only_product_decide_apply|domain_only_order_lifecycle_decide_apply' benches/domain_only.rs` finds both patterns.
    - `rg 'adapter_only_decode_envelope_submit|adapter_only_burst_overload|CommandGateway|CommandEnvelope' benches/adapter_only.rs` finds all listed patterns.
    - `! rg 'PostgresEventStore|CommandEngine::process_one|dispatch_once' benches/ring_only.rs benches/domain_only.rs benches/adapter_only.rs` exits 0.
    - `cargo bench --bench ring_only -- --warm-up-time 1 --measurement-time 3` exits 0.
  </acceptance_criteria>
  <done>Ring, domain, and adapter benchmark artifacts are separated by imports and scenario names.</done>
</task>

<task type="auto">
  <name>Task 07-04-02: Add storage and projector/outbox benchmark harnesses</name>
  <files>benches/storage_only.rs, benches/projector_outbox.rs</files>
  <read_first>
    - benches/storage_only.rs
    - benches/projector_outbox.rs
    - crates/es-store-postgres/tests/append_occ.rs
    - crates/es-store-postgres/tests/projections.rs
    - crates/es-store-postgres/tests/outbox.rs
    - crates/es-store-postgres/tests/common/mod.rs
    - .planning/phases/07-adapters-observability-stress-and-template-guidance/07-VALIDATION.md
  </read_first>
  <action>In `storage_only.rs`, define Criterion group `storage_only` with scenario functions `storage_only_append`, `storage_only_occ_conflict`, `storage_only_dedupe`, and `storage_only_global_read`. Use a helper that reads `DATABASE_URL` and prints `storage_only requires DATABASE_URL` if absent; do not silently fall back to in-memory storage. In `projector_outbox.rs`, define Criterion group `projector_outbox` with `projector_catch_up` and `outbox_claim_publish`. The projector scenario must call `PostgresProjectionStore::catch_up`; the outbox scenario must call `dispatch_once` with `PostgresOutboxStore` and `InMemoryPublisher`. Keep these scenarios separate inside the file with Criterion benchmark IDs exactly `projector_catch_up` and `outbox_claim_publish`.</action>
  <verify>
    <automated>cargo bench --bench projector_outbox -- --warm-up-time 1 --measurement-time 3</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'storage_only_append|storage_only_occ_conflict|storage_only_dedupe|storage_only_global_read|DATABASE_URL' benches/storage_only.rs` finds all listed patterns.
    - `rg 'projector_catch_up|outbox_claim_publish|PostgresProjectionStore|PostgresOutboxStore|dispatch_once|InMemoryPublisher' benches/projector_outbox.rs` finds all listed patterns.
    - `cargo bench --bench projector_outbox -- --warm-up-time 1 --measurement-time 3` exits 0.
  </acceptance_criteria>
  <done>Storage and projector/outbox benchmark artifacts exist with explicit layer separation; integrated, full E2E, hot-key, burst, and degraded-dependency scenarios are covered by Plan 07-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bench harness -> database | Bench inputs may run against a developer-provided PostgreSQL URL. |
| Bench results -> documentation/users | Mislabelled benchmark numbers could drive unsafe production conclusions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | Information Disclosure | benchmark output | mitigate | Do not print tenant IDs, command IDs, event IDs, or database URLs in bench reports. |
| T-07-12 | Tampering | benchmark scenario interpretation | mitigate | Scenario names and comments explicitly separate ring-only, layer-only, and stress-runner results. |
| T-07-13 | Denial of Service | storage benchmark database | accept | Bench requires explicit `DATABASE_URL`; no automatic production target discovery is added. |
</threat_model>

<verification>
Run `cargo bench --bench ring_only -- --warm-up-time 1 --measurement-time 3`, `cargo bench --bench domain_only -- --warm-up-time 1 --measurement-time 3`, and `cargo bench --bench projector_outbox -- --warm-up-time 1 --measurement-time 3`.
</verification>

<success_criteria>
Plan succeeds when all required benchmark files exist, scenario names cover TEST-03, and smoke benchmark commands compile and run.
</success_criteria>

<output>
After completion, create `.planning/phases/07-adapters-observability-stress-and-template-guidance/07-04-SUMMARY.md`.
</output>
