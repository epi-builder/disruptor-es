---
phase: 07-adapters-observability-stress-and-template-guidance
plan: 05
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
  - 07-03
files_modified:
  - crates/app/Cargo.toml
  - crates/app/src/lib.rs
  - crates/app/src/main.rs
  - crates/app/src/stress.rs
autonomous: true
requirements:
  - TEST-04
  - TEST-03
  - OBS-02
must_haves:
  truths:
    - "Single-service stress uses production-shaped in-process composition and bounded ingress."
    - "Stress scenarios separately cover single-service integrated, full E2E in-process, hot-key, burst, and degraded-dependency behavior."
    - "Stress summary reports throughput, p50, p95, p99, queue depths, append latency, projection lag, outbox lag, reject rate, and CPU/core utilization."
    - "Projection and outbox lag are reported but not treated as command success gates."
  artifacts:
    - path: "crates/app/src/stress.rs"
      provides: "Single-service integrated stress runner core and scenario harness"
      exports: ["StressConfig", "StressScenario", "StressReport", "run_single_service_stress"]
    - path: "crates/app/src/main.rs"
      provides: "Thin bootstrap shell for stress mode"
  key_links:
    - from: "crates/app/src/stress.rs"
      to: "crates/es-runtime/src/engine.rs"
      via: "CommandEngine and CommandGateway"
      pattern: "CommandEngine"
---

<objective>
Build the single-service integrated stress runner required by TEST-04.

Purpose: The template needs one production-shaped load path in a single process that reports tail latency and lag without redefining command success.
Output: Stress runner core, smoke test, and thin app bootstrap.
</objective>

<execution_context>
@/Users/epikem/.codex/get-shit-done/workflows/execute-plan.md
@/Users/epikem/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-adapters-observability-stress-and-template-guidance/07-RESEARCH.md
@.planning/phases/07-adapters-observability-stress-and-template-guidance/07-PATTERNS.md
@.planning/phases/07-adapters-observability-stress-and-template-guidance/07-VALIDATION.md
@crates/app/src/commerce_process_manager.rs
@crates/app/src/observability.rs
@crates/es-runtime/src/engine.rs
@crates/es-runtime/tests/runtime_flow.rs
@crates/es-store-postgres/tests/phase7_integration.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 07-05-01: Implement stress runner contracts and smoke summary</name>
  <files>crates/app/Cargo.toml, crates/app/src/lib.rs, crates/app/src/stress.rs</files>
  <read_first>
    - crates/app/Cargo.toml
    - crates/app/src/lib.rs
    - crates/app/src/stress.rs
    - crates/app/src/commerce_process_manager.rs
    - crates/app/src/observability.rs
    - crates/es-runtime/src/engine.rs
    - crates/es-runtime/tests/runtime_flow.rs
  </read_first>
  <behavior>
    - `run_single_service_stress(StressConfig::smoke())` returns a `StressReport` with required fields populated.
    - `StressScenario` supports `SingleServiceIntegrated`, `FullE2eInProcess`, `HotKey`, `Burst`, and `DegradedDependency`.
    - Rejected commands increment `reject_rate` instead of panicking.
    - Projection lag and outbox lag appear in the report and are not used as command success conditions.
  </behavior>
  <action>Add app dependencies `hdrhistogram.workspace = true`, `metrics.workspace = true`, and `sysinfo.workspace = true`; add dev dependency `adapter-http = { path = "../adapter-http" }` if tests use DTO/router helpers. Export `pub mod stress;` from `lib.rs`. Create `stress.rs` with `StressScenario` variants `SingleServiceIntegrated`, `FullE2eInProcess`, `HotKey`, `Burst`, and `DegradedDependency`; `StressConfig { scenario: StressScenario, command_count: usize, concurrency: usize, shard_count: usize, ingress_capacity: usize, ring_size: usize, tenant_count: usize }`; `impl StressConfig { pub fn smoke() -> Self { ... } pub fn hot_key_smoke() -> Self { ... } pub fn burst_smoke() -> Self { ... } pub fn degraded_dependency_smoke() -> Self { ... } }`; `StressReport { scenario, commands_submitted, commands_succeeded, commands_rejected, throughput_per_second, p50_micros, p95_micros, p99_micros, max_micros, ingress_depth_max, shard_depth_max, append_latency_p95_micros, projection_lag, outbox_lag, reject_rate, cpu_utilization_percent, core_count }`; and `pub async fn run_single_service_stress(config: StressConfig) -> anyhow::Result<StressReport>`. Use `hdrhistogram::Histogram<u64>` for latency percentiles and `sysinfo::System` for CPU/core fields. Use bounded `CommandGateway`/`CommandEngine` composition or a local fake store matching `RuntimeEventStore` so smoke tests run without external services; the code must still exercise `CommandGateway::try_submit`, command reply awaiting, and `CommandEngine::process_one`. Add tests named `single_service_stress_smoke`, `full_e2e_in_process_stress_smoke`, `hot_key_stress_smoke`, `burst_stress_smoke`, and `degraded_dependency_stress_smoke`.</action>
  <verify>
    <automated>cargo test -p app single_service_stress_smoke -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'StressConfig|StressScenario|SingleServiceIntegrated|FullE2eInProcess|HotKey|Burst|DegradedDependency|StressReport|run_single_service_stress|hdrhistogram|sysinfo|throughput_per_second|p50_micros|p95_micros|p99_micros|projection_lag|outbox_lag|reject_rate|cpu_utilization_percent|core_count' crates/app/src/stress.rs` finds all listed patterns.
    - `rg 'CommandGateway|try_submit|CommandEngine|process_one|oneshot' crates/app/src/stress.rs` finds all listed patterns.
    - `rg 'single_service_stress_smoke|full_e2e_in_process_stress_smoke|hot_key_stress_smoke|burst_stress_smoke|degraded_dependency_stress_smoke' crates/app/src/stress.rs` finds all listed patterns.
    - `cargo test -p app single_service_stress_smoke -- --nocapture` exits 0.
  </acceptance_criteria>
  <done>Stress runner produces the TEST-04 report shape and covers TEST-03 integrated/full-E2E/hot-key/burst/degraded-dependency scenarios through bounded in-process command execution.</done>
</task>

<task type="auto">
  <name>Task 07-05-02: Add thin stress bootstrap mode</name>
  <files>crates/app/src/main.rs</files>
  <read_first>
    - crates/app/src/main.rs
    - crates/app/src/stress.rs
    - crates/app/src/observability.rs
    - .planning/phases/07-adapters-observability-stress-and-template-guidance/07-PATTERNS.md
  </read_first>
  <action>Replace the existing shell `main` body with a thin Tokio bootstrap. If the first CLI argument is `stress-smoke`, call `app::stress::run_single_service_stress(app::stress::StressConfig::smoke()).await`, print one JSON line containing the exact keys `commands_submitted`, `commands_succeeded`, `commands_rejected`, `throughput_per_second`, `p50_micros`, `p95_micros`, `p99_micros`, `ingress_depth_max`, `shard_depth_max`, `append_latency_p95_micros`, `projection_lag`, `outbox_lag`, `reject_rate`, `cpu_utilization_percent`, and `core_count`, then return. For any other argument or no argument, print `usage: app stress-smoke`. Keep command runtime, projector, and outbox logic in `stress.rs`, not in `main.rs`.</action>
  <verify>
    <automated>cargo run -p app -- stress-smoke</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'stress-smoke|run_single_service_stress|usage: app stress-smoke' crates/app/src/main.rs` finds all listed patterns.
    - `! rg 'CommandEngine|PostgresEventStore|dispatch_once|catch_up' crates/app/src/main.rs` exits 0.
    - `cargo run -p app -- stress-smoke` exits 0 and prints `p95_micros`.
  </acceptance_criteria>
  <done>App binary exposes a thin stress smoke mode without embedding business/runtime internals in main.rs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI -> stress config | Local user-provided arguments select stress mode and load size. |
| Stress load -> bounded runtime ingress | Synthetic traffic can exhaust queue/ring capacity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-14 | Denial of Service | stress runner ingress | mitigate | Smoke config uses bounded `ingress_capacity` and records rejects instead of awaiting unbounded send capacity. |
| T-07-15 | Repudiation | stress report | mitigate | Report includes submitted/succeeded/rejected counts and reject rate. |
| T-07-16 | Tampering | command success interpretation | mitigate | Report includes projection/outbox lag separately and does not gate command success on projector/outbox catch-up. |
</threat_model>

<verification>
Run `cargo test -p app single_service_stress_smoke -- --nocapture`, `cargo run -p app -- stress-smoke`, and `cargo test --workspace --no-run`.
</verification>

<success_criteria>
Plan succeeds when the stress smoke test and CLI mode produce all TEST-04 report fields while exercising bounded command runtime composition.
</success_criteria>

<output>
After completion, create `.planning/phases/07-adapters-observability-stress-and-template-guidance/07-05-SUMMARY.md`.
</output>
