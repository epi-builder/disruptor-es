---
phase: 07-adapters-observability-stress-and-template-guidance
plan: 06
type: execute
wave: 4
depends_on:
  - 07-01
  - 07-02
  - 07-03
  - 07-04
  - 07-05
files_modified:
  - docs/hot-path-rules.md
  - docs/template-guide.md
  - docs/stress-results.md
autonomous: true
requirements:
  - API-04
  - DOC-01
must_haves:
  truths:
    - "Documentation states event-store source-of-truth, single-owner hot state, bounded gateway, and outbox rules."
    - "Documentation explains WebSocket and gRPC gateways as thin CommandGateway/query clients without shared hot state."
    - "Documentation explains single-service stress results separately from ring-only microbenchmarks."
  artifacts:
    - path: "docs/hot-path-rules.md"
      provides: "Forbidden patterns and hot-path rules"
    - path: "docs/template-guide.md"
      provides: "New domain service creation guide"
    - path: "docs/stress-results.md"
      provides: "Benchmark and stress interpretation guidance"
  key_links:
    - from: "docs/template-guide.md"
      to: "crates/adapter-http/src/commerce.rs"
      via: "CommandGateway gateway guidance"
      pattern: "CommandGateway"
---

<objective>
Document the rules and extension guidance that keep the template architecture correct.

Purpose: The template should be reusable by developers adding new gateways, domains, and stress runs without violating event-sourcing, single-owner, or outbox boundaries.
Output: Hot-path rules, template guide, and stress-results interpretation docs.
</objective>

<execution_context>
@/Users/epikem/.codex/get-shit-done/workflows/execute-plan.md
@/Users/epikem/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-adapters-observability-stress-and-template-guidance/07-RESEARCH.md
@.planning/phases/07-adapters-observability-stress-and-template-guidance/07-PATTERNS.md
@crates/adapter-http/src/commerce.rs
@crates/app/src/stress.rs
@benches/ring_only.rs
@benches/projector_outbox.rs
</context>

<tasks>

<task type="auto">
  <name>Task 07-06-01: Write hot-path and service-boundary rules</name>
  <files>docs/hot-path-rules.md</files>
  <read_first>
    - docs/hot-path-rules.md
    - AGENTS.md
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - crates/es-runtime/src/shard.rs
    - crates/es-store-postgres/src/sql.rs
    - crates/es-outbox/src/dispatcher.rs
  </read_first>
  <action>Create `docs/hot-path-rules.md` with headings exactly `# Hot Path Rules`, `## Source Of Truth`, `## Single Owner Execution`, `## Gateway Boundaries`, `## Outbox Publication`, `## Forbidden Patterns`, and `## Required Checks`. Include the exact sentences `The event store is the source of truth.`, `Disruptor sequences are never durable positions.`, `Adapters must submit through CommandGateway and must not mutate aggregate, projector, or outbox state directly.`, and `External publication must flow through durable outbox rows committed with domain events.` In `## Forbidden Patterns`, list `Arc<Mutex<HashMap<...>>> business state in adapters`, `direct broker publish from command handlers`, `using ring sequence as global position`, `projector catch-up as command success`, and `dynamic SQL built from request strings`.</action>
  <verify>
    <automated>rg "The event store is the source of truth|Disruptor sequences are never durable positions|Adapters must submit through CommandGateway|External publication must flow through durable outbox rows|Arc<Mutex<HashMap|direct broker publish|using ring sequence as global position|projector catch-up as command success|dynamic SQL" docs/hot-path-rules.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg '^# Hot Path Rules|^## Source Of Truth|^## Single Owner Execution|^## Gateway Boundaries|^## Outbox Publication|^## Forbidden Patterns|^## Required Checks' docs/hot-path-rules.md` finds all headings.
    - The automated `rg` command exits 0.
  </acceptance_criteria>
  <done>Hot-path rules document contains grep-verifiable architecture rules and forbidden patterns.</done>
</task>

<task type="auto">
  <name>Task 07-06-02: Write template and gateway extension guide</name>
  <files>docs/template-guide.md</files>
  <read_first>
    - docs/template-guide.md
    - crates/example-commerce/src/lib.rs
    - crates/adapter-http/src/commerce.rs
    - crates/adapter-grpc/src/lib.rs
    - crates/es-runtime/src/gateway.rs
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>Create `docs/template-guide.md` with headings exactly `# Template Guide`, `## Create A New Domain`, `## Add A Command Gateway`, `## HTTP Gateway`, `## WebSocket Gateway`, `## gRPC Gateway`, `## Projection Queries`, `## Outbox And Process Managers`, and `## Verification Commands`. State that new domains implement `Aggregate`, derive `StreamId`, `PartitionKey`, and `ExpectedRevision`, serialize events at storage boundaries, and keep `decide`/`apply` synchronous. For WebSocket and gRPC, include exact guidance: `WebSocket and gRPC gateways should be thin ingress clients of CommandGateway plus read-model query APIs; they must not share hot aggregate state.` Include verification commands `cargo test --workspace --no-run`, `cargo test -p adapter-http -- --nocapture`, `cargo test -p es-store-postgres -- --nocapture`, and `cargo run -p app -- stress-smoke`.</action>
  <verify>
    <automated>rg "WebSocket and gRPC gateways should be thin ingress clients of CommandGateway|Aggregate|StreamId|PartitionKey|ExpectedRevision|cargo test --workspace --no-run|cargo run -p app -- stress-smoke" docs/template-guide.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg '^# Template Guide|^## Create A New Domain|^## Add A Command Gateway|^## HTTP Gateway|^## WebSocket Gateway|^## gRPC Gateway|^## Projection Queries|^## Outbox And Process Managers|^## Verification Commands' docs/template-guide.md` finds all headings.
    - The automated `rg` command exits 0.
  </acceptance_criteria>
  <done>API-04 and DOC-01 gateway extension guidance is present with concrete commands.</done>
</task>

<task type="auto">
  <name>Task 07-06-03: Write stress interpretation guide</name>
  <files>docs/stress-results.md</files>
  <read_first>
    - docs/stress-results.md
    - crates/app/src/stress.rs
    - benches/ring_only.rs
    - benches/domain_only.rs
    - benches/adapter_only.rs
    - benches/storage_only.rs
    - benches/projector_outbox.rs
    - .planning/phases/07-adapters-observability-stress-and-template-guidance/07-VALIDATION.md
  </read_first>
  <action>Create `docs/stress-results.md` with headings exactly `# Stress Results`, `## Ring-Only Benchmarks`, `## Layer Benchmarks`, `## Single-Service Integrated Stress`, `## Required Report Fields`, `## Reading Projection And Outbox Lag`, and `## Do Not Compare`. Include the exact required field names `throughput_per_second`, `p50_micros`, `p95_micros`, `p99_micros`, `ingress_depth_max`, `shard_depth_max`, `append_latency_p95_micros`, `projection_lag`, `outbox_lag`, `reject_rate`, `cpu_utilization_percent`, and `core_count`. Include exact sentence `Ring-only microbenchmarks measure local disruptor handoff cost, not service throughput.` Include exact sentence `Single-service integrated stress includes adapter DTO work, bounded ingress, runtime execution, append behavior, projection lag, and outbox lag in one process.`</action>
  <verify>
    <automated>rg "Ring-only microbenchmarks measure local disruptor handoff cost, not service throughput|Single-service integrated stress includes adapter DTO work|throughput_per_second|p95_micros|projection_lag|outbox_lag|cpu_utilization_percent|core_count" docs/stress-results.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg '^# Stress Results|^## Ring-Only Benchmarks|^## Layer Benchmarks|^## Single-Service Integrated Stress|^## Required Report Fields|^## Reading Projection And Outbox Lag|^## Do Not Compare' docs/stress-results.md` finds all headings.
    - The automated `rg` command exits 0.
    - `rg "event store is the source of truth|CommandGateway|single-owner|outbox|ring-only" docs` exits 0.
  </acceptance_criteria>
  <done>Stress guidance explains layer separation and all report fields with grep-verifiable wording.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation -> future implementers | Incorrect guidance can cause unsafe adapters, direct state mutation, or false performance claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-17 | Tampering | architecture guidance | mitigate | Docs include exact forbidden patterns and required checks that can be grep-verified. |
| T-07-18 | Information Disclosure | gateway guidance | mitigate | Docs direct gateways to DTO/query APIs and do not instruct exposing raw event metadata beyond typed responses. |
| T-07-19 | Repudiation | stress interpretation | mitigate | Docs separate ring-only benchmark meaning from single-service stress fields. |
</threat_model>

<verification>
Run `rg "event store is the source of truth|CommandGateway|single-owner|outbox|ring-only" docs` plus each task's document grep.
</verification>

<success_criteria>
Plan succeeds when all three docs exist, contain exact required headings/phrases, and cover API-04 plus DOC-01.
</success_criteria>

<output>
After completion, create `.planning/phases/07-adapters-observability-stress-and-template-guidance/07-06-SUMMARY.md`.
</output>
