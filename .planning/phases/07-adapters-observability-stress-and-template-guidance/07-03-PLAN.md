---
phase: 07-adapters-observability-stress-and-template-guidance
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/es-store-postgres/tests/phase7_integration.rs
autonomous: true
requirements:
  - TEST-02
must_haves:
  truths:
    - "PostgreSQL integration tests cover append, OCC conflicts, dedupe, snapshots, projector checkpoints, and outbox dispatch through real storage paths."
    - "Tests use the existing PostgreSQL 18 Testcontainers harness and SQLx migrations."
  artifacts:
    - path: "crates/es-store-postgres/tests/phase7_integration.rs"
      provides: "Cross-layer Phase 7 PostgreSQL integration coverage"
  key_links:
    - from: "crates/es-store-postgres/tests/phase7_integration.rs"
      to: "crates/es-store-postgres/tests/common/mod.rs"
      via: "common::start_postgres"
      pattern: "common::start_postgres"
---

<objective>
Add a Phase 7 PostgreSQL integration test suite that samples all durable storage behaviors required by TEST-02.

Purpose: Phase 7 should prove the full storage-backed paths still work together under the real PostgreSQL schema rather than relying on mocks.
Output: One focused integration test file using the existing container harness.
</objective>

<execution_context>
@/Users/epikem/.codex/get-shit-done/workflows/execute-plan.md
@/Users/epikem/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/07-adapters-observability-stress-and-template-guidance/07-RESEARCH.md
@.planning/phases/07-adapters-observability-stress-and-template-guidance/07-PATTERNS.md
@.planning/phases/07-adapters-observability-stress-and-template-guidance/07-VALIDATION.md
@crates/es-store-postgres/tests/common/mod.rs
@crates/es-store-postgres/tests/append_occ.rs
@crates/es-store-postgres/tests/dedupe.rs
@crates/es-store-postgres/tests/snapshots.rs
@crates/es-store-postgres/tests/projections.rs
@crates/es-store-postgres/tests/outbox.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 07-03-01: Add real PostgreSQL Phase 7 integration coverage</name>
  <files>crates/es-store-postgres/tests/phase7_integration.rs</files>
  <read_first>
    - crates/es-store-postgres/tests/phase7_integration.rs
    - crates/es-store-postgres/tests/common/mod.rs
    - crates/es-store-postgres/tests/append_occ.rs
    - crates/es-store-postgres/tests/dedupe.rs
    - crates/es-store-postgres/tests/snapshots.rs
    - crates/es-store-postgres/tests/projections.rs
    - crates/es-store-postgres/tests/outbox.rs
    - crates/es-store-postgres/src/event_store.rs
    - crates/es-store-postgres/src/projection.rs
    - crates/es-store-postgres/src/outbox.rs
  </read_first>
  <behavior>
    - One test appends events, observes stream/global positions, and verifies an OCC conflict on a wrong expected revision.
    - One test repeats the same tenant/idempotency key and receives the original `AppendOutcome::Duplicate`.
    - One test saves a snapshot and loads rehydration from latest snapshot plus subsequent events.
    - One test catches up projections and dispatches an outbox row, proving offsets and outbox state update through PostgreSQL.
  </behavior>
  <action>Create `crates/es-store-postgres/tests/phase7_integration.rs`. Use `mod common;` and `common::start_postgres().await?`; serialize tests with a file-local `static POSTGRES_TEST_LOCK: tokio::sync::Mutex<()> = tokio::sync::Mutex::const_new(())`. Define helper functions for `tenant_id`, `stream_id`, `command_metadata`, `new_event`, `append_request`, `snapshot_request`, and `new_outbox_message` by copying the public-constructor style from existing tests. Add tests named exactly `phase7_append_conflict_and_global_positions`, `phase7_dedupe_returns_original_committed_result`, `phase7_snapshot_rehydration_uses_latest_snapshot`, and `phase7_projector_checkpoint_and_outbox_dispatch`. Use `PostgresEventStore`, `PostgresProjectionStore`, `PostgresOutboxStore`, `dispatch_once`, `InMemoryPublisher`, `WorkerId`, `DispatchBatchLimit`, and `RetryPolicy`. Do not introduce SQLite, mocks, or dynamic SQL.</action>
  <verify>
    <automated>cargo test -p es-store-postgres --test phase7_integration -- --test-threads=1 --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'phase7_append_conflict_and_global_positions|phase7_dedupe_returns_original_committed_result|phase7_snapshot_rehydration_uses_latest_snapshot|phase7_projector_checkpoint_and_outbox_dispatch' crates/es-store-postgres/tests/phase7_integration.rs` finds all four test names.
    - `rg 'common::start_postgres|PostgresEventStore|PostgresProjectionStore|PostgresOutboxStore|dispatch_once|InMemoryPublisher' crates/es-store-postgres/tests/phase7_integration.rs` finds all listed patterns.
    - `cargo test -p es-store-postgres --test phase7_integration -- --test-threads=1 --nocapture` exits 0.
    - `cargo test -p es-store-postgres -- --nocapture` exits 0.
  </acceptance_criteria>
  <done>TEST-02 has explicit real-PostgreSQL coverage across append, conflicts, dedupe, snapshots, projection checkpoints, and outbox dispatch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness -> PostgreSQL | Test code exercises real SQLx queries and transaction behavior. |
| Stored event payloads -> projection/outbox | JSON payloads are decoded and used to update read models and integration rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Tampering | append/OCC/dedupe tests | mitigate | Tests assert wrong expected revisions fail and duplicate idempotency returns original committed result. |
| T-07-09 | Information Disclosure | tenant-scoped reads | mitigate | Helpers must use explicit `TenantId` in append, snapshot, projection, and outbox calls. |
| T-07-10 | Tampering | SQL query paths | mitigate | Tests use existing public repositories and SQLx-bound methods; no string-built SQL is added. |
</threat_model>

<verification>
Run `cargo test -p es-store-postgres --test phase7_integration -- --test-threads=1 --nocapture` and `cargo test -p es-store-postgres -- --nocapture`.
</verification>

<success_criteria>
Plan succeeds when the new test file passes against containerized PostgreSQL 18 and covers every TEST-02 behavior by explicit test name.
</success_criteria>

<output>
After completion, create `.planning/phases/07-adapters-observability-stress-and-template-guidance/07-03-SUMMARY.md`.
</output>
