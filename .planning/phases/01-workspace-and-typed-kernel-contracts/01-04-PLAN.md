---
phase: 01-workspace-and-typed-kernel-contracts
plan: 04
type: execute
wave: 3
depends_on: [01-02, 01-03]
files_modified:
  - crates/example-commerce/Cargo.toml
  - crates/example-commerce/src/lib.rs
  - crates/example-commerce/tests/dependency_boundaries.rs
autonomous: true
requirements: [CORE-01, CORE-02, CORE-03, CORE-04]
must_haves:
  truths:
    - "Developer can inspect a compact typed example aggregate that uses the kernel without storage/runtime/adapters."
    - "Replay determinism is tested through the example aggregate."
    - "Automated checks prove core and kernel do not depend on forbidden runtime/storage/adapter crates."
    - "The full workspace builds and tests after all member crates exist."
  artifacts:
    - path: "crates/example-commerce/src/lib.rs"
      provides: "Minimal commerce-flavored aggregate fixture"
      contains: "impl Aggregate for ProductDraft"
    - path: "crates/example-commerce/tests/dependency_boundaries.rs"
      provides: "Automated dependency boundary tests"
      contains: "FORBIDDEN_DEPENDENCIES"
  key_links:
    - from: "crates/example-commerce/src/lib.rs"
      to: "crates/es-kernel/src/lib.rs"
      via: "Aggregate implementation"
      pattern: "impl Aggregate for ProductDraft"
    - from: "crates/example-commerce/tests/dependency_boundaries.rs"
      to: "Cargo dependency graph"
      via: "cargo tree -p es-core and cargo tree -p es-kernel"
      pattern: "cargo.*tree"
---

<objective>
Prove the Phase 01 contracts with a minimal typed commerce fixture and automated dependency-boundary tests.

Purpose: CORE-01 through CORE-04 are complete only when a developer can build all crates, use the typed kernel from an example domain, and verify lower-level crates remain deterministic and dependency-pure.
Output: Example aggregate crate, replay/property-style tests, and boundary tests that inspect `cargo tree`.
</objective>

<execution_context>
@/Users/epikem/.codex/get-shit-done/workflows/execute-plan.md
@/Users/epikem/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workspace-and-typed-kernel-contracts/01-CONTEXT.md
@.planning/phases/01-workspace-and-typed-kernel-contracts/01-RESEARCH.md
@.planning/phases/01-workspace-and-typed-kernel-contracts/01-PATTERNS.md
@.planning/phases/01-workspace-and-typed-kernel-contracts/01-VALIDATION.md
@Cargo.toml
@crates/es-core/src/lib.rs
@crates/es-kernel/src/lib.rs

<interfaces>
Use the `Aggregate` trait and `Decision` result from `es-kernel`; do not invent a second trait.

Minimal fixture shape:
```rust
pub struct ProductDraft;
pub enum ProductCommand { CreateProduct { stream_id: StreamId, sku: String, name: String } }
pub enum ProductEvent { ProductCreated { sku: String, name: String } }
pub enum ProductReply { Created { stream_id: StreamId } }
pub enum ProductError { EmptySku, EmptyName, AlreadyCreated }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement minimal typed commerce aggregate fixture</name>
  <files>crates/example-commerce/Cargo.toml, crates/example-commerce/src/lib.rs</files>
  <read_first>
    - crates/example-commerce/Cargo.toml
    - crates/example-commerce/src/lib.rs
    - crates/es-core/src/lib.rs
    - crates/es-kernel/src/lib.rs
    - .planning/phases/01-workspace-and-typed-kernel-contracts/01-CONTEXT.md
    - .planning/phases/01-workspace-and-typed-kernel-contracts/01-PATTERNS.md
  </read_first>
  <behavior>
    - Test 1: `ProductDraft::decide` on default state with valid `CreateProduct` returns `Decision { events: vec![ProductCreated], reply: ProductReply::Created { stream_id } }`.
    - Test 2: empty SKU returns `ProductError::EmptySku`; empty name returns `ProductError::EmptyName`.
    - Test 3: after applying `ProductCreated`, a second create command returns `ProductError::AlreadyCreated`.
    - Test 4: replaying generated product-created events with `es_kernel::replay::<ProductDraft>` produces the same state as applying them manually in order.
  </behavior>
  <action>Create `crates/example-commerce/Cargo.toml` with package name `example-commerce`, workspace inheritance, dependencies `es-core = { path = "../es-core" }`, `es-kernel = { path = "../es-kernel" }`, `thiserror.workspace = true`, dev-dependencies `proptest.workspace = true`, and `[lints] workspace = true`. In `src/lib.rs`, implement `ProductState { sku: Option<String>, name: Option<String> }`, `ProductCommand::CreateProduct { stream_id: StreamId, sku: String, name: String }`, `ProductEvent::ProductCreated { sku: String, name: String }`, `ProductReply::Created { stream_id: StreamId }`, `ProductError::{EmptySku, EmptyName, AlreadyCreated}`, and `ProductDraft` implementing `Aggregate`. `stream_id` returns the command stream, `partition_key` uses the same stream string, `expected_revision` returns `ExpectedRevision::NoStream`, `decide` validates only command/state/metadata inputs, and `apply` mutates only the provided `ProductState`. Add tests under `#[cfg(test)] mod aggregate_contract` plus a `proptest!` replay determinism test. Do not implement Phase 4 user/order workflows, inventory behavior, storage, runtime, HTTP, gRPC, async, clocks, random ID generation, or shared global business state.</action>
  <acceptance_criteria>
    - `grep -q 'name = "example-commerce"' crates/example-commerce/Cargo.toml`
    - `grep -q 'impl Aggregate for ProductDraft' crates/example-commerce/src/lib.rs`
    - `grep -q 'ProductCommand::CreateProduct' crates/example-commerce/src/lib.rs`
    - `grep -q 'ExpectedRevision::NoStream' crates/example-commerce/src/lib.rs`
    - `grep -q 'proptest!' crates/example-commerce/src/lib.rs`
    - `! grep -Eq 'async|tokio|sqlx|axum|tonic|serde_json::Value|Arc<Mutex|SystemTime::now|Uuid::now' crates/example-commerce/src/lib.rs crates/example-commerce/Cargo.toml`
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p example-commerce aggregate_contract</automated>
  </verify>
  <done>Example aggregate proves typed commands/events/state/replies/errors, deterministic decide/apply behavior, and replay tests without pulling later commerce workflows forward.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add dependency-boundary integration tests</name>
  <files>crates/example-commerce/tests/dependency_boundaries.rs</files>
  <read_first>
    - crates/example-commerce/tests/dependency_boundaries.rs
    - Cargo.toml
    - crates/es-core/Cargo.toml
    - crates/es-kernel/Cargo.toml
    - .planning/phases/01-workspace-and-typed-kernel-contracts/01-CONTEXT.md
    - .planning/phases/01-workspace-and-typed-kernel-contracts/01-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `cargo tree -p es-core` output does not contain forbidden dependencies.
    - Test 2: `cargo tree -p es-kernel` output does not contain forbidden dependencies.
    - Test 3: all required workspace member crate directories exist.
  </behavior>
  <action>Create `crates/example-commerce/tests/dependency_boundaries.rs` with integration tests that use `std::process::Command` to run `cargo tree -p es-core` and `cargo tree -p es-kernel`. Compute the workspace root from `env!("CARGO_MANIFEST_DIR")` by walking up from `crates/example-commerce` to the workspace root, and invoke Cargo with `.current_dir(workspace_root)` so `cargo tree` runs against the virtual root manifest. Define `const FORBIDDEN_DEPENDENCIES: &[&str] = &["tokio", "sqlx", "axum", "tonic", "async-nats", "rdkafka", "postgres", "disruptor"];` and fail with a clear message if any forbidden string appears in the tree output for `es-core` or `es-kernel`. Add a test `required_workspace_members_exist` that checks directories for `es-core`, `es-kernel`, `es-runtime`, `es-store-postgres`, `es-projection`, `es-outbox`, `example-commerce`, `adapter-http`, `adapter-grpc`, and `app` relative to the computed workspace root. Use only Rust standard library in the integration test.</action>
  <acceptance_criteria>
    - `grep -q 'FORBIDDEN_DEPENDENCIES' crates/example-commerce/tests/dependency_boundaries.rs`
    - `grep -q 'env!("CARGO_MANIFEST_DIR")' crates/example-commerce/tests/dependency_boundaries.rs`
    - `grep -q 'current_dir' crates/example-commerce/tests/dependency_boundaries.rs`
    - `grep -q 'required_workspace_members_exist' crates/example-commerce/tests/dependency_boundaries.rs`
    - `grep -q 'es-store-postgres' crates/example-commerce/tests/dependency_boundaries.rs`
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p example-commerce --test dependency_boundaries</automated>
  </verify>
  <done>Automated tests fail if `es-core` or `es-kernel` acquire forbidden runtime/storage/adapter dependencies or required crate directories are missing.</done>
</task>

<task type="auto">
  <name>Task 3: Run full Phase 01 verification</name>
  <files>Cargo.lock</files>
  <read_first>
    - Cargo.toml
    - rust-toolchain.toml
    - crates/es-core/src/lib.rs
    - crates/es-kernel/src/lib.rs
    - crates/example-commerce/src/lib.rs
    - crates/example-commerce/tests/dependency_boundaries.rs
    - .planning/phases/01-workspace-and-typed-kernel-contracts/01-VALIDATION.md
  </read_first>
  <action>Run a fast smoke sample first with `cargo test -p example-commerce aggregate_contract && cargo test -p example-commerce --test dependency_boundaries`, then run the full Phase 01 validation commands and allow Cargo to create or update `Cargo.lock`. Execute `cargo check --workspace`, `cargo test --workspace`, `cargo tree -p es-core`, and `cargo tree -p es-kernel`. The full workspace command is intentionally the final-only gate because it may exceed the focused feedback target after cold dependency resolution; use the smoke sample when iterating. If `cargo tree` output for core/kernel contains any forbidden dependency from `FORBIDDEN_DEPENDENCIES`, remove the dependency from the offending member manifest instead of weakening the test. Do not remove `Cargo.lock`; this template should commit the lockfile for reproducible service builds.</action>
  <acceptance_criteria>
    - `test -f Cargo.lock`
    - `cargo check --workspace` exits 0
    - `cargo test --workspace` exits 0
    - `! cargo tree -p es-core | grep -E 'tokio|sqlx|axum|tonic|async-nats|rdkafka|postgres|disruptor'`
    - `! cargo tree -p es-kernel | grep -E 'tokio|sqlx|axum|tonic|async-nats|rdkafka|postgres|disruptor'`
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p example-commerce aggregate_contract && cargo test -p example-commerce --test dependency_boundaries && cargo check --workspace && cargo test --workspace && cargo tree -p es-core && cargo tree -p es-kernel</automated>
  </verify>
  <done>Full workspace builds and tests, lockfile is present, and dependency tree checks show `es-core` and `es-kernel` remain free of forbidden dependencies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| example domain author -> kernel contract | Domain code exercises generic kernel traits and could accidentally introduce side effects or erased payloads. |
| integration test -> Cargo process | Boundary tests shell out to Cargo and trust local workspace paths/output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `example-commerce` aggregate | mitigate | Tests assert deterministic typed decisions, typed errors, and replay equivalence with no global mutable state or I/O. |
| T-04-02 | Repudiation | Dependency boundary verification | mitigate | Integration tests produce explicit failures naming forbidden dependencies in `cargo tree` output. |
| T-04-03 | Elevation of Privilege | Core/kernel dependency graph | mitigate | Full phase gate runs `cargo tree -p es-core` and `cargo tree -p es-kernel`; forbidden runtime/storage/adapter crates block completion. |
</threat_model>

<verification>
Run `cargo test -p example-commerce aggregate_contract`, `cargo test -p example-commerce --test dependency_boundaries`, then `cargo check --workspace && cargo test --workspace && cargo tree -p es-core && cargo tree -p es-kernel`.
</verification>

<success_criteria>
All CORE-01 through CORE-04 success truths are observable: all crates exist, typed contracts are used by an example domain, deterministic replay is tested, and dependency boundaries are mechanically enforced.
</success_criteria>

<output>
After completion, create `.planning/phases/01-workspace-and-typed-kernel-contracts/01-04-SUMMARY.md`
</output>
