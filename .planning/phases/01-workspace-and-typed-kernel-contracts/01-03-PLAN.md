---
phase: 01-workspace-and-typed-kernel-contracts
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - crates/es-runtime/Cargo.toml
  - crates/es-runtime/src/lib.rs
  - crates/es-store-postgres/Cargo.toml
  - crates/es-store-postgres/src/lib.rs
  - crates/es-projection/Cargo.toml
  - crates/es-projection/src/lib.rs
  - crates/es-outbox/Cargo.toml
  - crates/es-outbox/src/lib.rs
  - crates/adapter-http/Cargo.toml
  - crates/adapter-http/src/lib.rs
  - crates/adapter-grpc/Cargo.toml
  - crates/adapter-grpc/src/lib.rs
  - crates/app/Cargo.toml
  - crates/app/src/main.rs
autonomous: true
requirements: [CORE-01, CORE-04]
must_haves:
  truths:
    - "Developer can see separate crates for runtime, storage, projection, outbox, adapters, and app composition."
    - "Boundary crates compile as shell crates without implementing later-phase behavior."
    - "Lower-level workspace structure makes future dependency direction visible."
  artifacts:
    - path: "crates/es-runtime/src/lib.rs"
      provides: "Runtime boundary shell"
      contains: "Phase 3"
    - path: "crates/es-store-postgres/src/lib.rs"
      provides: "Durable storage boundary shell"
      contains: "Phase 2"
    - path: "crates/app/src/main.rs"
      provides: "Composition binary shell"
      contains: "fn main()"
  key_links:
    - from: "Cargo.toml"
      to: "crates/*/Cargo.toml"
      via: "workspace member glob"
      pattern: "members = \\[\"crates/\\*\"\\]"
---

<objective>
Create compile-visible crate boundaries for later runtime, storage, projection, outbox, adapter, and app composition work without implementing their behavior in Phase 01.

Purpose: CORE-01 requires separate crates, and CORE-04 requires those future concerns to remain outside deterministic core/kernel logic.
Output: Placeholder member crates with package/lint inheritance and crate docs describing future phase ownership.

Scope note: This plan intentionally modifies 14 files because they are low-risk boundary-shell boilerplate split into two task groups: runtime/storage/projection/outbox library shells, then adapter/app shells. The split keeps ownership clear without creating behavior or dependency risk, so the scope is accepted rather than split further.
</objective>

<execution_context>
@/Users/epikem/.codex/get-shit-done/workflows/execute-plan.md
@/Users/epikem/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workspace-and-typed-kernel-contracts/01-CONTEXT.md
@.planning/phases/01-workspace-and-typed-kernel-contracts/01-RESEARCH.md
@.planning/phases/01-workspace-and-typed-kernel-contracts/01-PATTERNS.md
@Cargo.toml

<interfaces>
Each boundary library crate should use this manifest shape, changing only `name`:
```toml
[package]
name = "es-runtime"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]

[lints]
workspace = true
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create service boundary library crates</name>
  <files>crates/es-runtime/Cargo.toml, crates/es-runtime/src/lib.rs, crates/es-store-postgres/Cargo.toml, crates/es-store-postgres/src/lib.rs, crates/es-projection/Cargo.toml, crates/es-projection/src/lib.rs, crates/es-outbox/Cargo.toml, crates/es-outbox/src/lib.rs</files>
  <read_first>
    - crates/es-runtime/Cargo.toml
    - crates/es-runtime/src/lib.rs
    - crates/es-store-postgres/Cargo.toml
    - crates/es-store-postgres/src/lib.rs
    - crates/es-projection/Cargo.toml
    - crates/es-projection/src/lib.rs
    - crates/es-outbox/Cargo.toml
    - crates/es-outbox/src/lib.rs
    - Cargo.toml
    - .planning/phases/01-workspace-and-typed-kernel-contracts/01-CONTEXT.md
    - .planning/phases/01-workspace-and-typed-kernel-contracts/01-PATTERNS.md
  </read_first>
  <action>Create `es-runtime`, `es-store-postgres`, `es-projection`, and `es-outbox` crates per D-04 and D-06. Each `Cargo.toml` must inherit `version`, `edition`, `rust-version`, `license`, and `[lints] workspace = true`, with an empty `[dependencies]` table in Phase 1. Each `src/lib.rs` must contain crate-level docs and one public constant `pub const PHASE_BOUNDARY: &str = "...";` describing future ownership: `es-runtime` owns Phase 3 local command routing/shards/disruptor execution, `es-store-postgres` owns Phase 2 durable event append, `es-projection` owns Phase 5 projector/read-model catch-up, and `es-outbox` owns Phase 6 outbox dispatch/process-manager integration. Do not add disruptor, Tokio, SQLx, PostgreSQL types, broker clients, runtime code, storage code, projector code, or outbox dispatch code.</action>
  <acceptance_criteria>
    - `grep -q 'name = "es-runtime"' crates/es-runtime/Cargo.toml`
    - `grep -q 'name = "es-store-postgres"' crates/es-store-postgres/Cargo.toml`
    - `grep -q 'name = "es-projection"' crates/es-projection/Cargo.toml`
    - `grep -q 'name = "es-outbox"' crates/es-outbox/Cargo.toml`
    - `grep -R 'PHASE_BOUNDARY' crates/es-runtime/src crates/es-store-postgres/src crates/es-projection/src crates/es-outbox/src`
    - `for manifest in crates/es-runtime/Cargo.toml crates/es-store-postgres/Cargo.toml crates/es-projection/Cargo.toml crates/es-outbox/Cargo.toml; do awk '/^\[dependencies\]/{in_deps=1; next} /^\[/{in_deps=0} in_deps && $0 !~ /^[[:space:]]*(#.*)?$/ { print FILENAME ":" $0; found=1 } END { exit found }' "$manifest" || exit 1; done`
  </acceptance_criteria>
  <verify>
    <automated>test -f crates/es-runtime/src/lib.rs && test -f crates/es-store-postgres/src/lib.rs && test -f crates/es-projection/src/lib.rs && test -f crates/es-outbox/src/lib.rs && grep -R 'PHASE_BOUNDARY' crates/es-runtime/src crates/es-store-postgres/src crates/es-projection/src crates/es-outbox/src && for manifest in crates/es-runtime/Cargo.toml crates/es-store-postgres/Cargo.toml crates/es-projection/Cargo.toml crates/es-outbox/Cargo.toml; do awk '/^\[dependencies\]/{in_deps=1; next} /^\[/{in_deps=0} in_deps && $0 !~ /^[[:space:]]*(#.*)?$/ { print FILENAME ":" $0; found=1 } END { exit found }' "$manifest" || exit 1; done</automated>
  </verify>
  <done>Runtime, storage, projection, and outbox crates exist as compile-visible boundary shells and do not implement later-phase behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Create adapter and composition crate shells</name>
  <files>crates/adapter-http/Cargo.toml, crates/adapter-http/src/lib.rs, crates/adapter-grpc/Cargo.toml, crates/adapter-grpc/src/lib.rs, crates/app/Cargo.toml, crates/app/src/main.rs</files>
  <read_first>
    - crates/adapter-http/Cargo.toml
    - crates/adapter-http/src/lib.rs
    - crates/adapter-grpc/Cargo.toml
    - crates/adapter-grpc/src/lib.rs
    - crates/app/Cargo.toml
    - crates/app/src/main.rs
    - Cargo.toml
    - .planning/phases/01-workspace-and-typed-kernel-contracts/01-CONTEXT.md
    - .planning/phases/01-workspace-and-typed-kernel-contracts/01-PATTERNS.md
  </read_first>
  <action>Create `adapter-http`, `adapter-grpc`, and `app` crates per D-04 and D-06. `adapter-http` and `adapter-grpc` are library crates with inherited package/lint settings, empty dependencies, crate docs, and `pub const PHASE_BOUNDARY: &str` values stating they will decode HTTP and gRPC requests in later phases without owning aggregate state. `app` is a binary crate with inherited package/lint settings, empty dependencies, and `src/main.rs` containing only `fn main() {}` plus a short comment that Phase 01 intentionally limits app composition to crate visibility. Do not add Axum, Tonic, Tokio, storage, runtime, projection, or outbox dependencies.</action>
  <acceptance_criteria>
    - `grep -q 'name = "adapter-http"' crates/adapter-http/Cargo.toml`
    - `grep -q 'name = "adapter-grpc"' crates/adapter-grpc/Cargo.toml`
    - `grep -q 'name = "app"' crates/app/Cargo.toml`
    - `grep -q 'fn main()' crates/app/src/main.rs`
    - `grep -R 'PHASE_BOUNDARY' crates/adapter-http/src crates/adapter-grpc/src`
    - `! grep -RE 'tokio|sqlx|axum|tonic|disruptor|async-nats|rdkafka' crates/adapter-http crates/adapter-grpc crates/app`
  </acceptance_criteria>
  <verify>
    <automated>test -f crates/adapter-http/src/lib.rs && test -f crates/adapter-grpc/src/lib.rs && test -f crates/app/src/main.rs && grep -E 'name = "adapter-http"|name = "adapter-grpc"|name = "app"' crates/adapter-http/Cargo.toml crates/adapter-grpc/Cargo.toml crates/app/Cargo.toml</automated>
  </verify>
  <done>Adapter and app composition crates exist as boundary shells with no runtime, network, storage, or broker implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future adapters -> domain/runtime | Placeholder adapter crates mark future protocol boundaries but must not mutate business state. |
| future storage/outbox crates -> deterministic kernel | Placeholder storage/outbox crates must not be pulled into core/kernel dependencies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Boundary crates | mitigate | Keep boundary shells dependency-empty and document later-phase ownership only; no mutation or runtime behavior in Phase 1. |
| T-03-02 | Elevation of Privilege | Adapter boundary shells | mitigate | Do not add Axum/Tonic/Tokio or network surfaces in Phase 1; later ASVS controls apply when endpoints exist. |
| T-03-03 | Tampering | Workspace dependency graph | mitigate | Member manifests inherit lints and avoid dependencies, making backward leakage into core/kernel visible to boundary tests. |
</threat_model>

<verification>
Run file existence checks, grep boundary constants, and verify forbidden dependency names do not appear in boundary crate manifests or source.
</verification>

<success_criteria>
CORE-01 is satisfied for non-core crate topology, and CORE-04 remains protected because runtime/storage/projection/outbox/adapter concerns are visible but behavior-free in Phase 1.
</success_criteria>

<output>
After completion, create `.planning/phases/01-workspace-and-typed-kernel-contracts/01-03-SUMMARY.md`
</output>
