---
phase: 12-external-process-http-e2e-stress-and-benchmark-closure
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/app/Cargo.toml
  - crates/app/tests/support/mod.rs
  - crates/app/tests/support/http_process.rs
  - crates/app/tests/serve_smoke.rs
  - crates/app/tests/external_process_http.rs
autonomous: true
requirements:
  - API-01
  - API-03
must_haves:
  truths:
    - "Canonical Phase 12 E2E checks launch the real `app serve` process and submit HTTP requests from outside that process."
    - "The external-process tests assert durable HTTP response metadata such as correlation ID, stream revision, and global positions through the real binary boundary."
    - "Readiness probing, child-process lifecycle, and failure-log capture are reusable support code rather than duplicated test-local glue."
  artifacts:
    - path: "crates/app/tests/support/http_process.rs"
      provides: "Reusable external-process app harness"
      contains: "wait_for_health"
    - path: "crates/app/tests/external_process_http.rs"
      provides: "Canonical external-process HTTP E2E scenarios"
      contains: "external_process_http_"
    - path: "crates/app/tests/serve_smoke.rs"
      provides: "Narrow readiness smoke reusing shared support"
      contains: "serve_smoke"
  key_links:
    - from: "crates/app/tests/external_process_http.rs"
      to: "crates/app/src/serve.rs"
      via: "spawn real `app serve` child process"
      pattern: "serve"
    - from: "crates/app/tests/external_process_http.rs"
      to: "crates/adapter-http/tests/commerce_api.rs"
      via: "mirrored success/error metadata assertions"
      pattern: "correlation_id|stream_id|global_positions"
---

<objective>
Build the reusable external-process HTTP harness and canonical request scenarios that prove API behavior through the real `app serve` binary path.

Purpose: Phase 11 already proved the runnable service path exists, but Phase 12 needs a repeatable E2E harness and response-contract checks that run outside the process boundary. The goal is not “one more smoke test”; it is a reusable external-process proof layer the later stress and benchmark work can build on.

Output: shared process-test support, a slimmed `serve_smoke`, and new external-process HTTP E2E tests.
</objective>

<execution_context>
@/Users/epikem/.codex/get-shit-done/workflows/execute-plan.md
@/Users/epikem/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/12-external-process-http-e2e-stress-and-benchmark-closure/12-CONTEXT.md
@.planning/phases/12-external-process-http-e2e-stress-and-benchmark-closure/12-RESEARCH.md
@.planning/phases/12-external-process-http-e2e-stress-and-benchmark-closure/12-VALIDATION.md
@.planning/phases/12-external-process-http-e2e-stress-and-benchmark-closure/12-PATTERNS.md
@crates/app/Cargo.toml
@Cargo.toml
@crates/app/src/main.rs
@crates/app/src/serve.rs
@crates/app/tests/serve_smoke.rs
@crates/adapter-http/tests/commerce_api.rs
@docs/template-guide.md
@docs/stress-results.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 12-01-01: Extract reusable external-process app-process support</name>
  <files>Cargo.toml, crates/app/Cargo.toml, crates/app/tests/support/mod.rs, crates/app/tests/support/http_process.rs, crates/app/tests/serve_smoke.rs</files>
  <read_first>
    - crates/app/tests/serve_smoke.rs
    - crates/app/src/serve.rs
    - crates/app/src/main.rs
    - crates/app/Cargo.toml
  </read_first>
  <behavior>
    - Child-process startup, readiness polling, request helpers, and log capture live in reusable test support.
    - `serve_smoke.rs` becomes a narrow smoke test that imports the shared helper instead of owning all the process glue itself.
    - Any added client dependency stays scoped and justified by repeated external-process use rather than convenience-only churn.
  </behavior>
  <action>
    Create a `crates/app/tests/support/` module (for example `mod.rs` plus `http_process.rs`) that generalizes the current `serve_smoke` helpers:
    - reserve an ephemeral listen address
    - resolve the app binary path
    - spawn `app serve` with explicit env vars
    - wait for `/healthz`
    - send HTTP requests
    - collect child stdout/stderr on failure
    - shut the child down cleanly

    Refactor `crates/app/tests/serve_smoke.rs` to use this shared support and keep it focused on readiness + one documented happy-path request. If a direct HTTP client dependency is introduced, wire it in the smallest scope needed for external-process tests.
  </action>
  <verify>
    <automated>cargo test -p app serve_smoke -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `test -f crates/app/tests/support/http_process.rs` exits 0.
    - `rg -n 'wait_for_health|spawn_app|child_logs|http_request' crates/app/tests/support/http_process.rs` finds the shared helpers.
    - `rg -n 'support|http_process' crates/app/tests/serve_smoke.rs` shows the smoke test now imports the shared support.
    - `cargo test -p app serve_smoke -- --nocapture` exits 0.
  </acceptance_criteria>
  <done>The repository has one reusable external-process app harness instead of duplicated startup logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 12-01-02: Add canonical external-process success-path and metadata-contract scenarios</name>
  <files>crates/app/tests/external_process_http.rs</files>
  <read_first>
    - crates/app/tests/support/http_process.rs
    - crates/adapter-http/tests/commerce_api.rs
    - crates/app/tests/serve_smoke.rs
    - .planning/phases/12-external-process-http-e2e-stress-and-benchmark-closure/12-RESEARCH.md
  </read_first>
  <behavior>
    - The new tests launch the real `app serve` binary and submit HTTP requests from a separate client/process context.
    - Success-path assertions prove `API-01` and `API-03` through the real boundary, including correlation ID, stream ID/revision, global positions, and typed reply payloads.
    - The scenarios use canonical request fixtures that later stress/benchmark code can reuse.
  </behavior>
  <action>
    Add `crates/app/tests/external_process_http.rs` with one or more tests whose names share an `external_process_http_` prefix. Mirror the strongest existing response-contract checks from `crates/adapter-http/tests/commerce_api.rs`, but route them through the real binary/process path.

    At minimum include:
    - a success-path scenario that posts a valid order command over HTTP and asserts the durable response fields
    - a metadata-contract scenario that specifically checks `correlation_id`, stream metadata, and typed reply payload structure

    Keep the request payloads/factories reusable so Plan 12-02 can build stress/benchmark traffic from the same canonical fixtures.
  </action>
  <verify>
    <automated>cargo test -p app external_process_http_success_path -- --nocapture && cargo test -p app external_process_http_metadata_contract -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `test -f crates/app/tests/external_process_http.rs` exits 0.
    - `rg -n 'external_process_http_success_path|external_process_http_metadata_contract' crates/app/tests/external_process_http.rs` finds the canonical scenarios.
    - `rg -n 'correlation_id|stream_id|global_positions|reply' crates/app/tests/external_process_http.rs` finds durable metadata assertions.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>The repo has real-process E2E tests that prove the HTTP success path and durable response metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 12-01-03: Prove external-process error and boundary behavior, not only the happy path</name>
  <files>crates/app/tests/external_process_http.rs</files>
  <read_first>
    - crates/app/tests/external_process_http.rs
    - crates/adapter-http/tests/commerce_api.rs
    - .planning/phases/12-external-process-http-e2e-stress-and-benchmark-closure/12-VALIDATION.md
  </read_first>
  <behavior>
    - External-process E2E coverage includes at least one non-happy-path response contract.
    - Error-path assertions stay grounded in existing adapter behavior (for example overload or malformed-request mapping), not invented semantics.
    - The tests still drive the official HTTP surface and never bypass into `CommandGateway` directly.
  </behavior>
  <action>
    Extend `crates/app/tests/external_process_http.rs` with one or more error/boundary scenarios that are realistic for the current codebase. Prefer cases already proven in adapter tests and likely to remain stable through the real service path, such as:
    - request validation / malformed body handling
    - overload behavior under intentionally constrained ingress
    - a conflict or duplicate-replay response if it can be driven deterministically through the external-process harness

    The point is to show that the real process boundary preserves error mapping semantics, not only success payload shape.
  </action>
  <verify>
    <automated>cargo test -p app external_process_http_error_contracts -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'external_process_http_error_contracts|StatusCode|overload|conflict|400|409|429' crates/app/tests/external_process_http.rs` finds at least one non-happy-path contract scenario.
    - The test file still launches the real binary rather than using `router().oneshot(...)` directly.
    - `cargo test -p app external_process_http -- --nocapture` exits 0.
  </acceptance_criteria>
  <done>Phase 12 E2E coverage proves that real-process HTTP behavior includes durable error/contract mapping, not only a single success path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> real `app serve` child process | Process startup, health, request I/O, and shutdown must stay deterministic enough for CI evidence. |
| external HTTP client -> adapter/runtime boundary | Phase 12 must prove behavior through the official HTTP surface, not in-memory shortcuts. |
| adapter contract tests -> archive-facing E2E claims | Existing adapter tests remain useful, but they are not alone sufficient evidence for external-process closure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Reliability / Denial of Service | child-process harness | mitigate | Centralize startup/readiness/logging/shutdown helpers so external-process tests do not flake independently. |
| T-12-02 | Tampering / Repudiation | API metadata evidence | mitigate | Mirror durable response-contract assertions from adapter tests through the real binary boundary. |
| T-12-03 | Spoofing | fake E2E proof | mitigate | Ensure every Phase 12 test launches `app serve` and submits real HTTP requests instead of invoking `router().oneshot(...)` as the primary evidence. |
</threat_model>

<verification>
Run after all tasks:

```bash
cargo test -p app serve_smoke -- --nocapture
cargo test -p app external_process_http -- --nocapture
rg -n "wait_for_health|spawn_app|child_logs|external_process_http_" crates/app/tests/support/http_process.rs crates/app/tests/serve_smoke.rs crates/app/tests/external_process_http.rs
```

Optional stronger confirmation before phase sign-off:

```bash
cargo test --workspace
```
</verification>

<success_criteria>
- The external-process app harness exists as reusable support code.
- `serve_smoke` reuses that support instead of owning a bespoke process harness.
- The repo has real-process success-path and metadata-contract E2E tests.
- At least one non-happy-path response contract is proven through the real HTTP service path.
</success_criteria>

<output>
After completion, create `.planning/phases/12-external-process-http-e2e-stress-and-benchmark-closure/12-01-SUMMARY.md`.
</output>
