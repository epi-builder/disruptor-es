---
phase: 12-external-process-http-e2e-stress-and-benchmark-closure
plan: 2
type: execute
wave: 1
depends_on:
  - 12-01
files_modified:
  - Cargo.toml
  - crates/app/Cargo.toml
  - crates/app/src/lib.rs
  - crates/app/src/main.rs
  - crates/app/src/stress.rs
  - crates/app/src/http_stress.rs
  - crates/app/tests/external_process_http.rs
  - benches/external_process_http.rs
  - docs/stress-results.md
  - docs/template-guide.md
autonomous: true
requirements:
  - TEST-03
  - TEST-04
  - OBS-02
must_haves:
  truths:
    - "No archive-facing workload label implies that the in-process stress lane is the representative full external-process E2E path."
    - "The repository has an explicit external-process HTTP stress/benchmark lane that launches `app serve` and reports the required stress fields."
    - "Documentation clearly distinguishes ring-only, in-process integrated, and external-process HTTP measurements."
  artifacts:
    - path: "crates/app/src/http_stress.rs"
      provides: "External-process HTTP stress runner/reporting"
      contains: "throughput_per_second"
    - path: "benches/external_process_http.rs"
      provides: "Dedicated external-process HTTP benchmark lane"
      contains: "criterion_group!"
    - path: "docs/stress-results.md"
      provides: "Updated report interpretation guidance"
      contains: "external-process"
  key_links:
    - from: "crates/app/src/http_stress.rs"
      to: "crates/app/src/serve.rs"
      via: "spawn/use the official service process"
      pattern: "serve"
    - from: "benches/external_process_http.rs"
      to: "crates/app/src/http_stress.rs"
      via: "benchmark harness delegates to reusable external-process runner"
      pattern: "http_stress"
    - from: "docs/stress-results.md"
      to: "crates/app/src/stress.rs"
      via: "explicit in-process vs external-process naming"
      pattern: "in-process|external-process"
---

<objective>
Replace the misleading in-process “full E2E” label, add the external-process HTTP stress/benchmark lane, and document how the resulting numbers should be interpreted.

Purpose: Plan 12-01 proves the real HTTP path functionally. Plan 12-02 closes the audit debt that still exists when the repo has only in-process stress labels and no dedicated external-process HTTP measurement lane.

Output: renamed/demoted in-process scenario, reusable external-process stress runner, dedicated benchmark lane, and refreshed operator/reporting docs.
</objective>

<execution_context>
@/Users/epikem/.codex/get-shit-done/workflows/execute-plan.md
@/Users/epikem/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/12-external-process-http-e2e-stress-and-benchmark-closure/12-CONTEXT.md
@.planning/phases/12-external-process-http-e2e-stress-and-benchmark-closure/12-RESEARCH.md
@.planning/phases/12-external-process-http-e2e-stress-and-benchmark-closure/12-VALIDATION.md
@.planning/phases/12-external-process-http-e2e-stress-and-benchmark-closure/12-PATTERNS.md
@crates/app/src/main.rs
@crates/app/src/stress.rs
@crates/app/src/serve.rs
@crates/app/tests/external_process_http.rs
@Cargo.toml
@crates/app/Cargo.toml
@docs/stress-results.md
@docs/template-guide.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 12-02-01: Rename or demote the misleading in-process “full E2E” lane</name>
  <files>crates/app/src/stress.rs, docs/stress-results.md, docs/template-guide.md</files>
  <read_first>
    - crates/app/src/stress.rs
    - docs/stress-results.md
    - docs/template-guide.md
    - .planning/v1.0-MILESTONE-AUDIT.md
  </read_first>
  <behavior>
    - The in-process lane remains available for diagnostics, but its name explicitly states that it is in-process.
    - No primary code/doc label remains as `FullE2eInProcess` or `full-e2e` unless clearly retained only as historical context in prose.
    - The renamed lane still preserves the existing in-process metrics/report semantics.
  </behavior>
  <action>
    Update `crates/app/src/stress.rs` so the old `FullE2eInProcess` scenario is renamed or otherwise demoted to an explicit in-process-only label. The exact replacement name should be unambiguous about the boundary (for example, an `InProcess...` name used consistently in enums, scenario strings, test names, and any JSON labels).

    Then update `docs/stress-results.md` and `docs/template-guide.md` so they describe the lane as in-process integrated coverage rather than representative full external HTTP E2E.
  </action>
  <verify>
    <automated>cargo test -p app stress_smoke -- --nocapture && rg -n "FullE2eInProcess|full-e2e" crates/app/src/stress.rs docs/stress-results.md docs/template-guide.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'FullE2eInProcess|full-e2e' crates/app/src/stress.rs docs/stress-results.md docs/template-guide.md` returns no archive-facing matches.
    - `rg -n 'in-process' crates/app/src/stress.rs docs/stress-results.md docs/template-guide.md` shows the replacement naming.
    - `cargo test -p app stress_smoke -- --nocapture` exits 0.
  </acceptance_criteria>
  <done>The repository no longer mislabels an in-process lane as representative full E2E.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 12-02-02: Add reusable external-process HTTP stress reporting</name>
  <files>Cargo.toml, crates/app/Cargo.toml, crates/app/src/lib.rs, crates/app/src/main.rs, crates/app/src/http_stress.rs, crates/app/tests/external_process_http.rs</files>
  <read_first>
    - crates/app/src/stress.rs
    - crates/app/src/main.rs
    - crates/app/src/serve.rs
    - crates/app/tests/support/http_process.rs
    - crates/app/tests/external_process_http.rs
    - docs/stress-results.md
  </read_first>
  <behavior>
    - The new workload launches `app serve` as an external process and drives it over HTTP.
    - Report output preserves the repo’s required stress fields: throughput, p50/p95/p99, queue depth, append latency, projection lag, outbox lag, reject rate, CPU, and core count.
    - The implementation remains reusable from both targeted tests and any benchmark/CLI entrypoint.
  </behavior>
  <action>
    Create a reusable `crates/app/src/http_stress.rs` module that owns external-process HTTP stress execution and machine-readable reporting. Use the shared process harness/factories from Plan 12-01 where practical, but keep the long-running workload logic in library code rather than test-only modules.

    If a new app entrypoint is the cleanest developer interface, expose it through a thin `main.rs` dispatch branch (for example `app http-stress`) and export the module from `crates/app/src/lib.rs`. The runner should drive canonical HTTP requests against the spawned `app serve` process and emit the required report fields.
  </action>
  <verify>
    <automated>cargo test -p app external_process_http_stress_smoke -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `test -f crates/app/src/http_stress.rs` exits 0.
    - `rg -n 'throughput_per_second|p50|p95|p99|projection_lag|outbox_lag|reject_rate|cpu_utilization_percent|core_count' crates/app/src/http_stress.rs` finds the required report fields.
    - `rg -n 'http-stress|external-process' crates/app/src/main.rs crates/app/src/lib.rs` shows the reusable entrypoint wiring if a CLI surface is added.
    - `cargo test -p app external_process_http_stress_smoke -- --nocapture` exits 0.
  </acceptance_criteria>
  <done>The repo has a reusable external-process HTTP stress runner with report-field parity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 12-02-03: Register a dedicated external-process HTTP benchmark lane</name>
  <files>Cargo.toml, benches/external_process_http.rs, crates/app/src/http_stress.rs</files>
  <read_first>
    - Cargo.toml
    - benches/ring_only.rs
    - benches/storage_only.rs
    - crates/app/src/http_stress.rs
    - docs/stress-results.md
  </read_first>
  <behavior>
    - The benchmark lane is registered separately from the existing microbench and projector/outbox benches.
    - The bench makes it impossible to confuse external-process numbers with ring-only or in-process integrated numbers.
    - The bench reuses the external-process runner instead of forking a second measurement implementation.
  </behavior>
  <action>
    Add `benches/external_process_http.rs` and register it in `Cargo.toml` as its own `[[bench]]` entry. The bench should delegate to the reusable external-process HTTP runner or the smallest possible wrapper around it, and its module/file comments should explicitly say this lane measures real HTTP client/service-process overhead.
  </action>
  <verify>
    <automated>cargo bench --bench external_process_http -- --sample-size 10</automated>
  </verify>
  <acceptance_criteria>
    - `test -f benches/external_process_http.rs` exits 0.
    - `rg -n 'external_process_http' Cargo.toml benches/external_process_http.rs` finds the dedicated bench registration.
    - `rg -n 'criterion_group!|criterion_main!' benches/external_process_http.rs` finds the standard Criterion structure.
    - `cargo bench --bench external_process_http -- --sample-size 10` exits 0.
  </acceptance_criteria>
  <done>The repository has an explicit benchmark lane for external-process HTTP baselines.</done>
</task>

<task type="auto">
  <name>Task 12-02-04: Update docs so report consumers cannot confuse workload layers</name>
  <files>docs/stress-results.md, docs/template-guide.md</files>
  <read_first>
    - docs/stress-results.md
    - docs/template-guide.md
    - .planning/phases/12-external-process-http-e2e-stress-and-benchmark-closure/12-RESEARCH.md
    - .planning/v1.0-MILESTONE-AUDIT.md
  </read_first>
  <behavior>
    - Docs explain how to run the external-process harness/bench and how it differs from `app serve` smoke, in-process integrated stress, and component microbenches.
    - Report interpretation guidance preserves the repo’s existing “Do Not Compare” discipline.
    - The docs mention required report fields for the external-process lane rather than treating them as ad hoc output.
  </behavior>
  <action>
    Update `docs/stress-results.md` to add the external-process HTTP lane explicitly and to state which report fields and environment metadata must accompany those runs. Update `docs/template-guide.md` with the operator-facing steps for running the new external-process workload(s), including the distinction between:
    - `app serve` as the service process
    - narrow readiness smoke
    - in-process integrated stress
    - external-process HTTP stress/bench runs

    Keep the existing anti-comparison guidance intact and extend it where needed.
  </action>
  <verify>
    <automated>rg -n "external-process|app serve|throughput_per_second|p95|projection_lag|outbox_lag|reject_rate|cpu_utilization_percent" docs/stress-results.md docs/template-guide.md crates/app/src</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'external-process' docs/stress-results.md docs/template-guide.md` finds explicit operator/reporting guidance.
    - `rg -n 'Do Not Compare|ring-only|single-service integrated|external-process' docs/stress-results.md` preserves the layer-separation rules.
    - `rg -n 'throughput_per_second|p95|projection_lag|outbox_lag|reject_rate|cpu_utilization_percent' docs/stress-results.md docs/template-guide.md` finds the required report-field guidance.
  </acceptance_criteria>
  <done>Operators and reviewers can run and interpret the external-process lane without confusing it with existing workload layers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| in-process stress evidence -> archive-facing full-E2E claims | Mislabeling here is the exact evidence defect Phase 12 must close. |
| external-process stress runner -> benchmark lane | The bench should call a shared runner instead of diverging into a second implementation. |
| docs/report labels -> future milestone audits | Ambiguous labels will recreate the same blocker later even if the code exists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Repudiation | benchmark/report labels | mitigate | Remove or demote `FullE2eInProcess` naming and require explicit in-process/external-process labels everywhere. |
| T-12-05 | Reliability | external-process stress runner | mitigate | Build one reusable library runner and verify it with targeted smoke tests before bench registration. |
| T-12-06 | Tampering / Confusion | documentation layer boundaries | mitigate | Update canonical docs so report consumers cannot compare unlike workload layers or mistake one for another. |
</threat_model>

<verification>
Run after all tasks:

```bash
cargo test -p app stress_smoke -- --nocapture
cargo test -p app external_process_http_stress_smoke -- --nocapture
cargo bench --bench external_process_http -- --sample-size 10
rg -n "FullE2eInProcess|full-e2e|external-process|throughput_per_second|projection_lag|outbox_lag|reject_rate|cpu_utilization_percent" crates/app/src docs/stress-results.md docs/template-guide.md benches/external_process_http.rs
```

Optional stronger confirmation before phase sign-off:

```bash
cargo test --workspace
```
</verification>

<success_criteria>
- The in-process lane is explicitly labeled as in-process only.
- The repo has a reusable external-process HTTP stress runner.
- The repo has a dedicated external-process HTTP benchmark lane.
- Docs clearly distinguish external-process, in-process integrated, and microbenchmark measurements.
</success_criteria>

<output>
After completion, create `.planning/phases/12-external-process-http-e2e-stress-and-benchmark-closure/12-02-SUMMARY.md`.
</output>
