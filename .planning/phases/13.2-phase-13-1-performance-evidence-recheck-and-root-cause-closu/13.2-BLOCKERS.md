Phase 14 archive blocker: none from Phase 13.2.

## Closed archive claims

1. Verification gap 1 closed: `live-http-unique.json`, `live-http-shard-1.json`, and `live-http-shard-8.json` are self-describing through `shard_count`, `ingress_capacity`, and `ring_size`, and each file exposes explicit append-latency semantics through `append_latency_observed`, `append_latency_sample_count_delta`, and `append_latency_unavailable_reason`.
2. Verification gap 2 closed: archive-facing docs now keep the ceiling at `durable command path (append latency unavailable in live HTTP artifacts)` because the regenerated success-throughput files report `append_latency_observed = false` with `append_latency_p95_micros = null`, while still proving scrape health with `metrics_scrape_successes = 121`.
3. Verification gap 3 closed: `live-http-single-hot-key-diagnostic.json` now records `failure_kind_counts = {"domain": 13180}` plus bounded `sample_failures`, so the repeated-stream diagnostic has explicit failure taxonomy instead of an opaque total-failure count.
