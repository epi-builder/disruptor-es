---
phase: 13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu
plan: 4
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/app/src/stress.rs
  - crates/app/src/http_stress.rs
  - crates/app/src/main.rs
  - scripts/compare-stress-layers.sh
  - .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-BASELINE-REGEN.md
  - target/phase-13.1/layer-comparison/live-http-unique.json
  - target/phase-13.1/layer-comparison/live-http-shard-1.json
  - target/phase-13.1/layer-comparison/live-http-shard-8.json
  - target/phase-13.1/layer-comparison/live-http-single-hot-key-diagnostic.json
autonomous: true
requirements:
  - RUNTIME-01
  - RUNTIME-02
  - RUNTIME-05
  - TEST-03
  - TEST-04
  - OBS-02
gap_closure: true
must_haves:
  truths:
    - "Live HTTP JSON distinguishes observed append latency from unavailable append latency instead of serializing an ambiguous `0`."
    - "Shard comparison artifacts are self-describing from their own JSON contents, including shard count, ingress capacity, and ring size."
    - "The hot-key diagnostic artifact records failure taxonomy and sample failures, or the regeneration ledger captures the exact reason it still cannot."
  artifacts:
    - path: "crates/app/src/http_stress.rs"
      provides: "explicit append-latency availability semantics and failure taxonomy capture for external HTTP stress"
      contains: "append_latency_observed"
    - path: "crates/app/src/main.rs"
      provides: "JSON output containing append-latency availability, lane metadata, and diagnostic failure fields"
      contains: "append_latency_sample_count_delta"
    - path: "target/phase-13.1/layer-comparison/live-http-unique.json"
      provides: "baseline live HTTP evidence with self-describing lane metadata"
      contains: "\"shard_count\""
    - path: "target/phase-13.1/layer-comparison/live-http-single-hot-key-diagnostic.json"
      provides: "diagnostic artifact with failure taxonomy and sample errors"
      contains: "\"failure_kind_counts\""
  key_links:
    - from: "crates/app/src/http_stress.rs"
      to: "crates/app/src/main.rs"
      via: "StressReport field population and JSON serialization"
      pattern: "append_latency_observed|failure_kind_counts|sample_failures|shard_count|ingress_capacity|ring_size"
    - from: "scripts/compare-stress-layers.sh"
      to: "target/phase-13.1/layer-comparison/live-http-shard-1.json"
      via: "baseline rerun command"
      pattern: "live-http-shard-1.json"
    - from: ".planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-BASELINE-REGEN.md"
      to: "target/phase-13.1/layer-comparison/live-http-single-hot-key-diagnostic.json"
      via: "artifact and field inventory"
      pattern: "failure_kind_counts|sample_failures"
---

<objective>
Repair the external HTTP stress report contract so regenerated Phase 13.1 artifacts can prove what was observed, what was unavailable, and why the hot-key lane failed.

Purpose: The current archive bundle still hides missing append-latency evidence behind `append_latency_p95_micros: 0`, omits lane configuration metadata from shard comparison files, and records total hot-key failure without a diagnosable cause. This plan fixes the report schema first, then regenerates the affected artifacts on the real durable path.
Output: updated report/data contract code, regenerated live HTTP JSON artifacts, and a refreshed baseline ledger that records the new fields explicitly.
</objective>

<execution_context>
@/Users/epikem/.codex/get-shit-done/workflows/execute-plan.md
@/Users/epikem/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-RESEARCH.md
@.planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-VERIFICATION.md
@.planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-REVIEW.md
@.planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-BASELINE-REGEN.md
@crates/app/src/stress.rs
@crates/app/src/http_stress.rs
@crates/app/src/main.rs
@scripts/compare-stress-layers.sh
@target/phase-13.1/layer-comparison/live-http-unique.json
@target/phase-13.1/layer-comparison/live-http-shard-1.json
@target/phase-13.1/layer-comparison/live-http-shard-8.json
@target/phase-13.1/layer-comparison/live-http-single-hot-key-diagnostic.json

<interfaces>
From `crates/app/src/stress.rs`:
```rust
pub struct StressReport {
    pub append_latency_p95_micros: Option<u64>,
    pub metrics_scrape_successes: u64,
    pub workload_shape: String,
    pub workload_purpose: String,
}
```

From `crates/app/src/http_stress.rs`:
```rust
pub struct HttpStressConfig {
    pub shard_count: usize,
    pub ingress_capacity: usize,
    pub ring_size: usize,
}

fn stress_report_from_measured(...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 13.2-04-01: Replace ambiguous append-latency fallback with explicit availability semantics and persist lane metadata in the JSON contract</name>
  <files>crates/app/src/stress.rs, crates/app/src/http_stress.rs, crates/app/src/main.rs</files>
  <read_first>
    - crates/app/src/stress.rs
    - crates/app/src/http_stress.rs
    - crates/app/src/main.rs
    - .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-VERIFICATION.md
    - target/phase-13.1/layer-comparison/live-http-unique.json
    - target/phase-13.1/layer-comparison/live-http-shard-1.json
    - target/phase-13.1/layer-comparison/live-http-shard-8.json
  </read_first>
  <behavior>
    - Success-throughput live HTTP JSON serializes `append_latency_p95_micros: null` when append histogram delta is unavailable rather than serializing `0`.
    - The JSON also records `append_latency_observed`, `append_latency_sample_count_delta`, and `append_latency_unavailable_reason` so docs can distinguish "missing metric evidence" from "measured zero latency."
    - Every external HTTP report includes `shard_count`, `ingress_capacity`, and `ring_size` sourced from `HttpStressConfig`, without adding any high-cardinality metric labels.
  </behavior>
  <action>
    Update the external HTTP stress report contract across `crates/app/src/stress.rs`, `crates/app/src/http_stress.rs`, and `crates/app/src/main.rs` to close verification gaps 1 and 2.

    Required field changes:
    - Keep `append_latency_p95_micros` as nullable JSON and make it `null` when the measured-window histogram delta is unavailable or has zero delta samples.
    - Add `append_latency_observed: bool`.
    - Add `append_latency_sample_count_delta: u64`.
    - Add `append_latency_unavailable_reason: Option<String>` with a bounded, low-cardinality string such as `missing_histogram_count`, `zero_histogram_delta`, or `no_successful_scrapes`.
    - Add lane metadata fields `shard_count: usize`, `ingress_capacity: usize`, and `ring_size: usize` to the report and JSON output, sourcing them directly from `HttpStressConfig`.

    Implementation requirements:
    - Refactor the append-latency scrape path so it returns both the p95 value and the measured sample-count delta instead of collapsing missing information into `0`.
    - Preserve `ensure_metrics_scrapes_observed` as the hard gate for zero-success scrape windows.
    - Do not add `tenant_id`, `command_id`, `correlation_id`, `causation_id`, `stream_id`, `event_id`, or `idempotency_key` as Prometheus labels or JSON keys.
    - Extend the existing Rust tests in these files so they assert the new nullable/availability semantics and JSON lane metadata.
  </action>
  <verify>
    <automated>cargo test -p app -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p app -- --nocapture` exits 0.
    - `rg -n 'append_latency_observed|append_latency_sample_count_delta|append_latency_unavailable_reason|shard_count|ingress_capacity|ring_size' crates/app/src/stress.rs crates/app/src/http_stress.rs crates/app/src/main.rs` finds the new fields in the report contract and JSON serializer.
    - `rg -n 'unwrap_or\\(0\\)' crates/app/src/http_stress.rs` returns no match for append-latency histogram delta handling.
    - `rg -n 'tenant_id|command_id|correlation_id|causation_id|stream_id|event_id|idempotency_key' crates/app/src/observability.rs` shows the bounded metric-label surface remains unchanged.
  </acceptance_criteria>
  <done>The report contract explicitly distinguishes unavailable append-latency evidence from observed values, and shard comparison artifacts can describe their own lane configuration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 13.2-04-02: Capture hot-key failure taxonomy and regenerate the affected live HTTP artifacts plus baseline ledger</name>
  <files>crates/app/src/stress.rs, crates/app/src/http_stress.rs, crates/app/src/main.rs, scripts/compare-stress-layers.sh, .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-BASELINE-REGEN.md, target/phase-13.1/layer-comparison/live-http-unique.json, target/phase-13.1/layer-comparison/live-http-shard-1.json, target/phase-13.1/layer-comparison/live-http-shard-8.json, target/phase-13.1/layer-comparison/live-http-single-hot-key-diagnostic.json</files>
  <read_first>
    - crates/app/src/http_stress.rs
    - crates/adapter-http/src/error.rs
    - scripts/compare-stress-layers.sh
    - .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-BASELINE-REGEN.md
    - target/phase-13.1/layer-comparison/live-http-single-hot-key-diagnostic.json
    - target/phase-13.1/layer-comparison/live-http-unique.json
    - target/phase-13.1/layer-comparison/live-http-shard-1.json
    - target/phase-13.1/layer-comparison/live-http-shard-8.json
  </read_first>
  <behavior>
    - The hot-key diagnostic artifact includes `failure_kind_counts` and `sample_failures` with bounded samples instead of a raw total-failure counter only.
    - Conflict-like responses such as HTTP `409` with API code `conflict` are classified and counted instead of surfacing as opaque harness failures.
    - Regenerated live HTTP artifacts carry the new append-latency semantics and lane metadata fields, and `13.2-BASELINE-REGEN.md` records those fields explicitly.
  </behavior>
  <action>
    Extend the external HTTP harness and artifact regeneration flow to close verification gap 3 and finish the artifact side of gap 2.

    Required code changes:
    - In `crates/app/src/http_stress.rs`, change per-request result handling so diagnostic failures capture structured information instead of only status totals. Support at minimum:
      - HTTP status code
      - API error code from the JSON body when present
      - a bounded `kind` classification such as `conflict`, `domain`, `invalid_request`, `overloaded`, `unavailable`, `internal`, `unexpected_status`, or `transport`
      - a bounded sample list capped to a small fixed number such as 5
    - Treat `409 Conflict` as diagnosable failed work for the hot-key diagnostic lane rather than an unexpected harness exception.
    - Add `failure_kind_counts` and `sample_failures` to `StressReport` and `main.rs` JSON serialization. Keep the samples bounded and artifact-safe.

    Regeneration requirements:
    - Run `PHASE13_1_COMPARE_MODE=baseline bash scripts/compare-stress-layers.sh`.
    - Overwrite `live-http-unique.json`, `live-http-shard-1.json`, `live-http-shard-8.json`, and `live-http-single-hot-key-diagnostic.json` with the new schema.
    - Update `13.2-BASELINE-REGEN.md` so `## Observed Baseline Fields` records the new append-latency fields and lane metadata for the three success-throughput files, and add a `## Hot-Key Diagnostic Fields` section recording `failure_kind_counts` and `sample_failures` from `live-http-single-hot-key-diagnostic.json`.
  </action>
  <verify>
    <automated>PHASE13_1_COMPARE_MODE=baseline bash scripts/compare-stress-layers.sh</automated>
  </verify>
  <acceptance_criteria>
    - `PHASE13_1_COMPARE_MODE=baseline bash scripts/compare-stress-layers.sh` exits 0.
    - `jq -e '.append_latency_observed | type == "boolean"' target/phase-13.1/layer-comparison/live-http-unique.json target/phase-13.1/layer-comparison/live-http-shard-1.json target/phase-13.1/layer-comparison/live-http-shard-8.json` succeeds.
    - `jq -e '.shard_count and .ingress_capacity and .ring_size' target/phase-13.1/layer-comparison/live-http-shard-1.json target/phase-13.1/layer-comparison/live-http-shard-8.json` succeeds.
    - `jq -e '.failure_kind_counts | type == "object"' target/phase-13.1/layer-comparison/live-http-single-hot-key-diagnostic.json` succeeds.
    - `jq -e '.sample_failures | type == "array"' target/phase-13.1/layer-comparison/live-http-single-hot-key-diagnostic.json` succeeds.
    - `rg -n '^## Hot-Key Diagnostic Fields$|append_latency_observed|append_latency_sample_count_delta|append_latency_unavailable_reason|failure_kind_counts|sample_failures|shard_count|ingress_capacity|ring_size' .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-BASELINE-REGEN.md` finds the refreshed ledger fields.
  </acceptance_criteria>
  <done>The regenerated artifacts are self-describing, hot-key failures are diagnosable from the JSON, and the baseline ledger records the new evidence contract explicitly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured Prometheus data -> archive JSON | Missing histogram deltas must not be serialized as trustworthy observed latency. |
| CLI/report contract -> downstream docs | Closure docs will overstate evidence unless the JSON clearly exposes unavailable vs observed fields. |
| hot-key diagnostic responses -> artifact consumers | Failure samples must stay bounded and low-risk while still revealing the true failure class. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.2-10 | T | `crates/app/src/http_stress.rs` append-latency assembly | mitigate | Replace ambiguous `0` fallback with explicit nullable value, sample-count delta, and unavailable reason in the report path. |
| T-13.2-11 | R | `target/phase-13.1/layer-comparison/*.json` shard comparison evidence | mitigate | Persist `shard_count`, `ingress_capacity`, and `ring_size` into each external HTTP artifact so the lane is self-describing without filename context. |
| T-13.2-12 | I | hot-key diagnostic samples | mitigate | Keep `sample_failures` bounded and artifact-safe; record status/code/kind/message only, and do not include high-cardinality identifiers or secret environment data. |
| T-13.2-13 | D | Prometheus metric surface | mitigate | Preserve the existing bounded label set and explicitly avoid adding tenant, command, correlation, causation, stream, event, or idempotency labels while fixing the evidence contract. |
</threat_model>

<verification>
- `cargo test -p app -- --nocapture`
- `PHASE13_1_COMPARE_MODE=baseline bash scripts/compare-stress-layers.sh`
</verification>

<success_criteria>
- Live HTTP JSON no longer uses `append_latency_p95_micros: 0` as a silent fallback for missing histogram evidence.
- Shard comparison files describe their own lane configuration directly from JSON.
- The hot-key diagnostic artifact exposes a bounded failure taxonomy and sample failures that explain why commands failed.
</success_criteria>

<output>
After completion, create `.planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-04-SUMMARY.md`
</output>
