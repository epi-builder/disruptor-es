## Evidence Inputs

- `target/phase-13.1/layer-comparison/live-http-unique.json`: `throughput_per_second = 424.6077384791371`, `metrics_scrape_successes = 121`, `ingress_depth_max = 2`, `shard_depth_max = 0`, `append_latency_observed = false`, `append_latency_p95_micros = null`, `append_latency_sample_count_delta = 12745`, `append_latency_unavailable_reason = "missing_histogram_count"`, `shard_count = 8`, `ingress_capacity = 256`, `ring_size = 256`, `ring_wait_p95_micros = 0`, `projection_lag = 0`, `outbox_lag = 0`.
- `target/phase-13.1/layer-comparison/live-http-shard-1.json`: `throughput_per_second = 106.77108772430238`, `metrics_scrape_successes = 121`, `ingress_depth_max = 1`, `shard_depth_max = 0`, `append_latency_observed = false`, `append_latency_p95_micros = null`, `append_latency_sample_count_delta = 3203`, `append_latency_unavailable_reason = "missing_histogram_count"`, `shard_count = 1`, `ingress_capacity = 256`, `ring_size = 256`, `ring_wait_p95_micros = 0`, `projection_lag = 0`, `outbox_lag = 0`.
- `target/phase-13.1/layer-comparison/live-http-shard-8.json`: `throughput_per_second = 424.4439409381085`, `metrics_scrape_successes = 121`, `ingress_depth_max = 5`, `shard_depth_max = 0`, `append_latency_observed = false`, `append_latency_p95_micros = null`, `append_latency_sample_count_delta = 12741`, `append_latency_unavailable_reason = "missing_histogram_count"`, `shard_count = 8`, `ingress_capacity = 256`, `ring_size = 256`, `ring_wait_p95_micros = 0`, `projection_lag = 0`, `outbox_lag = 0`.
- `target/phase-13.1/layer-comparison/adapter-only.txt`: `adapter_only_decode_envelope_submit = 2.1490 us`, `adapter_only_burst_overload = 4.3665 us`.
- `target/phase-13.1/layer-comparison/ring-only.txt`: `ring_only_publish_poll = 76.206 ns`, `ring_only_hot_key_publish_poll = 83.451 ns`.
- `target/phase-13.1/layer-comparison/in-process-runtime.json`: `throughput_per_second = 9.263467124871099`, `append_latency_p95_micros = 89279`, `ingress_depth_max = 4`, `shard_depth_max = 4`, `metrics_scrape_successes = 0`.
- `target/phase-13.1/layer-comparison/storage-only.txt`: `storage_only_append = 6.5430 ms`, `storage_only_occ_conflict = 3.9051 ms`, `storage_only_dedupe = 3.1946 ms`, `storage_only_global_read = 1.5301 ms`.
- `.planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-SCRAPE-REPRO.md`: the smoke rerun passed `cargo test -p app http_stress -- --nocapture` and `PHASE13_1_COMPARE_MODE=smoke bash scripts/compare-stress-layers.sh`, proving the current live HTTP path no longer fails the zero-scrape gate.
- `.planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-BASELINE-REGEN.md`: the baseline rerun regenerated `live-http-unique.json`, `live-http-shard-1.json`, and `live-http-shard-8.json` with `metrics_scrape_successes = 121` in every success-throughput live HTTP artifact.

## Dominant throughput ceiling

Phase 13.2 reclassifies the archive-safe dominant throughput ceiling as `durable command path (append latency unavailable in live HTTP artifacts)`, not `inconclusive`.

This label is evidence-safe because the regenerated baseline live HTTP artifacts `live-http-unique.json`, `live-http-shard-1.json`, and `live-http-shard-8.json` all report `metrics_scrape_successes = 121`, satisfying the scrape gate from `docs/stress-results.md`, while also reporting `append_latency_observed = false` with `append_latency_p95_micros = null` and `append_latency_unavailable_reason = "missing_histogram_count"`. The observed comparison still rules out the fastest local layers as the dominant service ceiling: `ring-only.txt` stays in the tens of nanoseconds, `adapter-only.txt` stays in the low single-digit microseconds, and the success-throughput live HTTP lanes top out near `106.8-424.6` commands per second depending on shard metadata recorded in `shard_count`, `ingress_capacity`, and `ring_size`. That makes `storage-only.txt` plus the shard-count comparison an inference about the durable path, not direct live HTTP append proof, so the archive wording must stay broader than `storage append`.

## Layer-by-layer classification

- `live HTTP/client harness`: `live-http-unique.json`, `live-http-shard-1.json`, and `live-http-shard-8.json` are now valid Phase 13.2 evidence because every cited baseline file has positive `metrics_scrape_successes`. The harness is no longer the blocker that forced the old `inconclusive` label.
- `adapter admission / bounded ingress`: `adapter-only.txt` shows microsecond-scale decode and admission work, while the baseline live HTTP files show `commands_rejected = 0` and `ingress_depth_max` only `1-2`. The adapter admission path is not the dominant ceiling in the regenerated baseline.
- `shard runtime / routing`: `live-http-shard-1.json` (`shard_count = 1`, `ingress_capacity = 256`, `ring_size = 256`) drops to `106.77108772430238` commands per second while `live-http-shard-8.json` (`shard_count = 8`, `ingress_capacity = 256`, `ring_size = 256`) reaches `424.4439409381085`. Routing and shard parallelism clearly affect throughput when the runtime is collapsed to one shard, but the eight-shard lanes show that shard runtime is not the final ceiling once parallelism is restored.
- `disruptor handoff / ring-only`: `ring-only.txt` remains two orders of magnitude faster than the observed service lanes. `ring_wait_p95_micros = 0` in every regenerated live HTTP baseline file, so the disruptor handoff is not the measured bottleneck.
- `aggregate decision / runtime-only`: `in-process-runtime.json` is useful as a small-sample contrast, but it still has `metrics_scrape_successes = 0` and only a four-command finite batch. It cannot override the baseline live HTTP classification. Its `append_latency_p95_micros = 89279` still points at the durable path rather than pure aggregate decision cost.
- `storage append / storage-only`: `storage-only.txt` is the slowest isolated layer artifact that remains consistent with the regenerated live HTTP ceiling. Millisecond-scale append, OCC, and dedupe work are vastly slower than ring-only or adapter-only measurements, but the live HTTP artifacts do not directly observe append latency because `append_latency_observed = false` in all three success-throughput lanes.
- `projection/outbox`: `projection_lag = 0` and `outbox_lag = 0` across `live-http-unique.json`, `live-http-shard-1.json`, and `live-http-shard-8.json`. Projection and outbox do not present as the dominant ceiling in the regenerated baseline evidence.
- `measurement configuration evidence`: `13.2-SCRAPE-REPRO.md` proves the smoke lane preserved the hard scrape-success gate, and `13.2-BASELINE-REGEN.md` proves the baseline rerun produced non-zero scrape counts in all cited success-throughput files. Phase 13.2 closes the stale zero-scrape measurement gap instead of narrating around it.

## Hot-key diagnostic interpretation

`live-http-single-hot-key-diagnostic.json` remains diagnostic-only and must stay out of success-throughput ceiling claims. It is archive-safe only as a failure taxonomy artifact: `failure_kind_counts = {"domain": 13180}` and `sample_failures` contains bounded `status_code = 400`, `api_error_code = "domain"`, `kind = "domain"`, and message `"order is already placed"` samples. That supports a repeat-stream domain-failure explanation rather than a raw failure-total narrative.

## Replaced Phase 13.1 claims

- Replaced the stale Phase 13.1 archive conclusion that the dominant ceiling had to remain `inconclusive` because `live-http-unique.json`, `live-http-shard-1.json`, and `live-http-shard-8.json` reported `metrics_scrape_successes = 0`.
- Replaced the weaker Phase 13.1 explanation that baseline live HTTP files could still be cited for throughput while scrape-derived fields stayed absent. Phase 13.2 regenerated those same files with observed scrape data.
- Replaced any suggestion that the old zero-scrape baseline files could remain authoritative after reruns. The archive-safe evidence source is now the Phase 13.2 regenerated `live-http-unique.json`, `live-http-shard-1.json`, and `live-http-shard-8.json`, together with `13.2-SCRAPE-REPRO.md` and `13.2-BASELINE-REGEN.md`.

## Blocker status

See `.planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-BLOCKERS.md`. Phase 13.2 closes the verification gaps by making the blocker disposition depend on the regenerated evidence contract rather than narrative inference.
