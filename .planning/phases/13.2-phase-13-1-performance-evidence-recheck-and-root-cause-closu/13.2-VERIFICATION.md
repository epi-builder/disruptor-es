---
phase: 13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu
verified: 2026-04-27T14:54:31Z
status: gaps_found
score: 2/5 must-haves verified
overrides_applied: 0
gaps:
  - truth: "The Prometheus scrape/readiness path is rerun or repaired enough that live HTTP stress reports include observed ingress depth, shard depth, append latency, and resource metadata, or the phase records a concrete blocker with reproduction steps."
    status: failed
    reason: "The regenerated live HTTP artifacts have positive scrape counts and observed depth fields, but all three baseline files still serialize `append_latency_p95_micros: 0`, and the report pipeline defaults missing histogram deltas to `0` instead of recording an unavailable state or blocker."
    artifacts:
      - path: "target/phase-13.1/layer-comparison/live-http-unique.json"
        issue: "`append_latency_p95_micros` is `0` despite positive scrapes, so the archive cannot tell whether append latency was measured or unavailable."
      - path: "target/phase-13.1/layer-comparison/live-http-shard-1.json"
        issue: "Same ambiguous `append_latency_p95_micros: 0` value."
      - path: "target/phase-13.1/layer-comparison/live-http-shard-8.json"
        issue: "Same ambiguous `append_latency_p95_micros: 0` value."
      - path: "crates/app/src/http_stress.rs"
        issue: "`scrape_metrics()` and `stress_report_from_measured()` fall back to `0` for missing append-latency histogram deltas."
    missing:
      - "Either emit a real observed append-latency value in the archive-safe live HTTP artifacts or encode/report an explicit unavailable/blocker state instead of `0`."
      - "Update `13.2-BLOCKERS.md` if the live HTTP append metric cannot be observed reproducibly."
  - truth: "The dominant throughput ceiling is reclassified from observed data, not estimates, across HTTP/client harness, adapter admission, shard runtime, disruptor handoff, aggregate decision, storage append, projection/outbox, and measurement configuration."
    status: failed
    reason: "The closure and shared docs reclassify the archive label to `storage append / durable command path`, but that claim is not backed by measured append latency in the cited live HTTP files, and the shard-count comparison depends on filename/script context because the archived JSON does not record `shard_count`, `ingress_capacity`, or `ring_size`."
    artifacts:
      - path: ".planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-CLOSURE.md"
        issue: "States `storage append / durable command path` as the final ceiling despite citing live HTTP append values of `0`."
      - path: "docs/stress-results.md"
        issue: "Publishes the same narrower label as archive-safe guidance."
      - path: "docs/template-guide.md"
        issue: "Publishes the same narrower label as operator guidance."
      - path: "target/phase-13.1/layer-comparison/live-http-shard-1.json"
        issue: "No self-describing shard configuration fields; comparison relies on filename and script command."
      - path: "target/phase-13.1/layer-comparison/live-http-shard-8.json"
        issue: "No self-describing shard configuration fields; comparison relies on filename and script command."
    missing:
      - "Downgrade the archive-safe ceiling label to match the available evidence, or regenerate artifacts that directly support a `storage append` conclusion."
      - "Persist lane configuration metadata such as `shard_count`, `ingress_capacity`, and `ring_size` in the archived JSON if shard-level comparisons are part of the sign-off claim."
  - truth: "Any confirmed implementation or harness bottleneck found during the recheck is fixed or converted into an explicit Phase 14 archive blocker; no uncertain performance claim remains hidden as accepted debt."
    status: failed
    reason: "The hot-key diagnostic lane still records total failure with no failure taxonomy or sample error payload, yet `13.2-BLOCKERS.md` declares `Phase 14 archive blocker: none from Phase 13.2.`"
    artifacts:
      - path: "target/phase-13.1/layer-comparison/live-http-single-hot-key-diagnostic.json"
        issue: "Contains `commands_failed: 13639` and `commands_succeeded: 0` but no `failure_kind_counts` or sample failures."
      - path: ".planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-BLOCKERS.md"
        issue: "Declares no remaining Phase 14 blocker."
    missing:
      - "Either add enough diagnostic detail to classify the hot-key failure cause or record it explicitly as a Phase 14 archive blocker with reproduction details."
---

# Phase 13.2: Phase 13.1 Performance Evidence Recheck and Root-Cause Closure Verification Report

**Phase Goal:** Reopen the uncertain parts of Phase 13.1's low-throughput investigation, replace any `inconclusive` archive claims with reproducible observed evidence where possible, and either classify the dominant full-service throughput ceiling with enough data for final sign-off or explicitly document the remaining measurement blocker as milestone-critical work.
**Verified:** 2026-04-27T14:54:31Z
**Status:** gaps_found
**Re-verification:** No - initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Phase 13.1's `inconclusive` statements are audited against raw artifacts, docs, and code so every uncertain claim is listed with the exact missing or weak evidence. | ✓ VERIFIED | [13.2-EVIDENCE-AUDIT.md](/Users/epikem/dev/projects/disruptor-es/.planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-EVIDENCE-AUDIT.md:1) contains the required matrix and cites each stale Phase 13.1 source plus the weak fields. |
| 2 | The Prometheus scrape/readiness path is rerun or repaired enough that live HTTP stress reports include observed ingress depth, shard depth, append latency, and resource metadata, or the phase records a concrete blocker with reproduction steps. | ✗ FAILED | [13.2-SCRAPE-REPRO.md](/Users/epikem/dev/projects/disruptor-es/.planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-SCRAPE-REPRO.md:1) proves scrapes succeed, but the regenerated live HTTP files still store `append_latency_p95_micros: 0` ([live-http-unique.json](/Users/epikem/dev/projects/disruptor-es/target/phase-13.1/layer-comparison/live-http-unique.json:1), [live-http-shard-1.json](/Users/epikem/dev/projects/disruptor-es/target/phase-13.1/layer-comparison/live-http-shard-1.json:1), [live-http-shard-8.json](/Users/epikem/dev/projects/disruptor-es/target/phase-13.1/layer-comparison/live-http-shard-8.json:1)), and [http_stress.rs](/Users/epikem/dev/projects/disruptor-es/crates/app/src/http_stress.rs:744) falls back to `0` when histogram deltas are unavailable. |
| 3 | Layer-comparison evidence is regenerated under corrected scrape semantics for ring-only, runtime-only, storage-only, adapter-only, and live HTTP lanes. | ✓ VERIFIED | [13.2-BASELINE-REGEN.md](/Users/epikem/dev/projects/disruptor-es/.planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-BASELINE-REGEN.md:1) inventories all required regenerated outputs, and the files exist under `target/phase-13.1/layer-comparison/`. |
| 4 | The dominant throughput ceiling is reclassified from observed data, not estimates, across HTTP/client harness, adapter admission, shard runtime, disruptor handoff, aggregate decision, storage append, projection/outbox, and measurement configuration. | ✗ FAILED | [13.2-CLOSURE.md](/Users/epikem/dev/projects/disruptor-es/.planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-CLOSURE.md:15), [docs/stress-results.md](/Users/epikem/dev/projects/disruptor-es/docs/stress-results.md:123), and [docs/template-guide.md](/Users/epikem/dev/projects/disruptor-es/docs/template-guide.md:164) publish `storage append / durable command path`, but the cited live HTTP artifacts do not contain measured append latency, and the shard comparison is not self-describing in the archived JSON. |
| 5 | Any confirmed implementation or harness bottleneck found during the recheck is fixed or converted into an explicit Phase 14 archive blocker; no uncertain performance claim remains hidden as accepted debt. | ✗ FAILED | [live-http-single-hot-key-diagnostic.json](/Users/epikem/dev/projects/disruptor-es/target/phase-13.1/layer-comparison/live-http-single-hot-key-diagnostic.json:1) records total failure with no cause taxonomy, while [13.2-BLOCKERS.md](/Users/epikem/dev/projects/disruptor-es/.planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-BLOCKERS.md:1) says there is no Phase 14 blocker. |

**Score:** 2/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `.planning/.../13.2-EVIDENCE-AUDIT.md` | Claim-by-claim stale evidence ledger | ✓ VERIFIED | Required header and mandatory Phase 13.1 sources are present. |
| `.planning/.../13.2-SCRAPE-REPRO.md` | Smoke rerun record with commands and scrape counters | ✓ VERIFIED | Records both required commands and the smoke JSON scrape counters. |
| `crates/app/src/http_stress.rs` | Hard scrape gate and live HTTP report assembly | ✓ VERIFIED | `ensure_metrics_scrapes_observed()` remains a hard failure gate at [http_stress.rs](/Users/epikem/dev/projects/disruptor-es/crates/app/src/http_stress.rs:533). |
| `target/phase-13.1/layer-comparison/live-http-unique.json` | Archive-safe baseline live HTTP evidence | ⚠ HOLLOW | Real scrape counts and depth fields are present, but `append_latency_p95_micros` is an ambiguous `0`. |
| `target/phase-13.1/layer-comparison/live-http-shard-1.json` | Self-describing one-shard comparison evidence | ⚠ HOLLOW | Throughput differs materially, but the JSON omits `shard_count`, `ingress_capacity`, and `ring_size`. |
| `target/phase-13.1/layer-comparison/live-http-shard-8.json` | Self-describing eight-shard comparison evidence | ⚠ HOLLOW | Same metadata omission as the one-shard artifact. |
| `.planning/.../13.2-BASELINE-REGEN.md` | Baseline rerun ledger | ✓ VERIFIED | Captures the baseline command and observed field inventory. |
| `.planning/.../13.2-CLOSURE.md` | Final closure memo | ✗ STUB FOR SIGN-OFF | Substantive prose exists, but the final label overstates what the cited artifacts prove. |
| `.planning/.../13.2-BLOCKERS.md` | Explicit blocker ledger | ✗ STUB FOR SIGN-OFF | Exists and is wired, but incorrectly records `none` despite unresolved evidence defects. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `13.2-EVIDENCE-AUDIT.md` | `13.1-06-SUMMARY.md` | claim-by-claim audit | ✓ WIRED | Verified by `gsd-tools verify key-links`. |
| `observability.rs` | `http_stress.rs` | Prometheus listener bootstrap and measured-window scrape enforcement | ✓ WIRED | Exporter install path is present in [observability.rs](/Users/epikem/dev/projects/disruptor-es/crates/app/src/observability.rs:68), and the hard gate remains in [http_stress.rs](/Users/epikem/dev/projects/disruptor-es/crates/app/src/http_stress.rs:533). |
| `scripts/compare-stress-layers.sh` | `live-http-shard-1.json` / `live-http-shard-8.json` | baseline regeneration commands | ✓ WIRED | The script runs explicit `--shard-count 1` and `--shard-count 8` lanes at [compare-stress-layers.sh](/Users/epikem/dev/projects/disruptor-es/scripts/compare-stress-layers.sh:65). |
| `13.2-CLOSURE.md` | `13.2-BASELINE-REGEN.md` | observed field citation | ✓ WIRED | Verified by `gsd-tools verify key-links`. |
| `13.2-BLOCKERS.md` | `Phase 14` | explicit archive blocker wording | ✓ WIRED | The blocker file is referenced from the closure memo, but the content is not sufficient. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| `live-http-unique.json` | `metrics_scrape_successes`, `ingress_depth_max`, `shard_depth_max` | `scrape_metrics()` -> `record_metrics_scrape_result()` -> `stress_report_from_measured()` -> `main.rs` JSON serialization | Yes | ✓ FLOWING |
| `live-http-unique.json` | `append_latency_p95_micros` | `histogram_p95_delta_micros(..., "es_append_latency_seconds", ...)` with `.unwrap_or(0)` fallback in [http_stress.rs](/Users/epikem/dev/projects/disruptor-es/crates/app/src/http_stress.rs:744) | No, `0` is also used when the histogram delta is unavailable | ⚠ STATIC |
| `live-http-shard-1.json` / `live-http-shard-8.json` | shard configuration for comparison claim | Script command flags only; not persisted by [main.rs](/Users/epikem/dev/projects/disruptor-es/crates/app/src/main.rs:7) | No | ✗ DISCONNECTED |
| `live-http-single-hot-key-diagnostic.json` | failure cause details | No `failure_kind_counts` or `sample_failures` field in artifact schema | No | ✗ HOLLOW |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Zero-scrape hard gate still enforced on the smoke path | `cargo test -p app http_stress -- --nocapture` | Passed, including `external_process_http_stress_smoke` and `zero_successful_metrics_scrapes_return_an_error` | ✓ PASS |
| Regenerated live HTTP baselines retain positive scrape counts and observed queue-depth fields | `jq` checks on `live-http-unique.json`, `live-http-shard-1.json`, `live-http-shard-8.json` | All three returned success for `metrics_scrape_successes > 0`, `metrics_sample_count > 0`, and non-null observed depth/lag fields | ✓ PASS |
| Live HTTP baselines provide measured append latency for the final ceiling claim | `jq '.append_latency_p95_micros'` on the three baseline live HTTP artifacts | `0`, `0`, `0` | ✗ FAIL |
| Hot-key diagnostic artifact exposes a diagnosable failure taxonomy | `jq 'has("failure_kind_counts") or has("sample_failures")' target/phase-13.1/layer-comparison/live-http-single-hot-key-diagnostic.json` | `false` | ✗ FAIL |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| `RUNTIME-01` | 13.2-02, 13.2-03 | Adapter requests enter the command engine through bounded ingress with explicit overload behavior. | ✓ SATISFIED | Baseline live HTTP artifacts show bounded ingress metrics and `commands_rejected = 0`; this phase revalidated the archived evidence, not the original implementation. |
| `RUNTIME-02` | 13.2-02, 13.2-03 | Partition routing sends all commands for the same aggregate key to the same local shard owner. | ✓ SATISFIED | The comparison script runs explicit `--shard-count 1` and `--shard-count 8` lanes, and the closure memo uses that routing contrast. Evidence traceability is incomplete because the JSON omits shard metadata. |
| `RUNTIME-05` | 13.2-01, 13.2-02, 13.2-03 | Command replies are sent only after durable event-store append commit succeeds. | ✓ SATISFIED | This phase’s smoke and baseline commands exercise the durable external-process path; [docs/stress-results.md](/Users/epikem/dev/projects/disruptor-es/docs/stress-results.md:49) keeps those lanes scoped to the durable command path. |
| `TEST-03` | 13.2-02 | Benchmark harnesses separately measure ring-only, domain-only, adapter-only, storage-only, single-service integrated, full E2E, projector/outbox, hot-key, burst, and degraded dependency scenarios. | ✓ SATISFIED | [compare-stress-layers.sh](/Users/epikem/dev/projects/disruptor-es/scripts/compare-stress-layers.sh:35) regenerates the separated ring, adapter, storage, runtime, and live HTTP comparison lanes used by this phase. |
| `TEST-04` | 13.2-01, 13.2-02 | A single-service integrated stress test runs the production-shaped composition and reports throughput, latency, depths, append latency, lag, reject rate, and CPU/core utilization. | ✓ SATISFIED | The external-process smoke test passed and the baseline JSON exposes the required report fields, though append-latency evidence remains ambiguous for final sign-off. |
| `OBS-02` | 13.2-01, 13.2-02, 13.2-03 | Metrics expose ingress depth, shard queue depth, ring wait, decision latency, append latency, OCC conflicts, dedupe hits, projection lag, outbox lag, and p95/p99 command latency. | ✓ SATISFIED WITH GAP | The instrumentation and JSON fields exist, but this phase did not prove that archive-safe live HTTP append-latency values are truly observed rather than `0` fallbacks. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| `crates/app/src/http_stress.rs` | 744 | Silent `.unwrap_or(0)` fallback for `append_latency_p95_micros` | 🛑 Blocker | Makes archive-safe live HTTP append latency indistinguishable from “metric unavailable,” undermining the final ceiling claim. |
| `docs/stress-results.md` | 123 | Archive label overstates evidence as `storage append / durable command path` | 🛑 Blocker | Publishes a narrower conclusion than the cited live HTTP artifacts can prove. |
| `docs/template-guide.md` | 164 | Same overstatement in operator-facing guidance | ⚠ Warning | Spreads the unsupported label to future users of the template. |
| `target/phase-13.1/layer-comparison/live-http-single-hot-key-diagnostic.json` | 1 | Diagnostic artifact has total failures with no cause taxonomy | ⚠ Warning | Prevents the hot-key lane from serving as root-cause evidence or blocker-ready repro data. |

### Gaps Summary

Phase 13.2 successfully removed the stale zero-scrape uncertainty and regenerated the comparison bundle, but it did not achieve final sign-off quality. The live HTTP baselines prove scrape health and observed queue-depth fields, yet they still do not provide trustworthy append-latency evidence for the narrower `storage append` label because the report pipeline serializes missing histogram deltas as `0`. On top of that, the shard comparison relies on filename/script context instead of self-describing JSON metadata, and the hot-key diagnostic still lacks failure-cause detail while the blocker ledger says none remain.

The phase should therefore be treated as incomplete for archive closure. The next revision needs either stronger artifacts that directly support the `storage append` conclusion or explicit blocker language that preserves the unresolved measurement gaps as milestone-critical work.

---

_Verified: 2026-04-27T14:54:31Z_
_Verifier: Claude (gsd-verifier)_
