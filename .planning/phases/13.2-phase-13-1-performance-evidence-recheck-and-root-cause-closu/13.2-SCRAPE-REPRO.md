## Commands

1. `cargo test -p app http_stress -- --nocapture`
   - Exit status: `0`
2. `PHASE13_1_COMPARE_MODE=smoke bash scripts/compare-stress-layers.sh`
   - Exit status: `0`

## Observed Results

- `cargo test -p app http_stress -- --nocapture`
  - The suite passed, including `http_stress::tests::external_process_http_stress_smoke`.
  - The smoke test still enforces `report.metrics_scrape_successes > 0`, so this command proved the zero-success hard gate did not fire on the current durable external-process smoke lane.
  - The command output does not print the exact scrape count; it only proves the measured window observed at least one successful scrape.
- `PHASE13_1_COMPARE_MODE=smoke bash scripts/compare-stress-layers.sh`
  - Refreshed `target/phase-13.1/layer-comparison/live-http-unique.json` with:
    - `metrics_scrape_successes = 9`
    - `metrics_scrape_failures = 0`
    - `metrics_sample_count = 9`
    - `ingress_depth_max = 1`
    - `shard_depth_max = 0`
    - `append_latency_p95_micros = 0`
    - `ring_wait_p95_micros = 0`
    - `throughput_per_second = 140.80139224416652`
  - Refreshed `target/phase-13.1/layer-comparison/live-http-single-hot-key-diagnostic.json` with:
    - `metrics_scrape_successes = 9`
    - `metrics_scrape_failures = 0`
    - `metrics_sample_count = 9`
    - `ingress_depth_max = 1`
    - `shard_depth_max = 0`
    - `append_latency_p95_micros = 0`
    - `ring_wait_p95_micros = 0`
  - `target/phase-13.1/layer-comparison/in-process-runtime.json` also refreshed during the smoke script, but it is not a live HTTP Prometheus scrape artifact and still reports `metrics_scrape_successes = 0`.

## Fix Applied

None. The current code path already preserves the required Prometheus listener bootstrap in `crates/app/src/observability.rs` and the hard zero-success failure gate in `crates/app/src/http_stress.rs`, and both required commands succeeded without a readiness or scrape-path patch.
