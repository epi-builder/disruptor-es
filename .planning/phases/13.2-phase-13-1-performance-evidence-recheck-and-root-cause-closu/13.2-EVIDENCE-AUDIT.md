| Claim ID | Source file | Exact claim | Current status | Missing evidence | Replacement evidence | Phase 13.2 disposition |
| --- | --- | --- | --- | --- | --- | --- |
| C-13.1-06-001 | `.planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-06-SUMMARY.md` | `13.1-06-SUMMARY.md` says the strongest defensible ceiling label stays `inconclusive` because `live-http-unique.json`, `live-http-shard-1.json`, and `live-http-shard-8.json` each report `metrics_scrape_successes = 0`. | Awaiting Phase 13.2 rerun evidence. The archive statement matches the old files, but research already records a newer smoke rerun with non-zero scrapes, so the summary claim is stale as an archive-wide conclusion. | Missing regenerated baseline live HTTP evidence where `metrics_scrape_successes > 0` and the scrape-derived fields `ingress_depth_max`, `shard_depth_max`, `append_latency_p95_micros`, and `ring_wait_p95_micros` are observed instead of null. | `.planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-SCRAPE-REPRO.md` plus the next regenerated `target/phase-13.1/layer-comparison/live-http-*.json` outputs from the current code path. | Keep the claim listed as historically accurate for the stale files, but do not reuse it after Phase 13.2 rerun evidence lands. |
| C-13.1-06-002 | `.planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-06-SUMMARY.md` | `13.1-06-SUMMARY.md` states the archived baseline files still carry throughput data while `ingress_depth_max` remains absent and only `ingress_depth_estimated_max` is present. | Still valid for the archived JSON files, but stale as a basis for future bottleneck classification. | Missing observed queue-depth and append-path evidence in the same baseline artifacts: `ingress_depth_max`, `shard_depth_max`, `append_latency_p95_micros`, and `ring_wait_p95_micros` are null because `metrics_scrape_successes = 0`. | Fresh baseline rerun artifacts that preserve the durable path and show non-zero `metrics_scrape_successes` with populated scrape-derived fields. | Preserve as the audit explanation for why the old archive is weak; replace with rerun evidence before any ceiling reclassification. |
| C-13.1-VFY-001 | `.planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-VERIFICATION.md` | `13.1-VERIFICATION.md` marks the `inconclusive` ceiling as verified because all cited baseline files still have `metrics_scrape_successes = 0`. | Stale. The verification statement is anchored to obsolete baseline artifacts and does not reflect the current smoke evidence path. | Missing current-command reproduction that proves whether zero successful scrapes still occur on `cargo test -p app http_stress -- --nocapture` and `PHASE13_1_COMPARE_MODE=smoke bash scripts/compare-stress-layers.sh`. It also lacks observed `ingress_depth_max`, `shard_depth_max`, `append_latency_p95_micros`, and `ring_wait_p95_micros`. | `.planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-SCRAPE-REPRO.md` with exact commands, exit status, and measured-window `metrics_scrape_successes` counters. | Supersede this verification claim with a Phase 13.2 smoke rerun record before trusting the old verification narrative. |
| C-13.1-VFY-002 | `.planning/phases/13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st/13.1-VERIFICATION.md` | `13.1-VERIFICATION.md` treats `live-http-shard-1.json` as regenerated baseline evidence under corrected semantics because it contains `metrics_scrape_successes`, observed-null fields, and estimated fallback values. | Still valid as a description of file shape, but insufficient as proof of healthy live scraping. | Missing non-zero scrape success evidence. Presence of the field names alone is not enough while `metrics_scrape_successes = 0`, `ingress_depth_max = null`, `shard_depth_max = null`, `append_latency_p95_micros = null`, and `ring_wait_p95_micros = null`. | Replacement comes from rerun artifacts where those same fields are observed from the live Prometheus path instead of remaining null. | Keep as a structural note only; do not treat it as authoritative performance evidence after the Phase 13.2 audit. |
| C-DOC-001 | `docs/stress-results.md` | `docs/stress-results.md` says any ceiling classification narrower than `inconclusive` requires `metrics_scrape_successes > 0` in every cited baseline live HTTP artifact, and a cited file with zero successful scrapes keeps the ceiling `inconclusive`. | Still valid. This is the current interpretation rule and matches the evidence contract in code. | No missing rule text. The gap is operational evidence showing the rule can now be satisfied on the current live HTTP path. | `13.2-SCRAPE-REPRO.md` and subsequent regenerated baseline JSON files demonstrating non-zero `metrics_scrape_successes` without bypassing the hard scrape gate. | Retain unchanged as the governing rule for Phase 13.2 and later archive updates. |
| C-ART-001 | `target/phase-13.1/layer-comparison/live-http-unique.json` | `live-http-unique.json` is cited as baseline success-throughput evidence even though it records `metrics_scrape_successes = 0`, `ingress_depth_max = null`, `shard_depth_max = null`, `append_latency_p95_micros = null`, and `ring_wait_p95_micros = null`. | Stale artifact. | Missing real measured-window scrape data from the live Prometheus endpoint, despite the file carrying durable throughput and latency values. | Recreated `target/phase-13.1/layer-comparison/live-http-unique.json` from the current smoke/baseline workflow with non-zero `metrics_scrape_successes`. | Replace this file before using it in any authoritative ceiling comparison. |
| C-ART-002 | `target/phase-13.1/layer-comparison/live-http-shard-1.json` | `live-http-shard-1.json` is archived as a shard-count comparison file while `metrics_scrape_successes = 0`, `ingress_depth_max = null`, `shard_depth_max = null`, `append_latency_p95_micros = null`, and `ring_wait_p95_micros = null`. | Stale artifact. | Missing observed shard-path evidence from successful Prometheus scrapes during the measured window. | Recreated `target/phase-13.1/layer-comparison/live-http-shard-1.json` from the current durable live HTTP path with successful scrapes. | Replace before using shard-count evidence to explain the bottleneck. |
| C-ART-003 | `target/phase-13.1/layer-comparison/live-http-shard-8.json` | `live-http-shard-8.json` is archived as baseline shard-count evidence while `metrics_scrape_successes = 0`, `ingress_depth_max = null`, `shard_depth_max = null`, `append_latency_p95_micros = null`, and `ring_wait_p95_micros = null`. | Stale artifact. | Missing observed eight-shard scrape data proving the metrics exporter was reachable during the measured window. | Recreated `target/phase-13.1/layer-comparison/live-http-shard-8.json` from the current durable live HTTP path with successful scrapes. | Replace before using the eight-shard lane in any final ceiling classification. |
