---
phase: 13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu
plan: 5
type: execute
wave: 2
depends_on:
  - 13.2-04
files_modified:
  - .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-CLOSURE.md
  - .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-BLOCKERS.md
  - docs/stress-results.md
  - docs/template-guide.md
autonomous: true
requirements:
  - RUNTIME-01
  - RUNTIME-02
  - RUNTIME-05
  - TEST-03
  - TEST-04
  - OBS-02
gap_closure: true
must_haves:
  truths:
    - "Archive-facing docs describe only what the regenerated artifacts prove, including append-latency unavailable semantics when applicable."
    - "The final ceiling label no longer narrows to `storage append` unless regenerated live HTTP evidence directly supports that wording."
    - "If hot-key or append-latency evidence is still incomplete after regeneration, the remaining issue is recorded as an explicit Phase 14 archive blocker with reproduction details."
  artifacts:
    - path: ".planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-CLOSURE.md"
      provides: "evidence-safe final classification tied to regenerated artifacts"
      contains: "append_latency_observed"
    - path: ".planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-BLOCKERS.md"
      provides: "explicit Phase 14 blocker outcome for any unresolved evidence gap"
      contains: "Phase 14 archive blocker"
    - path: "docs/stress-results.md"
      provides: "archive interpretation rules aligned with the regenerated evidence contract"
      contains: "append_latency_unavailable_reason"
    - path: "docs/template-guide.md"
      provides: "operator guidance that cites self-describing live HTTP artifacts"
      contains: "failure_kind_counts"
  key_links:
    - from: ".planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-CLOSURE.md"
      to: "target/phase-13.1/layer-comparison/live-http-unique.json"
      via: "final ceiling wording tied to regenerated artifact fields"
      pattern: "append_latency_observed|append_latency_p95_micros|shard_count|failure_kind_counts"
    - from: "docs/stress-results.md"
      to: ".planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-BLOCKERS.md"
      via: "shared archive guidance for unresolved evidence"
      pattern: "Phase 14 archive blocker"
    - from: "docs/template-guide.md"
      to: "target/phase-13.1/layer-comparison/live-http-single-hot-key-diagnostic.json"
      via: "operator guidance for diagnostic-only artifact interpretation"
      pattern: "failure_kind_counts|sample_failures"
---

<objective>
Reconcile the closure memo, blocker ledger, and shared docs to the regenerated evidence so the archive story no longer exceeds what the artifacts prove.

Purpose: Once the artifact contract is fixed and the files are regenerated, the remaining risk is documentation drift. This plan updates the final wording, narrows or preserves the ceiling claim correctly, and records any unresolved item as explicit Phase 14 blocker work instead of accepted debt.
Output: corrected closure and blocker files plus shared docs that reference the new append-latency semantics, lane metadata, and hot-key diagnostic fields.
</objective>

<execution_context>
@/Users/epikem/.codex/get-shit-done/workflows/execute-plan.md
@/Users/epikem/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-VERIFICATION.md
@.planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-REVIEW.md
@.planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-BASELINE-REGEN.md
@.planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-CLOSURE.md
@.planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-BLOCKERS.md
@docs/stress-results.md
@docs/template-guide.md
@target/phase-13.1/layer-comparison/live-http-unique.json
@target/phase-13.1/layer-comparison/live-http-shard-1.json
@target/phase-13.1/layer-comparison/live-http-shard-8.json
@target/phase-13.1/layer-comparison/live-http-single-hot-key-diagnostic.json

<interfaces>
From `target/phase-13.1/layer-comparison/live-http-unique.json` after Plan 13.2-04:
```json
{
  "append_latency_p95_micros": null,
  "append_latency_observed": false,
  "append_latency_sample_count_delta": 0,
  "append_latency_unavailable_reason": "zero_histogram_delta",
  "shard_count": 8,
  "ingress_capacity": 256,
  "ring_size": 256
}
```

From `target/phase-13.1/layer-comparison/live-http-single-hot-key-diagnostic.json` after Plan 13.2-04:
```json
{
  "failure_kind_counts": {
    "conflict": 13639
  },
  "sample_failures": [
    {
      "kind": "conflict",
      "status": 409,
      "code": "conflict"
    }
  ]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 13.2-05-01: Reword the closure memo and shared docs so they match the regenerated evidence contract exactly</name>
  <files>.planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-CLOSURE.md, docs/stress-results.md, docs/template-guide.md</files>
  <read_first>
    - .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-CLOSURE.md
    - .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-BASELINE-REGEN.md
    - .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-VERIFICATION.md
    - .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-REVIEW.md
    - docs/stress-results.md
    - docs/template-guide.md
    - target/phase-13.1/layer-comparison/live-http-unique.json
    - target/phase-13.1/layer-comparison/live-http-shard-1.json
    - target/phase-13.1/layer-comparison/live-http-shard-8.json
    - target/phase-13.1/layer-comparison/live-http-single-hot-key-diagnostic.json
  </read_first>
  <action>
    Rewrite `.planning/.../13.2-CLOSURE.md`, `docs/stress-results.md`, and `docs/template-guide.md` so they no longer overstate the evidence called out in verification gaps 1 and 2.

    Required wording rules:
    - Only use the narrower ceiling label `storage append / durable command path` if the regenerated live HTTP success-throughput files show `append_latency_observed = true` and a non-null `append_latency_p95_micros` that supports the claim.
    - If append latency remains unavailable in those files, downgrade the label explicitly to a wording such as `durable command path (append latency unavailable in live HTTP artifacts)` and explain that the storage-only bench plus lane comparison are still inferential rather than directly measured live HTTP append evidence.
    - Cite the self-describing lane metadata fields `shard_count`, `ingress_capacity`, and `ring_size` when discussing shard-count comparisons so the claim no longer depends on filenames alone.
    - In the hot-key guidance, cite `failure_kind_counts` and `sample_failures` from `live-http-single-hot-key-diagnostic.json` instead of a raw failure total only.
    - Preserve the rule that stale pre-Phase-13.2 zero-scrape files are not archive-safe evidence.

    Update `13.2-CLOSURE.md` headings and content only as needed to keep the memo aligned with the regenerated artifact fields. Keep the layer-by-layer structure, but make the classification evidence-safe.
  </action>
  <verify>
    <automated>bash -lc 'for f in target/phase-13.1/layer-comparison/live-http-unique.json target/phase-13.1/layer-comparison/live-http-shard-1.json target/phase-13.1/layer-comparison/live-http-shard-8.json; do jq -e ".append_latency_observed | type == \"boolean\"" "$f" >/dev/null; jq -e ".append_latency_sample_count_delta | numbers" "$f" >/dev/null; jq -e "has(\"append_latency_unavailable_reason\")" "$f" >/dev/null; jq -e ".shard_count | numbers" "$f" >/dev/null; jq -e ".ingress_capacity | numbers" "$f" >/dev/null; jq -e ".ring_size | numbers" "$f" >/dev/null; done && rg -n "append_latency_observed|append_latency_unavailable_reason|shard_count|ingress_capacity|ring_size|failure_kind_counts|sample_failures|Phase 13\\.2" .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-CLOSURE.md docs/stress-results.md docs/template-guide.md'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'append_latency_observed|append_latency_unavailable_reason' .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-CLOSURE.md docs/stress-results.md docs/template-guide.md` finds the new append-latency wording.
    - `rg -n 'shard_count|ingress_capacity|ring_size' .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-CLOSURE.md docs/stress-results.md docs/template-guide.md` shows shard-comparison claims now cite self-describing lane metadata.
    - `rg -n 'failure_kind_counts|sample_failures' .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-CLOSURE.md docs/template-guide.md` shows the hot-key diagnostic guidance is evidence-backed.
    - `jq -e '.append_latency_observed | type == "boolean"' target/phase-13.1/layer-comparison/live-http-unique.json target/phase-13.1/layer-comparison/live-http-shard-1.json target/phase-13.1/layer-comparison/live-http-shard-8.json` succeeds, and each file also exposes `append_latency_sample_count_delta`, `append_latency_unavailable_reason`, `shard_count`, `ingress_capacity`, and `ring_size` for the wording decision.
    - If `jq -e '.append_latency_observed == true' target/phase-13.1/layer-comparison/live-http-unique.json target/phase-13.1/layer-comparison/live-http-shard-1.json target/phase-13.1/layer-comparison/live-http-shard-8.json` fails, then `rg -n 'storage append / durable command path' .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-CLOSURE.md docs/stress-results.md docs/template-guide.md` returns no match.
  </acceptance_criteria>
  <done>The closure memo and shared docs say only what the regenerated artifacts can defend, with no lingering overstatement about live HTTP append evidence.</done>
</task>

<task type="auto">
  <name>Task 13.2-05-02: Set the explicit Phase 14 blocker outcome from regenerated append-latency and hot-key evidence</name>
  <files>.planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-BLOCKERS.md, .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-CLOSURE.md</files>
  <read_first>
    - .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-BLOCKERS.md
    - .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-CLOSURE.md
    - .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-BASELINE-REGEN.md
    - target/phase-13.1/layer-comparison/live-http-unique.json
    - target/phase-13.1/layer-comparison/live-http-shard-1.json
    - target/phase-13.1/layer-comparison/live-http-shard-8.json
    - target/phase-13.1/layer-comparison/live-http-single-hot-key-diagnostic.json
    - .planning/ROADMAP.md
  </read_first>
  <behavior>
    - Branch A: if regenerated artifacts fully close the three verification gaps, `13.2-BLOCKERS.md` explicitly records that no Phase 14 blocker remains from Phase 13.2.
    - Branch B: if append-latency evidence remains incomplete for live HTTP, lane metadata is incomplete, or hot-key taxonomy remains insufficient, `13.2-BLOCKERS.md` records a concrete Phase 14 blocker payload with reproduction details instead of hiding the issue in narrative prose.
    - The blocker record stays milestone-critical and avoids vague future-work language.
  </behavior>
  <action>
    Set `.planning/.../13.2-BLOCKERS.md` from the regenerated evidence, and mirror the outcome in `13.2-CLOSURE.md`.

    Decision rules:
    - Clear the blocker only if all three conditions are true:
      1. all three success-throughput live HTTP artifacts are self-describing via `shard_count`, `ingress_capacity`, and `ring_size`
      2. all three success-throughput live HTTP artifacts expose explicit append-latency semantics via `append_latency_observed`, `append_latency_sample_count_delta`, and `append_latency_unavailable_reason`
      3. the hot-key diagnostic artifact contains `failure_kind_counts` and a non-empty `sample_failures` array
    - Branch A, blocker cleared: keep the first line exactly `Phase 14 archive blocker: none from Phase 13.2.` and list which verification gaps were closed by the regenerated evidence.
    - Branch B, blocker remains: write `Phase 14 archive blocker:` followed by a numbered list that includes all of the following, with no omissions:
      1. the failing command
      2. the artifact files that still fail sign-off
      3. the unresolved field names chosen from `shard_count`, `ingress_capacity`, `ring_size`, `append_latency_observed`, `append_latency_sample_count_delta`, `append_latency_unavailable_reason`, `failure_kind_counts`, and `sample_failures`
      4. the sign-off impact explaining why final archive approval is still blocked
    - Branch B must be used whenever any branch-decision field above is missing, malformed, or semantically insufficient for evidence-safe sign-off; do not clear the blocker based on narrative interpretation alone.

    Also update the `## Blocker status` section in `13.2-CLOSURE.md` so it points to the final blocker disposition and does not contradict it.
  </action>
  <verify>
    <automated>bash -lc 'pass=true; for f in target/phase-13.1/layer-comparison/live-http-unique.json target/phase-13.1/layer-comparison/live-http-shard-1.json target/phase-13.1/layer-comparison/live-http-shard-8.json; do jq -e ".shard_count | numbers" "$f" >/dev/null || pass=false; jq -e ".ingress_capacity | numbers" "$f" >/dev/null || pass=false; jq -e ".ring_size | numbers" "$f" >/dev/null || pass=false; jq -e ".append_latency_observed | type == \"boolean\"" "$f" >/dev/null || pass=false; jq -e ".append_latency_sample_count_delta | numbers" "$f" >/dev/null || pass=false; jq -e "has(\"append_latency_unavailable_reason\")" "$f" >/dev/null || pass=false; done; jq -e ".failure_kind_counts | type == \"object\" and (.sample_failures | type == \"array\") and (.sample_failures | length >= 1)" target/phase-13.1/layer-comparison/live-http-single-hot-key-diagnostic.json >/dev/null || pass=false; rg -n "^## Blocker status$|13\\.2-BLOCKERS\\.md" .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-CLOSURE.md >/dev/null; if [ "$pass" = true ]; then rg -n "^Phase 14 archive blocker: none from Phase 13\\.2\\.$" .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-BLOCKERS.md; else rg -n "^Phase 14 archive blocker:" .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-BLOCKERS.md >/dev/null && rg -n "failing command|artifact files|unresolved field names|sign-off impact" .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-BLOCKERS.md; fi'</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n '^Phase 14 archive blocker:' .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-BLOCKERS.md` succeeds.
    - `rg -n 'accepted debt|future work|follow-up later|nice to have' .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-BLOCKERS.md .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-CLOSURE.md` returns no matches.
    - Branch A applies only if all three success-throughput artifacts expose valid `shard_count`, `ingress_capacity`, `ring_size`, `append_latency_observed`, `append_latency_sample_count_delta`, and `append_latency_unavailable_reason`, and the hot-key diagnostic artifact exposes `failure_kind_counts` plus a non-empty `sample_failures` array; when that gate passes, `rg -n '^Phase 14 archive blocker: none from Phase 13\\.2\\.$' .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-BLOCKERS.md` succeeds.
    - Branch B applies if any branch-decision field is missing, malformed, or insufficient; when that happens, `13.2-BLOCKERS.md` includes the exact labels `failing command`, `artifact files`, `unresolved field names`, and `sign-off impact`, and the unresolved field names cover the failing subset of `shard_count`, `ingress_capacity`, `ring_size`, `append_latency_observed`, `append_latency_sample_count_delta`, `append_latency_unavailable_reason`, `failure_kind_counts`, and `sample_failures`.
  </acceptance_criteria>
  <done>The phase ends with an explicit, evidence-backed blocker disposition that Phase 14 can trust without reinterpreting ambiguous docs, with either the exact blocker-clear line or a complete blocker payload.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| regenerated evidence -> closure narrative | The final archive language must not narrow beyond what the new fields actually support. |
| diagnostic artifact -> blocker ledger | Missing failure taxonomy must become an explicit blocker if the artifact still cannot explain the failures. |
| shared docs -> future phase sign-off | Operators and later planners will reuse these docs, so stale or overstated wording is a long-lived risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.2-14 | R | `13.2-CLOSURE.md` and shared docs | mitigate | Tie every final ceiling statement to regenerated artifact fields, especially `append_latency_observed`, `append_latency_unavailable_reason`, and lane metadata fields. |
| T-13.2-15 | T | `13.2-BLOCKERS.md` handoff | mitigate | Convert any remaining append-latency or hot-key evidence gap into an explicit Phase 14 blocker with command, artifacts, unresolved fields, and sign-off impact. |
| T-13.2-16 | D | observability guidance | mitigate | Preserve the bounded-label rule and avoid recommending any new high-cardinality metric labels while describing the repaired evidence contract. |
</threat_model>

<verification>
- `jq -e '.append_latency_observed, .append_latency_sample_count_delta, .append_latency_unavailable_reason, .shard_count, .ingress_capacity, .ring_size' target/phase-13.1/layer-comparison/live-http-unique.json`
- `jq -e '.failure_kind_counts, .sample_failures' target/phase-13.1/layer-comparison/live-http-single-hot-key-diagnostic.json`
- `rg -n 'Phase 13\\.2|append_latency_observed|append_latency_unavailable_reason|failure_kind_counts|Phase 14 archive blocker' .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-CLOSURE.md .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-BLOCKERS.md docs/stress-results.md docs/template-guide.md`
</verification>

<success_criteria>
- Archive-facing docs no longer claim a storage-append ceiling unless regenerated live HTTP evidence directly measures it.
- The closure memo and blocker ledger are consistent with the regenerated append-latency and hot-key fields.
- Any unresolved evidence gap is visible as explicit Phase 14 blocker work rather than accepted narrative debt.
</success_criteria>

<output>
After completion, create `.planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-05-SUMMARY.md`
</output>
