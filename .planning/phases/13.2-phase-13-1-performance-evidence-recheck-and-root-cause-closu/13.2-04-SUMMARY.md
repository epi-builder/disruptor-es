---
phase: 13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu
plan: 4
subsystem: testing
tags: [rust, stress, http, diagnostics, artifacts, performance]
requires:
  - phase: 13.1-disruptor-throughput-bottleneck-investigation-and-runtime-st
    provides: live HTTP comparison artifacts and baseline regeneration flow
provides:
  - explicit append-latency availability semantics in live HTTP stress reports
  - self-describing shard lane metadata in archived HTTP JSON
  - bounded hot-key failure taxonomy and sample diagnostics
  - refreshed baseline ledger for Phase 13.1 comparison artifacts
affects: [phase-14-archive-sign-off, stress-results, template-guide, verification]
tech-stack:
  added: []
  patterns:
    - explicit observed-vs-unavailable report fields for measured metrics
    - bounded failure taxonomy with capped diagnostic samples in archived artifacts
key-files:
  created:
    - .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-04-SUMMARY.md
  modified:
    - crates/app/src/stress.rs
    - crates/app/src/http_stress.rs
    - crates/app/src/main.rs
    - scripts/compare-stress-layers.sh
    - .planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-BASELINE-REGEN.md
    - target/phase-13.1/layer-comparison/live-http-unique.json
    - target/phase-13.1/layer-comparison/live-http-shard-1.json
    - target/phase-13.1/layer-comparison/live-http-shard-8.json
    - target/phase-13.1/layer-comparison/live-http-single-hot-key-diagnostic.json
key-decisions:
  - "Keep append latency nullable in archive JSON and pair it with explicit availability, sample-count delta, and unavailable reason fields."
  - "Store shard_count, ingress_capacity, and ring_size directly in each external HTTP report so comparisons do not depend on filenames."
  - "Treat non-success HTTP responses as bounded diagnostic samples with stable kinds instead of opaque harness failures."
patterns-established:
  - "Evidence contract: measured metrics use null + reason instead of zero fallbacks when observation is unavailable."
  - "Diagnostic artifact safety: failure samples record only bounded status/code/kind/message fields and cap samples at five."
requirements-completed: [RUNTIME-01, RUNTIME-02, RUNTIME-05, TEST-03, TEST-04, OBS-02]
duration: 34min
completed: 2026-04-28
---

# Phase 13.2 Plan 4: Performance Evidence Recheck and Root-Cause Closure Summary

**Live HTTP evidence now reports explicit append-latency availability, embedded shard lane metadata, and bounded hot-key failure diagnostics in the regenerated Phase 13.1 archive bundle**

## Performance

- **Duration:** 34 min
- **Started:** 2026-04-28T09:02:47+09:00
- **Completed:** 2026-04-28T09:36:35+09:00
- **Tasks:** 2
- **Files modified:** 13

## Accomplishments
- Replaced ambiguous `append_latency_p95_micros: 0` fallbacks with nullable latency plus `append_latency_observed`, `append_latency_sample_count_delta`, and `append_latency_unavailable_reason`.
- Added `shard_count`, `ingress_capacity`, and `ring_size` to every external HTTP report so archived shard comparisons are self-describing.
- Added bounded `failure_kind_counts` and `sample_failures` to the hot-key diagnostic lane and regenerated the baseline evidence bundle plus ledger.

## Task Commits

Each task was committed atomically:

1. **Task 1: Replace ambiguous append-latency fallback with explicit availability semantics and persist lane metadata in the JSON contract**
   - `b08e029` (`test`)
   - `0a9a1ee` (`feat`)
2. **Task 2: Capture hot-key failure taxonomy and regenerate the affected live HTTP artifacts plus baseline ledger**
   - `feb5d37` (`test`)
   - `a9cae87` (`feat`)

## Files Created/Modified

- `crates/app/src/stress.rs` - Expanded the shared report contract with append-latency availability, lane metadata, and bounded failure diagnostics.
- `crates/app/src/http_stress.rs` - Classified external HTTP failures, capped samples, and fixed histogram unavailability reporting for live metrics.
- `crates/app/src/main.rs` - Serialized the new report fields in the CLI JSON output.
- `scripts/compare-stress-layers.sh` - Guarded baseline regeneration on the new report schema.
- `.planning/phases/13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu/13.2-BASELINE-REGEN.md` - Recorded the regenerated append-latency, lane metadata, and hot-key diagnostic fields.
- `target/phase-13.1/layer-comparison/*.json` - Refreshed the live HTTP evidence files with the new contract.

## Decisions Made

- Nullable append latency is now the only archive-safe representation for missing histogram evidence; `0` is no longer used as a silent fallback.
- The hot-key diagnostic artifact records bounded `status_code`, `api_error_code`, `kind`, and `message` fields only, preserving diagnosability without leaking high-cardinality identifiers.
- The baseline rerun continues to regenerate the full comparison bundle, but plan sign-off focuses on the four live HTTP JSON artifacts and the ledger.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Raised the CLI JSON macro recursion limit**
- **Found during:** Task 1
- **Issue:** Adding the new report fields pushed `serde_json::json!` in `crates/app/src/main.rs` past the crate recursion limit, blocking `cargo test -p app`.
- **Fix:** Added `#![recursion_limit = "256"]` to the binary crate so the expanded report JSON compiles cleanly.
- **Files modified:** `crates/app/src/main.rs`
- **Verification:** `cargo test -p app -- --nocapture`
- **Committed in:** `0a9a1ee`

**2. [Rule 1 - Bug] Marked positive append-count windows as unavailable when finite bucket coverage is missing**
- **Found during:** Task 2
- **Issue:** Regenerated success-throughput artifacts had positive append sample deltas but no finite bucket able to satisfy the p95 lookup, which previously left `append_latency_unavailable_reason` empty.
- **Fix:** Updated the unavailable-reason logic to emit `missing_histogram_count` whenever count deltas exist but no finite p95 bucket can be derived, and added a regression test.
- **Files modified:** `crates/app/src/http_stress.rs`
- **Verification:** `cargo test -p app -- --nocapture`; regenerated live HTTP artifacts now carry `append_latency_unavailable_reason: "missing_histogram_count"`
- **Committed in:** `a9cae87`

---

**Total deviations:** 2 auto-fixed (1 blocking, 1 bug)
**Impact on plan:** Both fixes were necessary to keep the report contract truthful and the regenerated evidence reproducible. No architectural scope change was introduced.

## Issues Encountered

- `cargo` hit shared build-directory lock contention in the multi-agent workspace. Waiting for the lock was sufficient; no manual cleanup was needed.
- The regenerated evidence files live under ignored `target/`, so they had to be staged explicitly with `git add -f` for the task commit.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 14 can now consume self-describing live HTTP artifacts that explicitly distinguish unavailable append-latency evidence from observed values.
- The hot-key diagnostic artifact now explains the failure class (`domain`) and provides bounded sample responses suitable for archive review.
- The success-throughput live HTTP lanes still do not expose a finite append-latency p95; the archive evidence now states that limitation explicitly with `missing_histogram_count` instead of implying a measured zero.

## Self-Check

PASSED

---
*Phase: 13.2-phase-13-1-performance-evidence-recheck-and-root-cause-closu*
*Completed: 2026-04-28*
