---
milestone: v1.0
audited: 2026-04-21T08:30:00Z
status: gaps_found
scores:
  requirements: "39/39 evidenced; source-of-truth traceability reconciled"
  phases: "10/10 completed phase dirs verified; Phase 11 complete"
  integration: "runnable HTTP service restored; external-process HTTP closure still pending"
  flows: "official app serve path works; FullE2eInProcess still bypasses HTTP"
---

# Milestone v1.0 Audit

## Result

**Status:** gaps_found

Phase 11 closed the archive-evidence and runnable-service blockers from the previous audit:

1. Phase 10 now has a formal verification artifact.
2. `REQUIREMENTS.md`, `ROADMAP.md`, and `STATE.md` no longer leave Phase 7-proven items pending.
3. `cargo run -p app -- serve` now boots the official HTTP router and has smoke coverage through `/healthz` plus a real HTTP order request.

This milestone is still **not archive-ready**, but the remaining blockers are now the intentionally separated follow-up phases rather than stale evidence-chain defects:

- **Phase 12:** external-process HTTP E2E, stress, and benchmark closure against the real `app serve` path
- **Phase 13:** remaining milestone-critical validation hygiene and final archive sign-off

## Phase Verification Summary

| Phase | Status | Audit Note |
|---|---|---|
| 01 Workspace and Typed Kernel Contracts | passed | VERIFICATION.md present |
| 02 Durable Event Store Source of Truth | passed | VERIFICATION.md present |
| 03 Local Command Runtime and Disruptor Execution | passed | VERIFICATION.md present |
| 04 Commerce Fixture Domain | passed | VERIFICATION.md present |
| 05 CQRS Projection and Query Catch-Up | passed | VERIFICATION.md present |
| 06 Outbox and Process Manager Workflows | passed | VERIFICATION.md present |
| 07 Adapters, Observability, Stress, and Template Guidance | passed | VERIFICATION.md present |
| 08 Runtime Duplicate Command Replay | passed | VERIFICATION.md present |
| 09 Tenant-Scoped Runtime Aggregate Cache | passed | VERIFICATION.md present |
| 10 Duplicate-Safe Process Manager Follow-Up Keys | passed | `10-VERIFICATION.md` added and grounded in current regressions |
| 11 Evidence Recovery and Runnable HTTP Service | passed | `app serve`, `/healthz`, external-process smoke, docs, and evidence repair complete |

## Requirement Cross-Check

All 39 v1 requirements are evidenced and the source-of-truth traceability is now reconciled.

Phase 7 remains the original satisfying implementation phase for `API-02`, `API-04`, `OBS-01`, `TEST-03`, `TEST-04`, and `DOC-01`; Phase 11 adds evidence recovery plus the runnable HTTP service path without downgrading those requirements back to pending.

## Remaining Blockers

### B1: External-Process HTTP E2E/Stress/Benchmark Closure Is Still Pending

`app serve` now exists and is smoke-tested, but the canonical external-process HTTP workloads still belong to Phase 12. The current `FullE2eInProcess` stress scenario still constructs `CommandEnvelope` directly and does not measure real HTTP client/process overhead.

### B2: Final Validation Hygiene and Archive Sign-Off Are Still Pending

Partial Nyquist validation cleanup for earlier phases and any remaining milestone-critical hardening still route to Phase 13. The milestone should not be archived until those follow-up phases are complete and the audit is rerun.

## Cross-Phase Flow Results

| Flow | Status | Notes |
|---|---|---|
| Runtime cache isolation across tenants | wired | Phase 09 remains verified. |
| Duplicate command replay across process manager/runtime/store | wired | Phase 10 remains verified with a formal artifact now present. |
| HTTP command request -> adapter routes -> app binary server | wired | `app serve` composes `adapter_http::router(HttpState)` and exposes `/healthz`. |
| External-process HTTP smoke through the official service path | wired | `crates/app/tests/serve_smoke.rs` boots the binary, waits for readiness, and submits a real HTTP order request. |
| Full E2E stress through the HTTP adapter path | partial | Phase 12 still needs to replace in-process shortcut coverage with external-process HTTP workloads. |
| Archive evidence chain across phases and requirements | wired | Phase 10 verification exists and source-of-truth docs are reconciled. |

## Verification Performed During Audit Refresh

```bash
cargo test -p app duplicate_product_lines_emit_distinct_reserve_keys -- --nocapture
cargo test -p app duplicate_product_line_failure_releases_distinct_prior_lines -- --nocapture
cargo test -p app process_manager_replayed_followups_return_original_outcomes -- --nocapture
cargo test -p app commerce_process_manager -- --nocapture
cargo test -p es-runtime runtime_duplicate -- --nocapture
cargo test -p adapter-http -- --nocapture
cargo test -p app --no-run
cargo test -p app serve_smoke -- --nocapture
```

All passed during this audit refresh.

## Routing

The practical next move is **Phase 12 execution** against the now-official `app serve` path, followed by Phase 13 validation/debt closure and a final audit rerun.
